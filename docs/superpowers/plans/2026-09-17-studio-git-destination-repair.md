# Studio typed-Git destination/ref repair

**Mission:** remove review `5242168925`'s two reproduced release blockers on parent #696, preserving its existing publisher and mandatory controls.
**Authority:** current live Chairman implementation continuation; parent ruling `5722415125`. Original operation `studio-typed-git-destination-repair-20260917-sol-001` is an isolated stacked repair at parent `42d50bf67e2d3ae02cd0ea22723a316b8169cf0e`; no direct parent mutation or reopening of its released writer.
**Procedural pin:** protected `320f586126b7c82c843ef17612f12d40d20a42e0`, Skillpack 1.0.1. Technical writer-gate and exact release custody remain required before integration/transfer.

## Bounded design
Qualify exactly one effective push destination against the host allowlist before network observation. Pin the existing origin URL and pushURL lists per Git invocation, clearing lower-priority values and supplying the captured destination. Validate that the pinned fetch/push views resolve to that destination. Keep origin's hook identity, credentials and repository controls. Push only the fenced SHA/ref without follow-tags, force or mirror widening. Refuse submodule-pushing policy rather than disable a mandatory check. Reconcile the captured destination after a push even if origin configuration changes; never repeat an uncertain effect.

## Executed and remaining
- [x] Recovered the original dirty operation after interruption; no reset, replacement branch or discarded work.
- [x] Incumbent real-Git fixture now covers destination multiplicity/rewrite, ref scope, origin change, lost reply, hooks and submodule policy. Parent control: 19 pass / 11 intended failures; original 16 cases stay green.
- [x] Recovered implementation passes the 30-case focused suite and full adjacent source suite.
- [x] Immutable output child #785 plus this repair passes all 119 integration tests. Evidence: `research/STUDIO_TYPED_GIT_DESTINATION_REPAIR_2026-09-17.md`.
- [ ] Publish the original operation branch as one stacked repair; independent exact-head review and parent composition are separate gates.

## Pre-publication continuation
Local HEAD before commit is the exact parent above. Current local changes are the two publisher source/test files, this plan and the evidence report. Search found no remote PR for this operation. Next effect: commit these four exact paths, publish only the original operation branch, verify remote identity, then request independent review. Preserve parent #696, child #785 and #706 dirty source. No installed service, provider, Project-setting or production effect is authorized by this checkpoint. Review request is not START; tests are not production acceptance.
