# Active rotation context retrieval

Operation: prophet-rotation-consumer-read-20260909-sol-001.
Admission: Macro7000/comment5601033167; current Chairman suite-recovery directive.
Base: 686af274d8ae1558f3f3ae35e0b3aae68be80a01.

Goal: real existing get_sector_rotation and market_packet preserve group identity,
disclose unavailable/omitted context and retrieve every valid group in bounded pages.
No new rank, signal, size, alert, automatic review, scheduler, store or trade authority.

Owned: brain/portfolio_intelligence.py; brain/autonomous_mcp.py;
tests/test_portfolio_intelligence.py; tests/test_portfolio_intelligence_mcp.py;
this plan. Existing generic transport and all portfolio writers remain unchanged.

- [ ] Add regressions to the existing public library and actual MCP handler suites.
- [ ] Prove intended failures before source edits; SDK skips are not handler proof.
- [ ] Extend the existing fixed-path reader with a strict bounded option for rotation.
- [ ] Keep current/stale/unknown/all views and original authored ordering in each.
- [ ] Keep native IDs; quarantine malformed/duplicate identities with counts.
- [ ] Bind stateless section/offset continuation to the exact content generation.
- [ ] Fit actual returned rows below7600UTF8bytes; disclose exact continuation.
- [ ] Propagate identity/coverage/retrieval through the initial compact market packet.
- [ ] Run owning suites, adverse cases and exact actual-source snapshot consumption.
- [ ] Publish one Draft/HOLD; obtain official source checkpoint and independent review.
- [ ] Consume current natural CI; require separate explicit release/served proof.

Declared source date and row stale flags are not proof of market observation or
knowledge time. Unknown clocks remain disclosed. Current-view filtering is
freshness projection, not a ranking promotion. No previously blocked host query,
uncertain source operation, legacy activation or production PM request is replayed.

## Test-first checkpoint, not implementation

The first59-line test delta executes11 failing cases on the unchanged reader:
missing native IDs, missing view/page/generation API, and invalid requests reaching
I/O. Existing library cases remain10PASS. In an isolated environment installed
from this repository's declared .[dev] dependencies, the existing actual SDK/MCP
suite also runs: combined22PASS/11FAIL, zero skips. This is not a repaired result.

A further malformed-input test append and a production reader edit were refused
by platform safety checks. Neither was rerouted. A subsequent scoped Git read
confirms only the59-line tests and this plan differ; production files are unchanged.
This source operation stays held. Publishing this coherent test-first checkpoint
must not be presented as a feature implementation, a passing suite or release.
The older China weekly-member operation retains its separate unknown test effect.
