# Watcher Resource + Carrier Freshness — Current-Source Reconciliation

**Carrier:** Mastermind PR #241 / branch `sol/watcher-resource-freshness-plan-20260828`; closed draft #207 is metadata/history only.

**Purpose:** narrow current-source amendment to the historical rollout plan in `2026-08-28-watcher-resource-freshness-implementation.md`. This record changes no watcher/source-law/runtime behavior. Where current carrier facts below conflict with the older plan's planning receipts or collision examples, this record wins; the rollout architecture and non-goals remain unchanged.

## Current protected predecessor

Chairman-approved watcher design PR #205 is protected, and the later Agent Relay enrollment release PR #238 is now also protected. Current protected Mastermind is `7b8f3ca580f9872ca8ddf60f90c6022ce4a18e6b`, Skillpack `mastermind.sol_skillpack.v1` v1.0.1 / bootstrap-major 1.

The #238 movement is implementation in the Agent Relay enrollment surface and does not touch either Watcher Resource planning file. PR #241 was history-preservingly reconciled onto `7b8f3ca5...` on the same branch; immediately before this record correction, current-base compare was `behind_by=0` with exactly the two Watcher Resource plan/records paths.

Protected source still does **not** implement the Chairman-approved watcher resource/freshness law. #205 is `SPEC_ONLY`; #238 is Agent Relay enrollment. Neither changes the 60m/15m cadence law, principal-yield rule, fresh-read-before-substantive-write procedure, runtime enforcement, watcher registry, or Executive lifecycle.

## Mastermind Wave-A collision

Mastermind PR #147 (`Continuation Delta Law — Skillpack 1.1.0`) remains OPEN/DRAFT/HOLD and nonmergeable. It overlaps Wave-A Skillpack paths including `docs/sol_skills/INDEX.md`, `COMMISSION_WAVE.md`, and `RECONCILE_STATE.md`.

Sol's current composition ruling remains:

1. Wave A may precede #147 because #147's overlapping changes are independent Continuation-Delta semantics, not competing watcher-resource policy.
2. Wave A must not import unmerged #147 bytes or silently delete/duplicate its continuation semantics.
3. Immediately before Wave-A RED/source-law implementation, re-pin then-current protected `master` + same-SHA Skillpack and re-read #147 exact head/state and every overlapping path.
4. #147 later recomposes/revalidates on then-protected source and still owes its 16-run fresh-Sol behavioral matrix.
5. If a future path-level conflict becomes materially ambiguous, stop with a typed decision/collision return rather than overwrite either law.

#147 is not a substitute carrier for watcher-resource/freshness and this plan does not authorize merging #147 or bypassing MAS-136.

## Macro bootstrap collision — current owner is #6660

The older plan and earlier amendment named #6381 / #6652 as the active overlapping Macro `AGENTS.md` / `CLAUDE.md` carrier. Both facts are now stale:

* Macro #6381 is **CLOSED / UNMERGED / donor-only**; never reopen/rebase/merge it.
* Macro #6652 is **CLOSED / UNMERGED metadata/history** for accepted `ci-quiescence-v2-20260829-sol-001` implementation.
* Active same-branch non-draft release carrier is Macro **#6660**, still OPEN at accepted head `a170f1a362fd3d55ad94877399294229600b2479`, and it owns Macro `AGENTS.md`, `CLAUDE.md` plus the accepted CI-quiescence runtime/test surface until terminally reconciled.

Therefore the later Macro bootstrap projection from Watcher Resource remains dependency-held until #6660 is terminally reconciled. Do not edit Macro `AGENTS.md` or `CLAUDE.md` from this program while #6660 is nonterminal, and do not copy donor #6381/#6652 bytes around it.

## Current plan-release state

No Wave-A source-law implementation has STARTED. PR #241 remains `PLAN_ONLY / RECORDS_ONLY` on the same planning branch. Its predecessor exact-head CI was green, and the branch has been current-based to protected `7b8f3ca5...`; this record correction requires fresh exact-head CI/review before any plan release decision.

Current `SOL-DIR-PRO / director_epoch=20260829-pro-001` Mastermind release serialization also remains a live authority gate unless the bound director explicitly releases it. Corrected effective fence: after #238 protected, do not merge nonurgent Mastermind records/runtime carriers merely to create movement until canonical AD-ID1 release #239 is protected and the first immutable AD-CHILD1 review head is established, or until the director explicitly releases/overrides the fence.

Therefore #241 may continue current-base reconciliation, CI and review, but a green test alone does not authorize merge while that serialization remains active.

After the plan is protected/durable and the release fence is clear, ordinary unbound Wave-A implementation work is `WAITING_CAPACITY / needs_placement` until the canonical placement owner selects an exact eligible receiver. Do not emit `OPEN_PICKUP` or `ACCOUNT_BINDING: CHAIRMAN_SELECTS` merely because automated placement is incomplete. Preferred bounded avenue is CTO Sol/Codex or Terra; Fable is not justified while the law/tests are frozen and path reconciliation is bounded.

## No-rebuild boundary

Preserve the original plan's architecture:

* one universal procedure owner, not a new watcher-policy store;
* existing Agent Dialogue / Worker Presence / Wake / owner-specific enforcement surfaces only;
* no new watcher daemon, watcher DB, queue, cursor, retry plane, lifecycle owner, session registry, capacity router, or provider authority;
* design/plan/CI/merge are not production proof;
* active-session visibility is transport only and never substitutes for a consumed carrier read;
* Wake exact-session consumption remains owned by the existing Wake/RuntimeBinding architecture (MAS-229 ACK1), not by Watcher Resource procedure.
