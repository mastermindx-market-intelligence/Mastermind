# Direct-provider supervisory convergence delivery plan

> **For agentic workers:** Use the existing admitted owner's execution plan, with `superpowers:executing-plans` or `superpowers:subagent-driven-development` only when that operation permits it. Checkboxes below are delivery gates, not new worker assignments.

**Goal:** Complete a useful provider-to-supervisor loop without another Executive OS or routine Chairman account/prompt allocation.

**Architecture:** Preserve the sealed WorkerExecutionAdapter and rich OperatorHarnessAdapter; use their existing lifecycle, claim, result and Wake owners. Finish current source repairs, then prove small direct-provider and supervisory slices before expanding the fleet.

**Tech stack:** existing Python contracts, provider-supported stdio interfaces, current Runtime and test infrastructure; no new dependency.

**Spec:** `docs/superpowers/specs/2026-09-06-provider-app-server-adapter-fabric-design.md`.

**Original operation/carrier:** `mastermind-provider-app-server-adapter-fabric-f0-20260906-sol-001` / `C0BSBM78V1N/1788731079.486769`.

## Global constraints

This four-path F0 is records and static tests only. It creates no runtime, provider, credential, host-service, adapter-registration or installation effects. Current compatible protected law, exact owner, source and effect gates apply at action time. A later task inherits no START from this plan. Existing admitted HF/PF/Personal work continues independently; this plan is not another prerequisite to redo it.

No new Job, Attempt, Worker, Event, queue, lifecycle, identity, session store, transcript authority, permission service, scheduler, retry journal, provider-specific broker or memory system. Existing budgets are unchanged. Requested/observed model, account realm, capability grant, process generation and workspace remain independently bound. Unknown capacity is not free capacity; effect uncertainty forbids provider/account/host failover.

Keep source, review, passing CI, protected release, installed service, actual provider work, leader consumption and final acceptance separate. Do not call a documentation test a provider canary. The [estate](../../../research/PROVIDER_APP_SERVER_ADAPTER_ESTATE_2026-09-06.md) records dated observations and accepted child corrections, not a permanent current-state database.

## Task 1. Publish the original F0 selection, not a new program

**Files:** exactly the estate, design, this plan and `tests/test_provider_app_server_adapter_source_law.py`.

**Consumes:** protected hybrid topology, common-worker/PF1/MH1 plans, existing ACP source law, the C0/A0/G0 accepted corrections with their direct Root qualifications.

**Produces:** one immutable Draft PR on `sol/provider-app-server-adapter-fabric-f0-20260906`.

- [ ] Verify absence or exact existing ownership of the four paths and branch; use only one isolated source workspace.
- [ ] Write the record tests first and observe the intended missing-design assertion before creating the design.
- [ ] Write the three records, preserving both interface floors and the provider-specific first-slice distinctions.
- [ ] Execute the real record validator and all paired forbidden record mutations. Each negative case first validates the unchanged positive record, so missing or malformed baseline evidence cannot produce false green.
- [ ] Verify AST joins to the existing interface, result and intent seams without importing or launching project/provider code.

Run from the owned repository root:

```bash
python3 -I -B tests/test_provider_app_server_adapter_source_law.py -v
python3 -m pytest tests/test_provider_app_server_adapter_source_law.py -q
git diff --check
```

The first command is dependency-free. The second belongs to the existing qualified repository environment and natural CI; do not install into a shared worker environment just to duplicate the first command. Report their results separately. The record tests must refuse second lifecycle ownership, widened source/runtime scope, invented provider interfaces or quota, silent SDK/binary substitution, cancellation-by-ACK, effect-unknown retries/failover, hidden material helpers and false production claims. They do not prove runtime enforcement of those refusals.

- [ ] Review all four files against the Chairman outcome; source validators do not establish narrative completeness or permission safety.
- [ ] Record exact head/tree, four modes/blobs, current protected relationship, source cleanliness and effect ledger; push once by ordinary non-force advancement after rechecking the carrier and remote preimage.
- [ ] Open exactly one Draft/Hold PR. Deliver its actual immutable coordinates to the existing review root `1788731260.477349`; do not create a new reviewer operation.
- [ ] Consume independent non-author architecture/security review and actual current-base test/security checks. Resolve findings on this same source writer rather than generate a replacement branch.
- [ ] After accepted source result, explicitly close the source child and establish writer release before any separately admitted source-release maintenance. Unknown effects remain sticky and unreplayed.

**Stop:** exact four-path source result plus typed review/check disposition. F0's conditional Agent OS root `1788732558.668149` remains gated until positive protection; do not execute it early or invent another durable memory home.

## Task 2. Finish already-owned source prerequisites in parallel

**Files and owners:** no F0 edits to these lanes. HF1 owns #471 and its complete current eighteen-path closure. PF1-F0 owns #455's `control_plane/claude_cli_protocol.py`, `scripts/ohf/fake_claude_cli.py`, `tests/test_claude_cli_protocol.py`, and `tests/test_fake_claude_cli.py`. The Personal first-read controller owns its existing fourteen paths on root `1788719305.919709`; the plural reader, Wake/continuity and package-release owners retain their independent carriers.

- [ ] Consume HF1's actual final common-policy/caller/package result, review and hosted checks. Preserve adapter-private provider-home ownership and the real extracted-package boot test; source presence alone is not package closure.
- [ ] Consume PF1-F0's current-source START and real return. Its acceptance deadline must cover stream drain, EOF, exit observation and terminal promotion; cleanup grace cannot rescue an expired result.
- [ ] Keep PF1-F0's reviewed CLI versions 2.1.248 and 2.1.259 closed, with invocation-policy digest, complete fake-failure evidence, cumulative usage non-regression and no native provider call. A newly documented SDK field does not authorize a new fake profile.
- [ ] Complete the current first-read correction using its existing controller, not a recovered obsolete Task 1. Preserve the four read tools, outer authentication/inner re-verification, raw-path/metadata refusals, physical-reader accounting and bounded retained frame storage.
- [ ] Let the plural-reader owner dispose of existing review/release evidence; retain the distinct singular action-target ambiguity guard.

**Acceptance:** each existing owner returns one actual immutable source result and its required consumer/test proof. No new broad discovery task or duplicate completed review is needed. Production activation remains separately gated; a downstream production dependency does not prohibit independent, already-authorized source work.

## Task 3. Prove one selected direct-provider vertical

**Execution owner:** existing Capacity/Runtime/provider owner, selected by actual source readiness and eligibility. F0 does not elect an account or replace a started task.

**Files:** reuse the relevant existing plan below; its current complete producer/consumer closure is the source boundary, not a new list inferred from a provider brand.

**Interfaces:** sealed `WorkerExecutionAdapter` or rich `OperatorHarnessAdapter`, existing broker, RuntimePort and accepted result consumer.

| Selected first lane | Existing implementation source / plan | Useful completion |
| --- | --- | --- |
| Codex rich operator | `control_plane/codex_operator_adapter.py`, `operator_harness_orchestrator.py`, existing worker broker/materialization and current operation | One admitted Attempt reaches the direct App Server, returns a bound result through the canonical consumer and settles cleanup/effects |
| Claude sealed worker | `docs/superpowers/plans/2026-08-27-hybrid-workforce-pf1-claude-worker.md` plus current PF source law | One real bounded child through the common foreground-worker path; not SDK persistence, native background agents or generalized routing |
| Grok ACP worker | `research/MASTERMIND_EXECUTIVE_CURSOR_GROK_AUTH_ACP_SOURCE_LAW_2026-08-25.md` section 6 and existing G0-B/Capacity qualification | One neutral ACP implementation/manifest reaches an admitted child and canonical result, with functional permission/cancel behavior |

Only one ready lane is needed to establish the first complete supervisor loop. This is not permission to reroute an uncertain existing Attempt. A different provider can be selected for new, unstarted eligible work only through current routing and admission.

- [ ] Reconcile the selected lane's exact latest source and current owner; do not infer deployment from module headers or old prose.
- [ ] Under the existing realm owner, qualify the actual executable, supported profile, intended account and requested/observed model. No credential export, new API key or authentication substitution is admitted by this plan.
- [ ] Pair a valid launch fixture with wrong-version, wrong-realm, widened-tool, missing-session and malformed-result refusals through the same code path.
- [ ] Prove one committed intent precedes one permitted native dispatch, then exercise response loss using existing effect reconciliation. A repeated request must not duplicate dispatch; a changed request must not reuse the old operation identity.
- [ ] Prove cancellation at its actual terminal boundary, followed by identity-safe process/epoch cleanup. Preserve unknown outcome when that boundary cannot be observed.
- [ ] Admit one exact low-risk real child only after the necessary source, runtime, transport, permission and capacity gates clear. Require canonical result/validation consumption and visible output, not merely provider stdout.

**Proof:** exact Job/Attempt/Worker/operation/epoch identities, source and profile hashes, selected executable identity, non-secret realm evidence, requested/observed model, bounded result/usage facts, validation/review, cleanup and actual Runtime transitions. Author and independent review remain distinct. A real canary needs its own finite provider-effect allowance; none is spent by F0.

**Stop:** one accepted direct lane, or the first actual missing capability with exact owner and recovery edge. Do not default every unavailable observation to a new system. No general routing arming, credentials, GUI migration or multi-host activation follows automatically.

## Task 4. Close one real parent/worker/supervisor loop

**Owners and files:** existing Executive parent/child orchestration and result validation; first-read E1 source; #508 plural reader; existing RuntimeBinding/Wake and #491 continuity; existing Control Room/Steward projections. No new reader/store, and no F0 source edits to their files.

**Consumes:** one qualified direct-provider path and authoritative authenticated reads.

**Produces:** one responsibility that survives an interrupted leader conversation with a correctly consumed bounded decision.

- [ ] Admit a parent and two bounded children through the existing lifecycle. Freeze their workspaces, tools, independence and result contracts using current owner configuration.
- [ ] Show both children through the authorized plural reader. Inject a missing or stale join in the qualified fixture and verify truthful unknown/truncated state rather than a fabricated current operator.
- [ ] Have one child return evidence and the other request a bounded decision. Verify the exact parent receives the actual result and decision, not merely a Slack message or provider-session label.
- [ ] Have the leader reject one artifact for a concrete scoped defect and issue a permitted repair. Require exact artifact-bound independent review and actual receiving-worker continuation consumption.
- [ ] Interrupt the qualified leader at the agreed safe boundary. Recover its responsibility and outstanding decisions through existing RuntimeBinding/Wake and durable records; stale-session mutation must refuse.
- [ ] Complete and close the child operations explicitly, removing only their continuation sources while preserving permanent seat/aggregate resources.

**Acceptance:** no Chairman relay, no duplicate effect, one traceable review/repair cycle, correct parent consumption, and a useful machine projection plus browser proof for a user-facing view. A test-only dashboard or fake Runtime database cannot substitute for this acceptance.

## Task 5. Expand only through the same owners

**Files:** existing hybrid workforce, resource, provider-capacity and `2026-08-27-hybrid-workforce-mh1-multihost-broker.md` plans. No F0 runtime change.

**Consumes:** the accepted small loop and current source contracts.

**Produces:** measured mixed-provider and later multi-host operation without dependence on a single chat or desktop.

- [ ] Add the next already-qualified provider to new eligible work; preserve each adapter's different session and cancellation semantics rather than invent uniform support.
- [ ] Observe genuine native helper lineage under the admitted parent. Independently effectful work must enter the existing child-work mechanism before execution, not after an observer discovers it.
- [ ] Increase the admitted concurrent workload within existing budget and host limits. Measure build/test/browser pressure separately from cloud inference waiting; reserve supervisory and recovery headroom through current Capacity policy.
- [ ] Exercise one quota exhaustion and one worker crash in qualified resources. Distinguish a known pre-start refusal from an uncertain in-flight action; only the former permits ordinary fresh placement.
- [ ] Through MH1, qualify one second execution host with host-local credentials and exact workspace identity. Interrupt transport and reconcile the same Attempt before any later placement decision.
- [ ] Compare accepted outcomes, elapsed time, review/repair cost and Chairman interventions against the initial loop. Report denominators, exclusions and failure counts; agent count and token burn are not success metrics.

**Acceptance:** each added provider or host supplies an independently useful vertical and real recovery proof. Unsupported interface, missing realm, unresolved process/effect identity or scope collision is a typed hold, never a reason to manufacture a fallback or new control plane.

## Continuation handoff and release ownership

The next F0 handoff is the actual four-file source result to the already-existing independent-review root `1788731260.477349`. Its reviewer must be truly non-author; changing the authoring account does not create independence. Preserve that review child's separate ACK, START, result and STOP. A source merge, if subsequently authorized, protects records only.

After positive F0 protection, the already-declared conditional Agent OS records operation may record the ruling and exact next action through Macro's `agentos/` owner. Until then the original source carrier and Draft PR own candidate evidence. Do not introduce a parallel memory store or present this plan as installed runtime state.

At every material return record mission, owner, exact source/effect identity, evidence and limitations, pending decision, stop condition and next action. Use the appropriate durable home. No completed C0/A0/G0 researcher should remain polling because the next step belongs to the architecture author, and no failed or uncertain provider attempt is replayed to improve the appearance of a report.

This plan's implementation slices are owner-bound continuations of existing programs. The four-path F0 author has no source authority over them. The sole current deliverable here is the reviewed selection and delivery record; the overall supervisory capability remains `PARTIAL / END_TO_END_NOT_PROVEN` until the real loop is accepted.
