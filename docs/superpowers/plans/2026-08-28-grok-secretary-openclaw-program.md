# Grok Secretary MCP + OpenClaw Subordinate Actuation — Program Plan

> **For agentic workers:** execute one task/PR at a time with current protected source law, test-driven development, exact carrier reconciliation and Sol review. This plan is a dependency-gated program, not authorization to start every task in parallel.

**Date:** 2026-08-28  
**Owner:** Sol, AI CEO  
**Chairman:** Chris  
**Workstream:** existing `WS:CHAIRMAN-CONTROL-ROOM`; no new workstream  
**Operation key for this records wave:** `grok-secretary-openclaw-f0-20260828-sol-001`  
**Goal:** let Chris use Grok as an exception-first company secretary that reads exact Mastermind responsibility/runtime/attention state and, through a separately released private OpenClaw actuator, can inspect and foreground the exact bound ChatGPT, Claude Code or Codex surface without making Grok/OpenClaw a lifecycle, routing, memory, retry or session authority.

**Architecture:** one Mastermind MCP platform with distinct static capability surfaces. Executive MCP remains unchanged. A new Secretary MCP consumes the accepted OCR-6 Executive Steward and current binding owners. Its public HTTPS edge is the only Grok-facing endpoint. OpenClaw stays private behind a Mastermind-authored actuation envelope and is optional/removable.

**Controlling spec:** `docs/superpowers/specs/2026-08-28-grok-secretary-openclaw-integration-design.md`.

**Status:** records-only planning. No task below is live merely because this plan merges.

---

## Global constraints

- Re-pin current protected `master`, current compatible Skillpack, exact owner source law and exact carrier immediately before every modifying task and before push/merge.
- Continue existing carriers for Session Truth, Wake, Worker Presence/WP-2, Capacity/H0 and OCR. Never create replacements to accelerate this program.
- The accepted OCR-6 carrier remains the sole implementation owner for Executive Steward/continuity and the optional subordinate OpenClaw bridge. This plan decomposes reviewable child slices; it does not authorize a second OCR-6 implementation branch.
- Executive OS alone owns Job/Attempt/Worker/Event lifecycle, terminal/requeue/effect reconciliation and CEO-intent admission.
- Agent OS owns durable workstream/wave/decision/discovery/handoff identity. GitHub owns implementation/proof. Slack is transport/hot state. Linear is selective projection.
- Prefer provider-native exact session operations. OpenClaw is presentation/host actuation only.
- No generic shell, SSH, filesystem, arbitrary browser, arbitrary URL, free-form click/type, provider account selection or hidden retry tool.
- Public MCP, private OpenClaw Gateway/node, macOS permissions and provider login are distinct gates.
- One logical modifying operation binds to one operation key/carrier/destination until canonical reconciliation.
- `EFFECT_UNKNOWN` permits zero cross-surface/node/provider failover.
- Green CI, successful MCP tool discovery, connector installation, window foreground and message delivery are distinct from target verification, ACK, source resolution and final acceptance.
- No secret, cookie, private browser URL, raw screenshot, provider PII, node credential, token, host username/path or clipboard payload enters Git, Agent OS, Linear or Slack.

---

## Program DAG

```text
GSO-F0 records freeze (this carrier)
  |
  +--> existing Session Truth acceptance
  +--> existing OCR-3 RuntimeBinding/continuation projection
  +--> existing OCR-6 core Executive Steward
  +--> existing current binding/P0B exact-foreground owner
  |
  v
GSO-R1 read-only Secretary MCP
  |
  v
GSO-N1 public remote-MCP edge
  |
  v
GSO-G1 Grok read-only installation/canary
  |
  v
GSO-A1 OpenClaw private inspect-only bridge
  |
  v
GSO-A2 exact foreground navigation
  |                \
  v                 v
GSO-C1 ChatGPT       GSO-C2 Claude/Codex cockpit proofs
  \                 /
   v               v
GSO-W1 later bounded Wake/message/recovery releases
  |
  v
GSO-A3 final Chairman acceptance + closeout
```

Tasks may proceed only when their named predecessors are accepted, not merely merged.

---

## Task 0 — Publish and accept the records-only source law

**Files:**

```text
docs/superpowers/specs/2026-08-28-grok-secretary-openclaw-integration-design.md
docs/superpowers/plans/2026-08-28-grok-secretary-openclaw-program.md
```

**Observable mission:** protected source law unambiguously defines the product outcome, tool boundaries, owner matrix, security model, dependency DAG, failure law and production acceptance before any new Grok/OpenClaw code or installation.

### Steps

1. Re-read current operating-surface convergence and OCR-6 source law.
2. Confirm this carrier adds records only and does not modify existing owner code/config.
3. Open one draft PR under operation key `grok-secretary-openclaw-f0-20260828-sol-001`.
4. Require current hosted repository checks.
5. Sol adversarial review rejects any second control plane, generic RPA surface, premature actuation, duplicate OCR-6 owner, or claim that records are implementation.
6. Merge only on exact-head acceptance.

**Stop:** after records merge. No implementation task starts on this carrier.

**Return:** merge SHA, exact files, hosted checks and first released dependency action.

---

## Task 1 — GSO-X0 current xAI/OpenClaw/host contract falsification

**Release gate:** Task 0 accepted. Read-only research may run before OCR-6 implementation; installation/mutation may not.

**Files:** one source-law research artifact in the owning repository, exact path chosen at pickup under existing research conventions; no runtime/config edits.

**Observable mission:** current primary-source and installed-host evidence proves the exact contracts available for xAI remote MCP, authentication, OpenClaw Gateway/node invocation, browser/computer-use, macOS permissioning, receipts, timeouts and version identity—without exposing credentials or performing a production actuation.

### Required research

- current xAI/Grok custom remote MCP endpoint, supported transport, authentication and tool-manifest behavior;
- current OpenClaw exact installed version/build/digest on intended host(s);
- Gateway/node pairing and authentication contract;
- exact computer-use/browser/screenshot actions and their receipts;
- whether the Gateway exposes pending/offline work behavior that must be disabled/bypassed for Mastermind;
- node identity and host mapping seam;
- macOS Screen Recording, Accessibility and Automation permission requirements;
- network topology from public Secretary edge to private adapter/Gateway;
- log/redaction behavior and secret-bearing fields;
- timeout/cancellation/effect-unknown behavior;
- unplug/removal behavior.

### Falsifiers

- public xAI cannot reach the proposed endpoint/transport;
- connector cannot provide acceptable scoped auth;
- OpenClaw target identity cannot be bound without creating a new host/session registry;
- OpenClaw requires generic shell authority for foregrounding;
- screenshots/actions expose unredactable secrets under the proposed receipt path;
- offline queue/retry behavior cannot be disabled or bounded;
- exact postcondition cannot be proven for target surfaces.

**Acceptance:** reviewed source document names exact supported/refused capabilities and leaves unsupported fields null. Product docs do not count as installed proof.

**Stop:** no install, login, node pairing, public endpoint or host permission change.

---

## Task 2 — GSO-R1 read-only Secretary MCP facade

**Release gates:** accepted OCR-6 core `ExecutiveSteward` read API; accepted exact current Session Truth/binding joins; Task 1 contract compatible. If OCR-6 is not accepted, do not create a stand-alone fake Steward.

**Expected implementation owner:** the single OCR-6 implementation carrier/current accepted successor. Exact file paths are frozen at pickup after inspecting current code; likely one new package following `integrations/executive_mcp` and accepted company-MCP patterns.

**Observable mission:** a hermetic Secretary MCP server exposes exactly five read-only tools over the accepted Steward and surface-resolution read projections, with no Executive/Slack/OpenClaw mutation.

### Closed tool census

```text
list_responsibilities
get_responsibility
get_attention
get_current_runtime
resolve_surface
```

### Required contract behavior

- static `TOOL_SPECS`, exact server identity/version and pinned schema digest;
- strict `additionalProperties: false` inputs;
- no host, provider account, session/native handle, URL, browser profile, executable, path, action, command, credential or actor override in model inputs;
- trusted resolver supplies exact source identity/currentness;
- one tool call invokes at most one gateway operation;
- no server-side chaining from read result into mutation;
- explicit `UNKNOWN`, `DEGRADED`, `STALE`, `AMBIGUOUS` and `NOT_FOUND` outcomes;
- bounded/redacted output; no raw screenshot/private URL/native handle unless already an accepted public field;
- SDK imported only in `server.py` equivalent;
- no persistence, network egress beyond reviewed downstream read clients, queue, cache or cursor.

### TDD acceptance

RED first for:

- sixth tool added;
- write/destructive annotation;
- privileged identity field smuggled into input;
- fuzzy title join selecting a target;
- stale binding accepted;
- adversarial upstream text changing a tool description or causing a second call;
- hidden persistence/import of runtime mutation APIs;
- overlarge/secret-shaped output.

Then focused tests, mutation/source fences, full relevant MCP/Steward tests, compile, hosted CI and independent review.

**Real proof:** one production-inert/fixed current-estate read canary returns source-attributed responsibilities and one exact `surface_ref` or truthful `UNBOUND`; it performs zero host action.

**Stop:** no public listener, Grok installation or OpenClaw call.

---

## Task 3 — GSO-N1 public remote-MCP transport edge

**Release gates:** GSO-R1 accepted; Task 1 current xAI contract reviewed; credential-owner and deployment-owner micro-freeze accepted.

**Observable mission:** xAI can securely scan and call the exact read-only Secretary tool manifest over one stable public HTTPS endpoint, while Executive MCP's existing transport/binding laws and private Mastermind/OpenClaw services remain unchanged.

### Architecture requirements

- reuse reviewed MCP deployment/tunnel infrastructure when compatible;
- separate Secretary server identity/principal/tool grant from Executive and Company Dialogue MCP;
- TLS hostname and certificate lifecycle under existing infrastructure owner;
- current xAI-supported OAuth/service-auth contract with least privilege;
- exact manifest version/digest attestation at startup and in deployment receipt;
- public request size, response size, concurrency and timeout ceilings;
- no public direct access to AF_UNIX sockets, Executive runtime, OpenClaw Gateway/node, Tailscale or host-local ports;
- no generic reverse proxy that exposes arbitrary internal paths;
- read-only global kill switch/production arming state that does not create another lifecycle store;
- logs sanitized before persistence.

### Negative proof

- wrong principal;
- expired token;
- wrong audience/scope;
- altered manifest digest;
- tool not in grant;
- oversized body;
- concurrent abuse ceiling;
- downstream unavailable;
- prompt injection in returned data;
- direct OpenClaw/internal path attempt.

**Real proof:** remote isolated client authenticates, performs tool discovery, calls one harmless read tool and receives the exact bounded envelope. No Grok UI installation yet.

**Stop:** endpoint remains read-only; no OpenClaw actuator.

---

## Task 4 — GSO-G1 install read-only Secretary MCP into Grok

**Release gates:** GSO-N1 production endpoint accepted; dedicated Grok Secretary account/workspace and admin path identified; current connector installation contract re-pinned.

**Observable mission:** Grok Bot can answer a bounded current-state question through the Mastermind Secretary MCP, with exact tool identity and source-attributed facts, without any host/session mutation.

### Installation ceremony

1. Use the dedicated Grok Secretary principal/workspace.
2. Add the exact approved remote MCP endpoint.
3. Complete provider-native OAuth/service authorization without copying credentials into chat, Git or Slack.
4. Verify discovered server name/version/tool names/digest.
5. Refuse if the provider silently adds ambient tools or broadens scopes.
6. Record a sanitized installation receipt.

### Live canaries

- “What needs Sol or Chairman?”
- “Where is the named responsibility running?”
- “Which surface would be opened?”
- stale/unknown source explanation;
- adversarial text inside an upstream result remains inert.

Prove Grok cannot call Executive write tools, Company Dialogue tools, Wake or OpenClaw.

**Stop:** `GROK_READ_ONLY`. No host permission, node pairing or physical control.

---

## Task 5 — GSO-A1 private OpenClaw inspect-only bridge

**Release gates:** accepted OCR-6/OpenClaw implementation owner; GSO-G1 read-only proof; Task 1 exact installed contract; accepted host identity seam; no collision with active H0/host/capacity carrier.

**Observable mission:** for one exact harmless canary `surface_ref`, Mastermind privately asks the correct paired OpenClaw node for bounded readiness/inspection evidence and returns a sanitized receipt, with zero foreground/click/type/message effect.

### Expected interface

Trusted adapter operation, not Grok tool input:

```text
inspect(surface_ref, prepared_envelope)
```

The adapter receives an immutable Mastermind-authored target envelope. It resolves private node/native details through existing accepted owners. No caller-selected node, host, app, path, URL, command or screen coordinates.

### Required proof

- exact OpenClaw version/digest and Gateway identity attested;
- expected node/host join exact and current;
- no generic shell/filesystem/network capability in grant;
- screen snapshot/action output bounded and sanitized;
- private browser URLs, credentials, proxy args, cookies and unrelated screen regions never enter public receipt/log;
- no OpenClaw pending-work queue/retry/fallback;
- timeout before effect returns typed unavailable/unknown according to exact contract;
- unplug test leaves all canonical Mastermind state and admitted work intact.

**Stop:** inspection only. No focus, click, key, scroll, text or message.

---

## Task 6 — GSO-A2 exact foreground navigation

**Release gates:** GSO-A1 accepted; exact P0B/intended-seat foreground owner/current source law reconciled; macOS permission ceremony approved; one disposable non-production target.

**Observable mission:** one `foreground_surface(surface_ref, operation_key)` call foregrounds and verifies the exact bound canary surface through a native adapter or private OpenClaw node, with no other interaction.

### Closed actions

Implementation may use only the reviewed primitives required to:

```text
observe current UI
focus/activate exact app/window/tab/profile/session
verify exact postcondition
```

No message typing, form submission, clipboard, account switching, provider login, arbitrary navigation or new provider session.

### Immutable checks immediately before action

- current responsibility/root join;
- current `surface_ref` fingerprint/revision;
- expected seat/provider/host;
- expected RuntimeBinding/binding generation where applicable;
- target not expired;
- action policy digest matches deployment;
- exactly one candidate target.

### Adverse TDD/live tests

- wrong seat currently frontmost;
- two matching-looking windows;
- target closed/missing;
- target moved hosts;
- binding generation changed;
- login/MFA page;
- missing Accessibility/Screen Recording;
- hostile on-screen instructions;
- OpenClaw disconnected mid-inspection;
- postcondition not provable.

**Stop:** navigation-only receipt. No wake/send/recovery.

---

## Task 7 — GSO-C1 ChatGPT exact managed-seat/conversation vertical

**Release gates:** GSO-A2 accepted; current managed-browser seat/binding contract re-read; disposable canary environment authorized.

**Observable mission:** from Grok, a company-language request resolves one exact Sol responsibility and foregrounds the exact intended managed-browser seat and exact bound ChatGPT conversation from a minimized/obscured state.

### Complete user journey

```text
Grok request using responsibility/workstream language
→ read-only Secretary resolution
→ exact opaque surface_ref
→ navigation policy acceptance
→ correct private host/node
→ exact managed-browser environment
→ exact bound conversation
→ verified active/foreground postcondition
→ bounded receipt and Grok explanation
```

### Hard boundaries

- never use ChatGPT account switching as addressing;
- never substitute another Sol seat;
- never choose by page/window title alone;
- never expose exact private conversation URL publicly;
- never type/send a prompt;
- no stop/start/delete/edit of GoLogin/Multilogin profile data unless a separately reviewed vendor operation owns that capability;
- real Chairman seats remain untouched until disposable proof is accepted and a separate admin ceremony authorizes them.

### Proof progression

1. hermetic fixtures;
2. disposable non-seat managed-browser canary;
3. one separately approved real seat read/navigation proof;
4. all approved seats, each independently verified;
5. minimized/hidden/other-window-frontmost adverse cases.

**Stop:** exact foreground only.

---

## Task 8 — GSO-C2 Claude Code and Codex cockpit verticals

**Release gates:** GSO-A2 accepted; current Wake/OHF/OCR identities accepted for the surface under test.

**Observable mission:** Grok resolves the exact current Claude Code or Codex responsibility from canonical runtime/session evidence and foregrounds the matching cockpit without creating or selecting replacement execution.

### Claude law

- accepted current Attempt/Worker/provider realm/session first;
- provider-native discovery/resume/Wake outranks GUI;
- OpenClaw only foregrounds the already-selected cockpit;
- visible window/account labels cannot select realm/capacity;
- stale old Attempt/session refuses.

### Codex law

- App Server/OHF thread identity remains execution truth;
- native app is presentation only;
- no new thread/session because exact UI is absent;
- exact current binding/generation required.

### Live proofs

- one harmless Claude canary where exact native session identity is known;
- one harmless Codex canary where exact App Server/OHF thread is known;
- stale prior session, multiple windows, app closed, wrong account/realm visible, changed generation and native transport unavailable.

**Stop:** cockpit foreground/inspection only. No autonomous continuation unless current native Wake owner separately authorizes it.

---

## Task 9 — GSO-W1 separately released Wake/message/recovery tools

**Release gates:** all prior read/navigation proofs accepted; current provider-native Wake and target ACK/source-resolution law production-proven; company-dialogue binding accepted; exact effect reconciliation available; explicit new Chairman intent for each modifying release.

This is not one broad PR. Release independently useful tools one at a time.

### GSO-W1A `request_bound_wake`

Input is stable responsibility/surface reference plus operation key. Mastermind constructs one exact existing Wake obligation/route; Grok cannot select transport/session/host. Accepted proof distinguishes `DELIVERED`, target ACK and source resolution.

### GSO-W1B `send_bound_message`

Input is bounded text under a closed schema plus exact current target envelope. The message is entered/sent only through an accepted provider/dialogue operation with same-operation reconciliation. A timeout after possible send is `EFFECT_UNKNOWN`; zero retry/failover.

### GSO-W1C `perform_bound_recovery`

Only enumerated recovery kinds whose owner law already exists, for example foregrounding a known auth-required dialog or invoking an accepted same-session native resume. No arbitrary problem-solving/RPA.

Each tool requires:

- new static manifest version/digest;
- new capability grant and deployment receipt;
- RED-first identity/effect/adversarial tests;
- one harmless production canary;
- explicit stop before the next tool.

---

## Task 10 — GSO-A3 end-to-end Chairman acceptance and closeout

**Release gates:** accepted read-only Grok, ChatGPT foreground, Claude/Codex cockpit proofs, and any modifying tools explicitly included in the release.

**Observable mission:** Chris can use Grok to understand and manage the intended session surfaces without manual hunting, while canonical state remains truthful under every adverse condition.

### Positive journeys

1. Ask Grok what needs Chairman/Sol/COO attention.
2. Open the exact Sol ChatGPT responsibility.
3. Identify and foreground the exact returned Fable/Claude responsibility.
4. Open the exact Codex implementation thread/cockpit.
5. Where separately released, request one exact Wake or send one harmless bound message and prove correct reconciliation.

### Adverse journeys

- ambiguous responsibility;
- stale Session Truth/binding;
- cross-seat lookalike;
- changed RuntimeBinding generation;
- OpenClaw offline/unpaired;
- public MCP auth/digest mismatch;
- macOS permission absent;
- provider login/MFA;
- hostile on-screen text;
- native transport unavailable;
- effect-unknown modification;
- OpenClaw fully removed.

### Acceptance evidence

- exact source/branch/PR/merge SHAs;
- deployed Secretary server identity/version/tool digest;
- sanitized public endpoint/auth profile receipt;
- Grok connector installation receipt;
- OpenClaw Gateway/node/version/permission receipt without secrets;
- exact surface/binding revisions and operation IDs;
- screenshots/video only when privacy-reviewed and bounded;
- provider-native session/thread proof;
- no-rebuild/static-analysis/mutation tests;
- desktop/product proof at relevant states;
- unplug proof;
- Chairman acceptance statement.

### Durable closeout

- update Agent OS workstream decision/discovery/handoff records through existing owner;
- update Linear only as selected projection;
- retain GitHub as implementation/evidence truth;
- record exact deployed mode and kill-switch state;
- identify remaining unsupported surfaces/tools explicitly;
- leave one exact next action recoverable without this chat.

**Final stop:** only after real Chairman acceptance. No claim of full automation merely because OpenClaw can click or Grok can call MCP.

---

## Operator return packet for every child wave

Every return must include:

```text
operation key
owning workstream/plan task
protected Skillpack and pickup SHA
repository / branch / PR / exact head
changed-file census
capability before/after
RED-before evidence
focused/full/hosted CI and security results
live proof or explicit not-owed reason
current deployment/arming mode
credential/PII redaction receipt
no-rebuild receipt
known unknowns / discovered collisions
exact stop condition met
next dependency and whether it is authorized
```

A nonterminal return on a watcher-enabled dialogue must explicitly re-arm the accepted wait/watch path. A terminal return must explicitly stop/disarm the temporary watcher; silence is never terminal receipt.
