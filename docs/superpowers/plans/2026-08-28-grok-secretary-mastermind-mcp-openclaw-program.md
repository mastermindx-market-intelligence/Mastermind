# Grok Secretary → Mastermind MCP → OpenClaw — Implementation Program

> **For agentic workers:** use the current protected Skillpack and the appropriate planning/TDD/debugging/review skills. Every modifying wave follows RED → observed discriminating failure → minimal GREEN → exact-head hosted proof → independent review → Sol acceptance. Steps use checkbox syntax for tracking.

**Goal:** let Chairman Chris manage the correct ChatGPT Sol, Claude/Fable and Codex surfaces through Grok Secretary without hunting windows, choosing provider accounts, copying prompts or creating a second control plane.

**Architecture:** Grok calls one bounded Mastermind Secretary MCP. The MCP resolves responsibility/session/surface through accepted Mastermind owners, then either returns read-only state, enters the existing native Wake/session path, or invokes one immutable actuation envelope through the OCR-6 subordinate OpenClaw adapter. Executive OS, Agent OS, GitHub, Capacity Fabric, Wake, Agent Relay and RuntimeBinding ownership remain unchanged.

**Spec:** `docs/superpowers/specs/2026-08-28-grok-secretary-mastermind-mcp-openclaw-design.md`

**Planning basis:** `Mastermind@97f85ce5b84030faf4d291f988a1c642fb15e80a`, Skillpack `mastermind.sol_skillpack.v1` v1.0.0 / bootstrap major 1. This is a historical planning pin only. Re-pin current protected `master` and all required skills before every modifying step.

## Program-wide laws

- Existing workstream only: `WS:CHAIRMAN-CONTROL-ROOM`.
- This program is an implementation sequence over existing owners; it creates no new lifecycle/workstream/database/queue/router/memory plane.
- Existing carriers remain exclusive: #170, #174, #178, #184, Macro #6182, the unnamed active Codex/H0 canary and OCR-6.
- OCR-6 is the sole implementation owner for Executive Steward/Control Room continuity and the optional subordinate OpenClaw bridge. This program may coordinate/review OCR-6; it may not create a second bridge carrier.
- The existing Executive MCP v1 five-tool surface remains unchanged. New Secretary MCP code follows its pattern in a separate package/server identity.
- Grok/model input never contains canonical host/provider/account/thread/window/coordinate/argv authority.
- Public MCP ingress never exposes an Executive/Agent Relay/OpenClaw socket directly.
- Provider-native session/Wake mechanics outrank GUI/computer control.
- One modifying/actuation operation uses one stable key and one target/carrier until canonical reconciliation.
- `EFFECT_UNKNOWN` blocks retry/failover.
- Green CI, public reachability, connector discovery, OpenClaw pairing, foreground success, Wake delivery, target ACK and task completion are distinct states.
- All credentials, cookies, auth files, OAuth refresh material, proxy argv and private chat URLs remain outside Git/Slack/model output.
- No production arming from a records/contract/fixture PR.

## Program DAG

```text
GS-0 current-estate and provider-surface falsifier
      |
      +-------------------+-------------------+
      |                   |                   |
      v                   v                   v
existing WP/Wake       OCR-6 core         exact-foreground/P0B
prerequisites          Steward API        successor law/proof
      |                   |                   |
      +-------------------+-------------------+
                          |
                          v
                 GS-1 Secretary read MCP
                          |
                          v
                 GS-2 public authenticated edge
                          |
                          v
                 GS-3 Grok read-only canary
                          |
                  OCR-6 OpenClaw subwave
                          |
                          v
                 GS-4 actuation profile
                          |
                          v
                 GS-5 exact surface canaries
                          |
                          v
                 GS-6 bounded dialogue/recovery
                          |
                          v
                 GS-7 final Chairman acceptance
```

GS-6 is optional for the first useful release. A production-proven read + exact-foreground Secretary is already independently useful.

---

## GS-0 — Current estate, client and host falsifier

**Mode:** read-only evidence first. Any installation/admin ceremony is separately authorized after the evidence return.

**Observable mission:** produce one secret-free current receipt proving which Grok/Grok Bot MCP surface, Mastermind MCP deployment substrate, OpenClaw version/Gateway/node surface and Mac permissions actually exist—and name every unavailable dependency without guessing.

### Sources to inspect

- current protected Mastermind Skillpack and exact relevant owner law;
- current #170/#174/#178/#184/OCR-6 carriers and recent comments/checks;
- current Executive MCP files/config/deployment receipts;
- current Grok account/team/connector administration surface;
- current Grok Bot team MCP policy, if Grok Bot is the target client;
- installed OpenClaw binary/app/Gateway/node versions and current docs;
- paired-node list and secret-free capability descriptors;
- macOS Screen Recording/Accessibility status only through approved metadata/probe paths;
- current surface binding/Control Room/P0B state;
- current Codex/H0 canary identity/collision boundary.

### Required receipt

Produce `mastermind.grok_secretary_preflight.v1`, secret-free, containing conceptually:

```text
observed_at
protected_master_sha
skillpack identity
client_kind = grok_chat | grok_bot | both | unknown
custom_mcp_supported
mcp_transport_supported[]
auth_modes_supported[]
team_tool_allowlist_state
existing_mastermind_mcp_state
public_ingress_state
openclaw_version
openclaw_gateway_state
openclaw_node_state[]
computer_control_capability
browser_proxy_capability
mac_permission_state = ready | incomplete | unknown
surface_binding_state
p0b_exact_foreground_state
reserved_collisions[]
blocking_reasons[]
```

### Falsifiers

- `localhost`, private RFC1918 or Tailnet URL supplied to Grok as public endpoint must be classified unreachable, not “probably works.”
- Connector discovered tools ≠ authenticated call success.
- OpenClaw process present ≠ paired node/computer-control ready.
- Accessibility permission present ≠ exact target controllable.
- xAI/Grok model support ≠ Mastermind integration installed.
- a recent Slack message/commit ≠ active runtime.
- unidentified H0 canary keeps overlapping host modification held.

### Stop

Return one of:

```text
READY_FOR_GS1_CONTRACT
WAITING_STEWARD
WAITING_WP_WAKE
WAITING_P0B
WAITING_ADMIN
HELD_COLLISION
```

Do not install, authenticate, pair or expose anything in the read-only evidence wave.

---

## Existing-prerequisite lane — reconcile, never duplicate

Before GS-1/GS-4, Sol continues/reviews the existing carriers in place.

### Worker Presence / company dialogue

- accept/merge #178 WP-1 only after exact-head independent review;
- implement WP-2 on its own accepted separate carrier using the checked-in six-tool plan;
- do not add Secretary tools to WP-2 or company-dialogue tools to Executive MCP;
- Agent Relay production A2/A3 and exact live binding remain separate proof gates.

### Session Truth / Steward joins

- #170 remains its same carrier;
- missing owner digest/source stays blocking/unknown;
- Secretary cannot compensate by fuzzy matching titles or Slack prose.

### Wake

- #174 remains native provider delivery owner;
- `DELIVERED != ACK != SOURCE_RESOLVED`;
- Secretary `request_wake` delegates to accepted Wake and does not invent a second nudge path.

### Operator Continuity / OCR-6

- OCR-3 must supply accepted current RuntimeBinding/continuation source before continuity-dependent Steward output;
- OCR-5 and WP/Relay prerequisites gate rebound dialogue;
- OCR-6 core owns `ExecutiveStewardSnapshot` and the optional OpenClaw bridge;
- no GSM branch may edit the protected OCR-6 file surface.

### Codex/H0 collision

Until the active canary returns exact operation/thread/repository/branch/head/four paths/host boundary, do not install or mutate public ingress/OpenClaw/host/capacity/broker surfaces that could overlap it.

---

## GS-1 — Production-inert Secretary read MCP contract

**Dependency:** accepted Executive Steward read interface or an exact protected protocol frozen by OCR-6. If Steward implementation is not yet accepted, only interface-level tests/fixtures may proceed; no fake production composition.

**Observable mission:** expose one static, production-inert six-tool MCP facade that returns bounded source-attributed Steward facts and navigation references, with zero modification or direct owner access.

### Expected paths

```text
integrations/mastermind_secretary_mcp/__init__.py
integrations/mastermind_secretary_mcp/schemas.py
integrations/mastermind_secretary_mcp/adapter.py
integrations/mastermind_secretary_mcp/server.py
tests/test_mastermind_secretary_mcp.py
tests/test_mastermind_secretary_mcp_mutation.py
```

Modify an existing capability-policy test only if needed to prove compatibility in a temp/in-memory production-disarmed fixture. Do not check in a fake endpoint or token.

### Static tool census

Exactly:

```text
list_responsibilities
get_responsibility
get_attention
get_current_runtime
explain_blocker
resolve_surface
```

No aliases or dynamic tools.

### Closed inputs

Prefer empty or opaque-reference-only inputs:

```text
list_responsibilities: {optional bounded filters from closed vocabulary}
get_responsibility: {responsibility_ref}
get_attention: {optional target = chairman|ceo|coo}
get_current_runtime: {responsibility_ref}
explain_blocker: {responsibility_ref}
resolve_surface: {responsibility_ref, purpose = inspect|foreground|wake}
```

The schemas recursively forbid:

```text
host_ref provider account realm session_id thread_id channel thread_ts
browser_profile window_title coordinates url command argv shell ssh token secret
job_id attempt_id worker_id actor seat authority retry failover
```

Seat may be a closed read filter only where the server can prove it is not target authority; otherwise omit it.

### Adapter law

- consume one injected `SecretaryReadResolver`/Executive Steward protocol;
- no direct SQLite/raw SQL;
- no GitHub/Slack/network reads inside the pure adapter unless the accepted Steward interface itself owns them;
- no Executive/Agent OS/Wake/Agent Relay mutation import;
- one MCP call -> at most one resolver operation;
- returned adversarial text remains structured inert data;
- no server-side chaining from a read result into another tool.

### Response

One bounded versioned envelope, source revisions, freshness/degraded list, no hidden cache/persistence.

### RED tests

- exact six-tool census and schema digest;
- forbidden privileged field mutation kills;
- extra tool/dynamic registration mutation kills;
- untrusted result text cannot alter descriptions or call another tool;
- unknown responsibility/surface stays typed unknown;
- stale source cannot become modification-ready;
- SDK isolation;
- response size/redaction/secret-shape bounds;
- AST no-write/no-network/no-store/no-Wake/no-provider fences.

### Completion

Merge may establish `BUILT_NOT_PROVEN / PRODUCTION_INERT` Secretary read MCP only. It does not establish a running endpoint, connector, Steward implementation or production read.

---

## GS-2 — Public authenticated Streamable HTTP MCP edge

**Dependency:** accepted GS-1 static surface; exact current Grok client transport/auth preflight; no H0/host collision.

**Observable mission:** serve the exact Secretary read MCP surface over one stable authenticated HTTPS Streamable HTTP path while preserving the sealed existing Executive MCP/AF_UNIX/private-owner boundaries.

### Architecture

```text
public TLS/reverse proxy
    -> dedicated Secretary MCP HTTP process
        -> local narrow Steward read service/protocol
```

Do not convert existing `integrations/executive_mcp/server.py` into a public listener.

### Expected implementation surfaces

Determine exact current deployment ownership before coding. Likely:

```text
integrations/mastermind_secretary_mcp/http_server.py
ops/executive_os or existing reviewed service-install/config path
focused tests for auth, bind, proxy headers, transport and shutdown
```

Any installer/service path overlapping the unidentified H0 canary returns to Sol.

### Authentication

Action-time preflight selects exactly one supported mode:

- OAuth through current Grok custom connector/team surface; or
- dedicated scoped Authorization bearer/header through current supported team/connector policy.

Requirements:

- root/approved secret owner;
- token/secret never in Git/argv/log/output;
- exact audience/server identity;
- revocation/rotation test;
- no bearer grants organizational authority beyond the static MCP profile;
- unauthenticated/invalid/expired requests fail before tool execution;
- one request identity/trace receipt without sensitive headers.

### Network tests

- bind only reviewed interface/port behind proxy;
- direct private owner sockets remain unreachable externally;
- HTTPS required at external edge;
- host header/origin/forwarded-header policy pinned;
- request/response/time/concurrency ceilings;
- graceful shutdown; no durable request queue;
- no resources/prompts/sampling/roots endpoints;
- static tools identical to GS-1 digest;
- SSRF/path traversal/alternate MCP method attacks fail closed.

### Deployment state

Initial deployment is production-disarmed or read-canary-only. Public reachability alone is not acceptance.

---

## GS-3 — Grok read-only connector installation and canary

**Dependency:** GS-2 deployed, exact connector/admin surface available, accepted live Steward source.

**Observable mission:** from the actual Grok Secretary/Grok Bot conversation, invoke the six read tools against real safe Mastermind state and return truthful source-attributed answers.

### Admin ceremonies

Use the actual supported path discovered in GS-0:

- ordinary Grok custom connector at `grok.com/connectors`; and/or
- Grok Business/team connector console; and/or
- Grok Bot team MCP Configuration/allowlist/auth shared with the supported product.

The Chairman may need to complete login/MFA/admin consent or enter a secret in the native masked UI. Do not request credentials in chat.

### Tool profile

Allow only the six GS-1 tools. Capture the discovered tool names/schema digest. Refuse if the client exposes unexpected tools or strips required authentication.

### Real canaries

Use harmless responsibilities with no modification:

```text
"What currently needs Sol?"
"Where is responsibility X running?"
"Why is X blocked?"
"Resolve the navigation surface for X without opening it."
```

Prove:

- exact responsibility/source references;
- typed unknown/stale/degraded behavior;
- no Slack/Executive/GitHub/Linear/OpenClaw mutation;
- connector disconnect/revocation stops access;
- no private URL/provider account/host PII leakage;
- removing Grok changes zero canonical state.

### Completion

`PROVEN_LIVE` only for read-only Grok→Secretary MCP→Steward answers on the accepted environment. No foreground, Wake, message or recovery authority.

---

## GS-4 — OCR-6 subordinate OpenClaw/native actuator

**Carrier law:** implement only as the already-protected OCR-6 optional OpenClaw subwave/same carrier. GS program coordinates and reviews; it creates no second implementation carrier.

**Dependencies:** accepted OCR-6 core/Steward, exact P0B/successor foreground law, current installed OpenClaw falsifier, no H0 collision.

**Observable mission:** execute one immutable exact-target `INSPECT` or `FOREGROUND` envelope on the correct Mac and return a bounded verified receipt, with zero company lifecycle/routing/retry ownership.

### Expected interface

Conceptually:

```python
class SurfaceActuator(Protocol):
    async def inspect(envelope: SurfaceActuationEnvelope) -> SurfaceActuationReceipt: ...
    async def foreground(envelope: SurfaceActuationEnvelope) -> SurfaceActuationReceipt: ...
```

OpenClaw is one adapter implementation. Native URL/deep-link/session operations may be another adapter behind the same policy.

### Envelope tests

Reject:

- caller/model-authored host/session/profile/url/coordinate/argv;
- expired envelope;
- stale/changed binding digest;
- wrong provider/seat/purpose;
- non-navigation action in read/foreground release;
- ambiguous/multiple target;
- unsupported node command;
- OpenClaw own fallback/provider selection;
- hidden queue/persistence/retry.

### OpenClaw host proof

On the exact installed version prove secret-free:

- one Gateway identity;
- one paired macOS app node identity per Mac (no duplicate CLI node on same Mac);
- current approved command descriptors;
- screen/computer-control/browser-proxy capabilities;
- execution approval/allowlist;
- target Mac mapping from existing accepted host identity;
- Screen Recording + Accessibility readiness;
- disconnect/reconnect behavior;
- no public Gateway exposure;
- unplug test.

### Exact-target provider proof

#### ChatGPT

Use one disposable/non-sensitive test seat or approved production-safe surface. Prove exact managed environment + exact conversation foreground; no cross-seat fallback; no title-only identity; no proxy argv/cookie leakage.

#### Claude

Mastermind resolves exact current session. Prefer native resume/Wake; OpenClaw only foregrounds/verifies cockpit. No account selection.

#### Codex

Mastermind resolves exact current App Server/thread. OpenClaw only foregrounds/verifies cockpit. No substitute thread.

### Receipt states

At minimum:

```text
INSPECTED
FOREGROUNDED_VERIFIED
TARGET_NOT_FOUND
TARGET_CHANGED
AMBIGUOUS_TARGET
UNSUPPORTED_SURFACE
AUTH_REQUIRED
ACTUATOR_UNAVAILABLE
POSTCONDITION_UNPROVEN
EFFECT_UNKNOWN
```

### Completion

The OpenClaw adapter may become `PROVEN_LIVE` only for exact accepted inspect/foreground journeys. It still owns no lifecycle and does not make Grok actuation live until GS-5 integrates it.

---

## GS-5 — Secretary navigation/actuation MCP profile

**Dependency:** accepted GS-3 read connector + accepted GS-4 actuator + accepted native Wake path where exposed.

**Observable mission:** from Grok, inspect or foreground one exact resolved surface and optionally request one accepted Wake, with no arbitrary computer control.

### Static tool version

Create a new explicitly reviewed server version/tool digest that adds exactly:

```text
inspect_surface
foreground_surface
request_wake
```

Do not mutate the already-deployed read-only schema silently. Use explicit version/capability rollout and tool allowlist.

### Inputs

```text
inspect_surface: {surface_ref, operation_key}
foreground_surface: {surface_ref, operation_key}
request_wake: {responsibility_ref, operation_key, reason_code from closed vocabulary}
```

The server rereads current binding/source state immediately before action and authors the complete envelope/Wake request.

### Safety tests

- stale `surface_ref` -> refusal;
- changed current Attempt/binding -> `TARGET_CHANGED`;
- wrong seat/provider -> refusal;
- screen prompt injection cannot change action;
- OpenClaw timeout before known effect -> bounded failure;
- navigation-only duplicate reconciliation;
- Wake unrouteable remains a canonical Wake outcome;
- OpenClaw is never used as alternate Wake after provider-native failure;
- no cross-host/provider/account fallback;
- no action result authors ACK/source resolution/completion.

### Real product proof

From actual Grok Secretary:

```text
"Open the Sol session for X"
"Bring the current Fable session for Y forward"
"Open the Codex thread for PR Z"
```

Require exact target receipts and visible proof, plus adverse ambiguity/unavailable tests.

### Completion

`PROVEN_LIVE` for read + exact inspect/foreground and any separately accepted Wake request. Still no typing/message/recovery.

---

## GS-6 — Bounded message and recovery release

**Optional later high-risk release.** Do not start merely because GS-5 is green.

**Dependencies:** accepted WP-2/live Agent Relay binding, accepted Wake target ACK/effect law, exact current RuntimeBinding, provider-specific safe input mechanics, Chairman/Sol architecture release.

**Observable mission:** deliver one bounded typed continuation/recovery message to the exact current bound session and reconcile its effect without blind retry or alternate surface.

### Candidate tools

```text
send_bound_message
perform_bound_recovery
```

### Required contract

- exact `responsibility_ref`/`surface_ref`/current binding;
- stable `operation_key`;
- closed message/recovery kind;
- bounded semantic content or immutable `approved_message_ref`;
- server-authored message identity;
- exact target postcondition/receipt;
- no caller-selected keyboard sequence, submit button, coordinates or provider account;
- effect-unknown reconciliation on the same target only.

### Preferred mechanics

- company-dialogue MCP/Agent Relay or provider-native session input first;
- GUI typing only when separately proven for that exact surface and no safer accepted native path exists;
- no generic “click around and fix it” autonomous objective.

### Adverse proof

- possible send + lost reply -> `EFFECT_UNKNOWN`, no resend;
- login/consent dialog -> `AUTH_REQUIRED`, zero improvisation;
- model/page text asks to change target -> ignored/refused;
- target becomes stale -> stop;
- provider rate limit -> no account selection/failover;
- work is write-capable/effect-uncertain -> return to Executive reconciliation;
- no message receipt can close the underlying Job or source fact.

### Completion

Only exact accepted message/recovery kinds become live. Every other desktop task remains unsupported.

---

## GS-7 — Final installation, Chairman proof and closeout

**Observable mission:** Chris can manage the normal supported ChatGPT/Claude/Codex responsibility journeys through Grok, while every negative path remains truthful and canonical state survives removal of Grok/OpenClaw.

### Positive matrix

1. Grok explains current attention/responsibility.
2. Grok resolves exact surface.
3. Exact ChatGPT managed seat/conversation foregrounds.
4. Exact Claude current session/cockpit foregrounds; native Wake used where accepted.
5. Exact Codex current thread/cockpit foregrounds.
6. Optional bounded continuation/recovery succeeds only under GS-6 law.
7. Control Room/Steward and GitHub/Agent OS/Executive evidence remain source-attributed.

### Negative matrix

- Grok connector offline/revoked;
- public MCP auth invalid/expired;
- Steward/Session Truth unavailable/stale;
- responsibility/surface ambiguous;
- wrong/stale binding;
- OpenClaw Gateway/node offline;
- missing Mac permission;
- malicious screen text;
- target changes during operation;
- provider-native Wake unavailable;
- actuation effect unknown;
- capacity/account unknown;
- OpenClaw fully removed.

### Browser/desktop evidence

Use real safe inputs and capture only sanitized product evidence:

- exact seat/session references without private URLs/provider PII;
- before/after foreground identity proof;
- relevant desktop/display state;
- console/network only where a browser path exists and without credentials;
- no screenshots containing secrets unless retained privately under accepted evidence law.

### Security/adversarial review

Independent review must attack:

- tool widening/dynamic registration;
- auth bypass;
- SSRF/private socket exposure;
- target/reference forgery;
- screen/tool prompt injection;
- cross-seat/account fallback;
- hidden OpenClaw task state;
- blind retry/effect laundering;
- secret leakage;
- false completion claims;
- unplug/removal invariants.

### Durable closeout

Update:

- correct Agent OS workstream/handoff/discoveries;
- GitHub implementation/proof records;
- Linear selective projection only when useful;
- Control Room/Steward capability status;
- exact installed connector/server/version/tool digest/auth mode/node receipts;
- remaining unsupported surfaces and exact next action.

Do not create another program memory store.

## Program completion states

Use exact capability vocabulary per sub-capability. The whole program is complete only when:

- **Truth:** exact current responsibility/session/surface joins are fresh, failure-aware and correction-safe;
- **Intelligence/product:** Grok explains and navigates useful real states, not just exposes infrastructure;
- **Actuation:** exact foreground and any released message/recovery operations are policy-bound, idempotent/reconcilable and production-proven;
- **Learning:** failures/ambiguities/usage are instrumented through existing evidence/telemetry owners without another lifecycle store;
- **Removal:** Grok/OpenClaw unplug changes zero canonical company truth.

## Immediate next action after this records carrier

1. Merge this records-only amendment after exact-head review/CI.
2. Continue existing #178/#170/#174/#184/OCR dependencies on their same carriers.
3. Run GS-0 current-estate falsifier when the active H0 carrier identity is reconciled enough to inspect host surfaces safely.
4. Release GS-1 only when its exact Steward interface dependency is accepted or explicitly frozen for a production-inert contract wave.

This plan authorizes no implementation merely by existing.
