# H0 Large-Closure Transport v3 Implementation Plan

1. Re-pin protected Mastermind, the current Skillpack and the same
   `codex/cf2-h0-source-closure-20260827` carrier. Confirm no owned-path overlap.
2. Add RED tests for explicit v3 schema/CLI, canonical ZIP64 records, ZIP32-threshold crossing,
   cross-schema refusal, hostile end-record mutation, bounded streaming, Apple Python 3.9, repair
   consumption, manifest-bound executable modes, retained destination-parent verification across a
   mounted local volume, and runbook truth. Preserve the existing v2 rejection matrix unchanged.
3. Implement the smallest same-owner v3 writer, raw-layout validator, extractor, materializer and
   exact manifest dispatcher. Change only the repair's new-input materializer and finite copy cap.
4. Run focused RED/GREEN proof, the entire artifact/source-repair/source-contract matrix, Bash 3.2,
   Apple Python 3.9, compilation, diff, secret and hygiene gates.
5. Build a real v3 carrier from the accepted full Macro repository. Record exact bytes, manifest,
   payload SHA-256, enclosing SHA-256, object count and semantic inventory digest; materialize and
   verify it in an inert temporary checkout before privilege.
6. Obtain independent same-head spec/security and quality/lifecycle reviews, publish the same
   carrier, require hosted CI and CodeQL green, re-pin, then merge only under the loaded Skillpack.
7. Build the final carrier at the protected merge SHA and run the one native administrator ceremony.
   Require repair PASS, two verify-only PASS receipts, empty stderr and root-carrier absence.
8. Stop H0 at `H0_INSTALLED_HOST_PASS_NOT_P0_ACCEPTED` and hand exact identities to the separate
   CF2-P0 read-only carrier. Do not authenticate providers, start services, route or execute work.
