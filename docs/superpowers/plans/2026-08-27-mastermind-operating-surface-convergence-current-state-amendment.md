# Mastermind Operating-Surface Convergence — Current-State Amendment

**Date:** 2026-08-27  
**Operation key:** `mastermind-operating-surface-convergence-f0-20260827-sol-001`  
**Authority:** narrow correction to the OSC-F0 design, program plan and program index. For the exact topics below, this amendment wins over older generic wording in those three files. It creates no implementation/runtime authority.

## 1. OSC-F0 records carrier file census

Task 1's records-only carrier consists of exactly four files:

```text
docs/superpowers/specs/2026-08-27-mastermind-operating-surface-convergence-design.md
docs/superpowers/plans/2026-08-27-mastermind-operating-surface-convergence-program.md
docs/superpowers/plans/2026-08-27-mastermind-operating-surface-convergence-program-index.md
docs/superpowers/plans/2026-08-27-mastermind-operating-surface-convergence-current-state-amendment.md
```

Any older Task 1 wording saying to reread "both documents" means all four records files above. This does not widen OSC-F0 beyond records/source law.

## 2. Closed OSC-R0 owner-state vocabulary

The closed census vocabulary is exactly:

```text
ACTIVE_ACKNOWLEDGED
RETURNED_FOR_SOL
HELD_FOR_SOL
DELIVERY_ONLY
AUTHOR_SESSION_UNKNOWN
IDLE_RECONCILABLE
EFFECT_UNKNOWN
TERMINAL_ACCEPTED
ACTIVE_LOCAL_CARRIER_IDENTITY_UNKNOWN
```

The active Codex/H0 canary is classified `ACTIVE_LOCAL_CARRIER_IDENTITY_UNKNOWN` until an exact GitHub/operation/thread identity is returned and reconciled. Supersede the older prose form `ACTIVE_LOCAL_CARRIER / IDENTITY_PENDING_RETURN`; it must not appear as a second state.

These values are read-only census descriptions, not a lifecycle, queue or persisted state machine.

## 3. Organizational identity and company-priority ownership

Supersede any owner-matrix wording that assigns the company's complete "what is next" priority to Agent OS.

The exact split is:

| Question / fact | Canonical owner |
|---|---|
| What durable workstream/wave/decision/discovery/handoff identity exists, and what next action did that record author? | Agent OS direct records |
| What company phase and P0 objectives govern? | current strategic-state owner |
| What is the ranked company priority / next executable objective? | canonical Improvement Agenda after its accepted strategy/readiness joins |
| Is an executable Job admitted, queued, claimed, running or terminal? | Executive OS |

Agent OS readiness and per-record `next_action` are inputs/observations. They do not outrank or replace the Improvement Agenda. Executive admission remains a separate runtime act.

## 4. One OCR-6 implementation node; OSC-P1 is a coordination alias only

There is exactly one protected implementation owner for:

```text
control_plane/operator_continuity_projection.py
control_plane/executive_steward.py
control_plane/chairman_control_room.py
same-thread runtime-rebound projection
optional subordinate OpenClaw bridge
```

That owner is the accepted Operator Continuity **OCR-6** carrier and plan.

`OSC-P1` is not a second wave, branch, PR, operation key, builder commission or file owner. It is only the convergence-program milestone/acceptance alias meaning:

```text
observe the one OCR-6 carrier
→ verify its prerequisites across the other lanes
→ review its returned implementation against the OSC Chairman outcome
→ run the shared Responsibility Matrix / Steward acceptance ruler
```

Task 9 in the generic program plan must therefore be read as an OCR-6 continuation/review checklist, not authorization to create another implementation carrier. Any later Control Room UI-only follow-on requires an explicit disjoint path/mission ruling after OCR-6 acceptance.

Dependency normalization:

```text
OCR-3 before continuity-projection core
OCR-5 + accepted WP-2 + production Agent Relay before rebound Slack emission
accepted current Control Room source path before UI integration
```

## 5. Fail-closed reservation while the Codex/H0 carrier identity is unknown

While the active Codex/H0 canary remains `ACTIVE_LOCAL_CARRIER_IDENTITY_UNKNOWN`, **all modifying work that could overlap it is held**, including:

```text
H0 / privileged bootstrap
production-host preparation or ceremony
capacity acquisition / broker-host setup
CF2-H0 / CF2-P0 modifications
OCR modifying slices that touch host identity, broker execution context, capacity acquisition, realm installation or the unnamed four paths
```

Read-only review, records work and unquestionably disjoint non-host/non-capacity lanes may continue. Mere planned path names or a belief that another PR is only two files is insufficient to prove disjointness against four unnamed files.

Parallel modification is released only after the existing canary returns an exact carrier/operation/thread identity, repository/branch/head, the four changed paths and its host/privilege boundary, and Sol reconciles that receipt. No replacement worker may recreate or take over the unnamed local operation in the interim.

## 6. One logical Fable responsibility; multiple disjoint runtime executions only through existing law

Supersede the phrase "separate Fable COOs."

There is one logical **Fable COO responsibility/seat** under the accepted root/seat identity model. It is not multiplied by assigning Claude accounts or Slack users.

Parallel work may use:

- already-authorized distinct logical roles; or
- separate disjoint Executive child Jobs/Attempts/workers under the single Fable COO orchestration responsibility, once Executive routing is production-proven; or
- temporary manual worker commissions that are explicitly transport-only and never presented as multiple durable Fable seats.

Every modifying lane still has one current carrier and exact accountability. Provider realms/accounts remain runtime placement, not organizational identity.

## 7. FULL FABRIC requires exact intended-seat foreground proof

The final Chairman acceptance ruler must include the accepted Chairman Control Room P0B exact-foreground capability, or an explicitly accepted successor with the same user outcome.

FULL FABRIC cannot pass on links, background URL navigation, exact browser destination alone or optional OpenClaw availability. It requires a real proof that Chris can select a Control Room responsibility and foreground the **exact intended managed-browser seat and exact bound conversation** with:

```text
no cross-seat fallback
no guessed tab/window/title authority
no hidden credential exposure
no Chairman hunting among windows
truthful degraded/refusal behavior when exact foreground is unavailable
```

OpenClaw remains optional/noncanonical. It may later implement a bounded actuator only if separately accepted; it is not the acceptance owner and cannot substitute a generic shell or guessed UI automation for P0B/successor proof.

Normalized final dependency:

```text
accepted P0B exact-intended-seat foreground proof (or explicit accepted successor)
+ accepted Steward/Responsibility Matrix
+ accepted Executive/Capacity/OHF runtime path
+ accepted company-dialogue/attention path
→ FULL FABRIC Chairman acceptance may be attempted
```

## 8. No other change

All remaining canonical owners, one-carrier/effect-unknown laws, cutover modes, positive/adverse journeys and no-rebuild boundaries remain unchanged. This amendment does not make #170, #174, #178, #184, Macro #6182, the current Codex/H0 canary, OCR-6, Grok, OpenClaw or any production capability accepted/live.