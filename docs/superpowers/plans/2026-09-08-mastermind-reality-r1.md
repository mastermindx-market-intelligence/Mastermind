# Mastermind Reality R1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deliver a production-inert, skills-only `mastermind-reality` package that teaches an authorized web specialist to inspect one approved Mastermind product journey, consume actual image bytes, correlate semantic and runtime evidence, exercise negative controls, and return a closed evidence record without creating another browser, evidence, lifecycle, identity, authorization, or control plane.

**Architecture:** Reality is an orchestration skill over existing browser, trace, observability, artifact-retention, Executive OS, Agent OS, GitHub, Linear, and Slack owners. A closed JSON Schema carries target, source relationship, capture window, screenshot-byte consumption, semantic/runtime evidence, inference, unknowns, controls, limitations, and the exact next owner. A package-local catalog fragment is input to the single shared package integration owner; this source child does not edit the global marketplace or the closed BSC-P1 validator.

**Tech Stack:** Codex skills-only plugin manifest JSON, Markdown skill/reference documents, JSON Schema Draft 2020-12, Python 3.12+ contract tests with `pytest` and `jsonschema`, and existing browser/trace/observability providers for installed real-path proof.

**Spec:** Chairman-delivered `HANDOFF_C_REALITY.md`, September 7, 2026. The implementation is carried by Mastermind PR #535.

## Global Constraints

- Protected Mastermind source at pickup: `03eaa8e9aa32fdc949551ad0f88a890d269daea4`, tree `98adcc5f6ae4fd27d40753ec1819a1e22e510d8a`.
- Skillpack at the same pickup commit: `mastermind.sol_skillpack.v1`, version `1.0.1`, bootstrap major `1`.
- Re-read protected `master` and all required Skillpack files from one exact commit before every release-sensitive modification.
- Parent responsibility is `WS:CHAIRMAN-CONTROL-ROOM`. The handoff named `MAS-198`, but current Linear owns that issue for the Web-Sol extension/native-bridge program; no Reality-specific Linear child is inferred or created by this plan.
- Source carrier is branch `sol/mastermind-reality-r1-20260908`, Draft PR #535, operation `mastermind-reality-r1-source-20260908-sol-001`.
- Do not edit `.agents/plugins/marketplace.json`, `scripts/validate_mastermind_plugins.py`, or `tests/test_mastermind_plugin_packages.py`; they are shared BSC-P1 integration-owner paths.
- Do not create an MCP server, app binding, browser registry, trace store, artifact store, lifecycle, queue, scheduler, retry service, identity system, authorization system, transcript store, memory plane, or control plane.
- Do not inspect or actuate managed Chairman account surfaces, Web-Sol ChatGPT provider-control surfaces, browser settings, password managers, cookies, tokens, or unrelated profiles.
- A path, digest, HTTP 200, DOM node, or worker statement is not visual proof. Actual PNG bytes must be delivered to and inspected by the intended model session.
- Unknown is not zero. Missing source, deployment, runtime, or coverage facts remain `UNKNOWN` or unavailable; never fill them with aliases.
- `NOT_APPLIED`, `APPLIED`, and `EFFECT_UNKNOWN` are effect states. Refusal, timeout, cancellation, and acceptance are separate.
- Source green, source protection, package installation, authenticated invocation, successful journey, and final acceptance remain distinct.
- Private screenshots, traces, state payloads, and semantic snapshots remain under their approved private owner; public source carries sanitized findings and cryptographic identities only.

## File Structure

- Create `plugins/mastermind-reality/.codex-plugin/plugin.json` — closed skills-only, read-capability package manifest.
- Create `plugins/mastermind-reality/skills/inspect-product-journey/SKILL.md` — complete governed journey-inspection procedure.
- Create `plugins/mastermind-reality/references/reality-boundary.md` — authority, evidence, time, correction, effect, privacy, completion, and rollback rules.
- Create `plugins/mastermind-reality/references/reality-observation.schema.json` — closed transfer/evidence schema; never a mutable registry.
- Create `plugins/mastermind-reality/references/catalog.fragment.json` — package-local inert integration input.
- Create `tests/test_mastermind_reality_contract.py` — package, schema, hostile-mutation, negative-control, and no-duplicate-plane tests.
- Create `research/MASTERMIND_REALITY_R1_CONTROL_ROOM_OBSERVATION_2026-09-07.json` — sanitized schema-valid record of the already-inspected real Control Room journey; private bytes remain outside GitHub.
- Create `docs/superpowers/plans/2026-09-08-mastermind-reality-r1.md` — this implementation and release plan.

---

### Task 1: Recover Current Owners and Establish One Source Carrier

**Files:**
- Read: `docs/sol_skills/INDEX.md`, `COLD_START.md`, `RECONCILE_STATE.md`, `COMMISSION_WAVE.md`, `WORKER_AVENUE_ROUTING.md`, `REVIEW_RETURN.md`, and `CLOSEOUT.md` from one protected commit.
- Read: Macro `agentos/workstreams/WS-CHAIRMAN-CONTROL-ROOM.md`.
- Create: branch `sol/mastermind-reality-r1-20260908` and Draft PR #535.

**Interfaces:**
- Consumes: current Chairman continuation, protected source, Agent OS parent, GitHub source authority.
- Produces: one branch/PR carrier, exact pickup SHA/tree, one source-writer identity, and a collision receipt.

- [x] **Step 1: Pin protected source and same-commit Skillpack**

Read protected `master`, record `03eaa8e9aa32fdc949551ad0f88a890d269daea4`, and verify every required procedure reports schema `mastermind.sol_skillpack.v1`, version `1.0.1`, minimum bootstrap major `1`.

- [x] **Step 2: Reconcile parent and projections**

Read current Macro and Linear. Preserve `WS:CHAIRMAN-CONTROL-ROOM` as the parent. Record the Linear ownership disagreement instead of creating a false Reality issue or changing `MAS-198`.

- [x] **Step 3: Run collision checks**

Confirm no `plugins/mastermind-reality` path, Reality branch, Reality implementation PR, or reciprocal Slack dialogue already owns the source. Treat PR #528 as research only.

- [x] **Step 4: Create one source carrier**

Create the branch at the protected pickup and Draft PR #535. No alternate carrier, local source branch, or second PR is permitted.

### Task 2: Establish RED-First Behavioral Contracts

**Files:**
- Create: `tests/test_mastermind_reality_contract.py`.

**Interfaces:**
- Consumes: handoff acceptance rules and existing BSC-P1 package conventions.
- Produces: exact package API and schema behavior required by Tasks 3–6.

- [x] **Step 1: Write missing-package tests before source**

The test module requires the exact manifest, exact five-file package inventory, closed observation schema, actual screenshot-byte consumption, UTC capture timestamps, target-class refusal, source-relation integrity, required skill/boundary clauses, inert catalog fragment, and absence of live binding or secret shapes.

- [x] **Step 2: Publish the RED-only commit**

Commit `72aa3e3be5a06f0dc8fc984632fdc0996829880d` contains only the contract test. Its natural hosted run `34190955895` must be consumed before the implementation commit. Do not manually rerun it for a fresher timestamp.

- [x] **Step 3: Prove additional hostile REDs locally**

Observe separate failures for a non-UTC capture timestamp and for the old one-object semantic-evidence shape plus an absent real observation record. Implement only after each intended failure is observed.

### Task 3: Add the Closed Package Manifest and Integration Fragment

**Files:**
- Create: `plugins/mastermind-reality/.codex-plugin/plugin.json`.
- Create: `plugins/mastermind-reality/references/catalog.fragment.json`.

**Interfaces:**
- Produces: package `mastermind-reality` version `0.1.0`, one skill root, read-only declared capability, and a package-local fragment with schema `mastermind.reality_catalog_fragment.v1`.

- [x] **Step 1: Add the exact manifest**

Use no `apps`, `mcpServers`, `.app.json`, MCP declaration, OAuth material, or installed app identifier. The manifest declares only `skills: ./skills/` and `capabilities: [Read]`.

- [x] **Step 2: Add the inert fragment**

The fragment identifies the package and skill, reports `SOURCE_CANDIDATE` and `NOT_INSTALLED`, and deliberately does not use the current validator's installed-app-shaped `plugin_...` schema token.

- [x] **Step 3: Keep shared integration paths untouched**

Do not modify the marketplace, validator, or existing BSC-P1 package tests. Their expected failure after adding the third package is a cross-owner gate, not permission for this child to expand them.

### Task 4: Add the Authority and Evidence Boundary

**Files:**
- Create: `plugins/mastermind-reality/references/reality-boundary.md`.

**Interfaces:**
- Consumes: canonical owner law and the handoff's no-rebuild/actual-pixels requirements.
- Produces: one package-local procedure reference loaded by the Reality skill.

- [x] **Step 1: Freeze canonical ownership**

Name Executive OS, Agent OS, GitHub, Linear, Slack, browser, trace, observability, and evidence owners. State that Reality cites their facts and creates no replacement authority.

- [x] **Step 2: Freeze target and credential exclusions**

Allow only `APPROVED_PRODUCT` and `TEST_FIXTURE`. Exclude managed Chairman accounts, Web-Sol provider control, credentials, profiles, cookies, storage, raw process arguments, and secret-bearing settings.

- [x] **Step 3: Freeze evidence and inference law**

Require actual PNG bytes, digest, viewport/data state, model consumption, semantic evidence, runtime/trace coverage, and capture window. Separate deterministic facts from model inference and preserve unknowns.

- [x] **Step 4: Freeze effects, corrections, completion, and rollback**

Preserve same-carrier `EFFECT_UNKNOWN`, immutable historical evidence, capability-state honesty, and package-only rollback.

### Task 5: Add the Closed Observation Schema

**Files:**
- Create: `plugins/mastermind-reality/references/reality-observation.schema.json`.

**Interfaces:**
- Produces: `mastermind.reality_observation.v1` validated by Draft 2020-12.

- [x] **Step 1: Close the envelope**

Require exactly: schema, observation identity, capability state, `recorded_at`, persona, target, source relationship, journey, capture, findings, negative controls, effect, cleanup, limitations, and next action. Set `additionalProperties: false` at every object boundary.

- [x] **Step 2: Enforce target and source identity**

Restrict surface classes to `APPROVED_PRODUCT | TEST_FIXTURE`; require forty-character lowercase hexadecimal source identities; record deployed-observation time and a closed `EQUAL | DISTINCT | UNOBSERVABLE` comparison; require a deployed SHA for `MATCH` or `DIFFERENT`; preserve `UNKNOWN` when deployment is unobserved. The skill mechanically verifies the two SHA strings because Draft 2020-12 cannot compare sibling string values by itself.

- [x] **Step 3: Enforce visual consumption and UTC time**

Require at least one PNG with positive bytes, `bytes_present: true`, exact viewport, and data state. Replace the bare consumption boolean with a closed receipt binding consumer, delivery method, proof reference and digest, optional native consumption time, and an explicit limitation when that time is unavailable. Require UTC timestamps ending in `Z` using an explicit regex rather than relying on an optional format checker.

- [x] **Step 4: Enforce semantic/runtime coverage and findings**

Use closed `AVAILABLE | PARTIAL | UNAVAILABLE` evidence sets so absence cannot masquerade as an empty successful array. Desktop and mobile artifacts retain distinct identities and coverage; every retained semantic/runtime artifact is consumed. Every finding states its basis, safe owner-relative evidence references, and explicit unknowns.

- [x] **Step 5: Enforce all seven negative controls**

Require exact keys for wrong target, stale capture, different build, different viewport/data state, missing screenshot bytes, broken browser connection, and excluded account surface. Each is `DETECTED | REFUSED | NOT_EXERCISED` with a non-empty detail.

### Task 6: Add the Journey-Inspection Skill

**Files:**
- Create: `plugins/mastermind-reality/skills/inspect-product-journey/SKILL.md`.

**Interfaces:**
- Consumes: `reality-boundary.md`, `reality-observation.schema.json`, current protected Skillpack, and existing owner-native tools.
- Produces: one bounded inspection procedure and one schema-valid return.

- [x] **Step 1: Require current-source recovery**

The skill rereads protected Mastermind and same-commit procedure before modifying work; inability to establish compatibility makes modifying workflow unavailable.

- [x] **Step 2: Freeze one meaningful persona journey**

Require one exact product, environment, start/end, success/partial/failure/unknown definitions, explicit non-goals, and inspectable evidence target.

- [x] **Step 3: Qualify existing owners and actual pixels**

Use the existing browser, trace, observability, and artifact owners. Refuse visual claims when actual PNG bytes cannot be delivered to and inspected by the model.

- [x] **Step 4: Execute bounded read-only observation**

Capture one safe degraded/partial/stale/error state, preserve clocks and viewports, minimize private evidence, and avoid public upload of sensitive artifacts.

- [x] **Step 5: Exercise controls and emit the closed return**

Address all seven controls and validate the final record. Keep source, installation, authenticated use, journey success, and acceptance separate.

### Task 7: Publish the Sanitized Real-Path Observation

**Files:**
- Create: `research/MASTERMIND_REALITY_R1_CONTROL_ROOM_OBSERVATION_2026-09-07.json`.

**Interfaces:**
- Consumes: the private one-shot capture set already inspected by Web Sol.
- Produces: a public, schema-valid, sanitized evidence index that does not publish private artifact bytes.

- [x] **Step 1: Record exact historical identities**

Record the capture window, desktop/mobile viewports, screenshot byte counts and SHA-256 digests, semantic artifact identities, rendered-state identity, trace identity, and the historical protected/deployed source mismatch.

- [x] **Step 2: Separate deterministic findings from model inference**

Deterministic facts include HTTP/network/console state and rendered counts. Model inferences include the mobile diagnostic-first hierarchy and desktop competition between status/navigation and the decision area. Retain the absence of user-impact instrumentation.

- [x] **Step 3: Record all controls and privacy limits**

Preserve the wrong-target 404, stale source clocks, build mismatch, separate viewport identities, missing-byte refusal, Opera-specific disconnection, and excluded account refusal. State that private bytes remain on the authorized Mac.

- [x] **Step 4: Validate against the packaged schema**

Run:

```bash
python -m pytest -q tests/test_mastermind_reality_contract.py
```

Expected focused result after the pre-review hardening amendment: 22 passed, zero failed, zero skipped.

### Task 8: Publish One Immutable Source Candidate

**Files:**
- Create/update exactly the eight paths listed under File Structure.

**Interfaces:**
- Consumes: terminal natural RED from commit `72aa3e...` and final local GREEN.
- Produces: one non-force fast-forward commit on PR #535.

- [x] **Step 1: Re-read protected source and collision state**

If protected master moved, compare changed paths and governing Skillpack/material sources. Do not make an ancestry-only commit merely to change ahead/behind counters. Any overlapping Reality writer blocks publication.

- [x] **Step 2: Create one ordinary implementation commit**

Create all package, plan, observation, and corrected test blobs in one Git tree based on the RED head. Update the existing branch by expected old SHA, without force or a replacement PR.

- [x] **Step 3: Read back exact head, tree, and path census**

Require exactly eight changed paths and verify each blob against the prepared bytes.

- [x] **Step 4: Update PR truthfully**

Set capability to `BUILT_NOT_PROVEN / NOT_INSTALLED`. Preserve the shared BSC-P1 integration blocker, private evidence boundary, and no-runtime/no-account effect.

### Task 8A: Harden the Transfer Contract Before Independent Review

**Files:**
- Modify: `tests/test_mastermind_reality_contract.py`.
- Modify: `plugins/mastermind-reality/references/reality-observation.schema.json`.
- Modify: `plugins/mastermind-reality/references/reality-boundary.md`.
- Modify: `plugins/mastermind-reality/skills/inspect-product-journey/SKILL.md`.
- Modify: `research/MASTERMIND_REALITY_R1_CONTROL_ROOM_OBSERVATION_2026-09-07.json`.

**Interfaces:**
- Consumes: the immutable initial semantic head and retained private capture evidence.
- Produces: a stronger same-carrier candidate with explicit evidence absence, structured visual-consumption proof, effect/cleanup receipts, safe references, and source-comparison semantics.

- [x] **Step 1: Establish the hardening RED**

Add focused tests for structured visual-consumption receipts, explicit unavailable runtime evidence, available-without-artifact refusal, unsafe artifact-reference refusal, source-relation comparison consistency, and mandatory skill/boundary checks. Observe five failures and seventeen passes before implementation.

- [x] **Step 2: Harden the closed schema and procedures**

Add `recorded_at`, deployed-observation time, closed revision comparison, structured consumption proof, `AVAILABLE | PARTIAL | UNAVAILABLE` evidence sets, safe owner-relative references, and separate effect/cleanup receipts. Keep the package skills-only and production-inert.

- [x] **Step 3: Correct the sanitized historical observation**

Migrate the public observation to the hardened schema. Bind the two exact PNG identities to a retained private session-local consumption attestation, disclose the absence of a provider-native consumption timestamp, and preserve the read-only `NOT_APPLIED` effect plus clean child-resource shutdown without publishing private bytes.

- [x] **Step 4: Prove GREEN and discriminating mutations**

Require 22 focused passes with zero skips. Mutate each new safety boundary independently and require the targeted test to fail, then restore byte-identical source before publication.

- [x] **Step 5: Publish one same-carrier hardening commit**

Re-read protected source, confirm no overlapping Reality writer, commit only the existing eight-path carrier subset, push by normal fast-forward, and update PR #535 without Ready, merge, installation, or production claims.

### Task 9: Verify, Review, and Return

**Files:**
- Read: exact PR #535 head, current merge ref, current protected source, workflow results, reviews, and review threads.

**Interfaces:**
- Produces: exact release or hold decision and the required parent return.

- [ ] **Step 1: Consume natural final-head CI**

Run no duplicate workflow. Read the full final job log. Record Reality-focused pass/fail counts and the complete repository result. Historical natural run `34192680952` on semantic head `2de2d4e...` completed with exactly two failures in the shared two-package BSC-P1 inventory assertions and no Reality-contract failure; it becomes historical after the hardening commit. The new immutable hardening head requires its own natural current-head result.

- [ ] **Step 2: Obtain independent exact-head review**

Route to a non-author technical reviewer after the head is immutable.

```text
PREFERRED_AVENUE: Terra
WHY: the candidate is a bounded schema/procedure/package review with exact paths and tests
WHY NOT FABLE: product and authority boundaries are frozen; principal-level ambiguity is not required
RECEIVER_BINDING_MODE: CAPACITY_SELECTABLE
```

Request review on the PR. A requested reviewer is not approval. No receiver-specific watcher is claimed unless a real continuation path is armed.

- [ ] **Step 3: Classify current integration**

Until the shared owner accepts the package into the global marketplace/validator contract and the full required gate is green, remain `BUILT_NOT_PROVEN / INTEGRATION_BLOCKED`. Do not mark Ready or merge.

- [ ] **Step 4: Return the parent packet**

State current Skillpack SHA, branch/head/tree/PR, exact paths, RED and GREEN evidence, independent review state, real-path observation, all controls, private artifact location/limits, unresolved shared integration owner, rollback, and exact next action.

- [ ] **Step 5: Explicit terminal or continuation edge**

This source operation has no worker watcher. Use `NOT_WATCHER_ENABLED`. If the shared integration owner or reviewer has not returned, leave the PR Draft/Hold and state the exact next owner rather than claiming completion.

## Pre-review Hardening Evidence

- RED after the hardening tests and before implementation: five failed, seventeen passed.
- GREEN on the restored current candidate: twenty-two passed, zero failed, zero skipped.
- Six hostile mutations were killed independently: empty `AVAILABLE` evidence, unsafe artifact references, `MATCH` with a `DISTINCT` comparison, optional consumption proof reference, missing source-equality procedure, and acceptance of a bare consumption boolean. All changed source files were restored byte-identically after mutation.
- The sanitized historical observation validates under the hardened schema and retains private exact-image, visual-consumption, cleanup, state, semantic, and trace evidence outside the public repository.
- Shared validator status remains an explicit owner gate: five `UNEXPECTED_PACKAGE_FILE` findings, one per new package file, and no secret/live-state/app-id/encoding finding. The existing shared package suite remains fifty-three passed and two expected owner-gate failures.

## Acceptance

Reality R1 source is acceptable only when:

1. the package source remains exactly bounded and production-inert;
2. the focused tests pass with zero skips;
3. the real historical observation validates and actual screenshot consumption is supported by retained private evidence;
4. all seven negative controls are recorded;
5. independent exact-head review has no blocker;
6. current-base integration is known;
7. the shared package owner has admitted the third package without weakening existing safety tests;
8. required repository/security checks are green on the actual integration candidate;
9. installation and account qualification are either separately proven or explicitly unproven;
10. no merge, install, provider, account, runtime, or production claim exceeds its evidence.

## Rollback

Before installation, rollback closes or reverts PR #535 while retaining the historical observation and private evidence. After a later integration owner adds the global catalog mapping, rollback removes only that mapping or Reality package generation. It never deletes historical evidence, disables a shared browser/trace/observability service, touches a managed account, or closes unrelated sessions.
