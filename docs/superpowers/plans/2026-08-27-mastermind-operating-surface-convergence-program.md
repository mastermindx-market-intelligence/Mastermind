# Mastermind Operating-Surface Convergence Program Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reconcile and complete the existing Mastermind execution, dialogue, attention, capacity, Linear and Chairman operating-surface lanes so Chris can run the company from Control Room + Linear + Grok while Slack becomes transport plumbing.

**Architecture:** This is a convergence and release plan over existing canonical owners and existing implementation carriers. It creates no new lifecycle, queue, workstream, provider database or generic `Mastermind-OS` service. Each lane remains on its current operation key/branch/PR until reconciled; the program converges at the read-only Executive Steward and Chairman Control Room Responsibility Matrix.

**Tech Stack:** Existing Mastermind Executive OS/SQLite, Macro Agent OS, GitHub, Linear, Slack/Agent Relay, Worker Presence, Wake, Capacity Fabric, OHF/provider adapters, Chairman Control Room, Grok Secretary, optional OpenClaw.

**Spec:** `docs/superpowers/specs/2026-08-27-mastermind-operating-surface-convergence-design.md`

## Global Constraints

- Protected procedure is loaded from `mastermindx-market-intelligence/Mastermind@b901dee0272a99b8a1d60385848b99b7273e8261`, Skillpack `mastermind.sol_skillpack.v1` v1.0.0 / bootstrap major 1. Every modifying continuation re-pins current protected master and the required skills from one exact SHA.
- Executive OS remains the only Job / Attempt / Worker / Event lifecycle and CEO-intent admission owner.
- Agent OS remains the only durable organizational workstream / decision / discovery / handoff owner.
- GitHub remains implementation/evidence truth. Linear is selective projection. Slack is transport/hot-state.
- One logical modifying operation binds to one carrier until canonical reconciliation. Never create replacement carriers for #170, #174, #178, #184, Macro #6182 or the current Codex/H0 canary.
- The current Codex/CEO canary, its four changed files, privileged bootstrap, production host and eventual native H0 ceremony are reserved. No other lane may touch or rerun that operation until its existing carrier returns and Sol reviews it.
- No active operation is migrated from Slack to Linear or another worker simply to make the UI cleaner.
- Exact-thread ACK / BLOCKED / DECISION_REQUEST / RESULT / RULING / CONTINUE discipline is mandatory for any Slack/session continuation. Until canonical WP-TW/Wake continuation is live, use at most one temporary non-authoritative convergence watch.
- A Slack ACK is not Executive execution. A Linear status is not runtime truth. Green CI/merge is not production acceptance.
- Every operator return is reviewed by Sol against the Chairman outcome and exact lane stop condition before continuation or merge.

---

### Task 1: Publish the records-only OSC-F0 architecture carrier

**Files:**
- Existing: `docs/superpowers/specs/2026-08-27-mastermind-operating-surface-convergence-design.md`
- Existing: `docs/superpowers/plans/2026-08-27-mastermind-operating-surface-convergence-program.md`

**Interfaces:**
- Produces: one protected, records-only authority/precedence and dependency carrier.
- Does not produce: implementation, runtime arming, Slack installation, Linear mutation, Wake delivery, provider selection or OpenClaw access.

- [ ] **Step 1: Re-read both documents from the branch**

Confirm no `TBD`, no ambiguous owner, no claim that an open PR is live, and no duplicate system.

- [ ] **Step 2: Compare branch base with current protected master**

Use exact branch and master SHAs. If protected master advanced, inspect material changes to Skillpack, CCR, Agent Relay, Wake, Capacity/OCR and Linear source law before reconciling the records carrier.

- [ ] **Step 3: Open one draft PR**

Required metadata:

```text
Workstream: WS:CHAIRMAN-CONTROL-ROOM
Linear: NONE
Portfolio-Mode: records
Wave: OSC-F0
Authority: architecture/source-law
Completion: records-only
Operation-Key: mastermind-operating-surface-convergence-f0-20260827-sol-001
```

The body must name #170/#174/#178/#184/#6182 and the current Codex/H0 canary as reserved existing carriers.

- [ ] **Step 4: Run hosted proof**

Require the repository's current protected test/fence path. Records-only green does not authorize any implementation lane.

- [ ] **Step 5: Sol adversarial review**

Reject if the PR creates a new lifecycle/store, makes Linear or Slack canonical, assigns provider accounts as durable people, or silently absorbs an existing carrier.

- [ ] **Step 6: Merge only after exact-head acceptance**

Record merge SHA. If merge effect is unknown, reconcile the same PR; do not create another records PR.

---

### Task 2: Run OSC-R0 exact carrier and operation census

**Files / sources (read-only):**
- Mastermind #170, #174, #178, #184 and their comments/checks
- Macro #6182 and MAS-27/MAS-64/MAS-65/MAS-66
- current protected Mastermind and Macro heads
- exact Slack threads for known operation keys
- the current Codex/H0 canary's visible carrier/plan receipt when it returns
- Agent OS records for `WS:CHAIRMAN-CONTROL-ROOM`, `WS:AGENT-OS`, `WS:EXECUTIVE-CAPACITY-FABRIC`

**Interfaces:**
- Produces: a source-attributed census with `carrier`, `operation_key`, `head`, `current_owner_state`, `last_canonical_evidence`, `watch_state`, `collision_paths`, `next_allowed_action`.
- Persists: only the correct existing Agent OS handoff/discovery or this PR's records artifact; no census database/cursor.

- [ ] **Step 1: Freeze exact identities**

For each candidate record:

```text
repository
PR/branch
operation_key
workstream/wave
head SHA
base SHA
last update
current draft/merge state
latest CI
Slack thread/channel/message identity if present
current declared stop condition
```

- [ ] **Step 2: Classify current owner state**

Use only:

```text
ACTIVE_ACKNOWLEDGED
RETURNED_FOR_SOL
HELD_FOR_SOL
DELIVERY_ONLY
AUTHOR_SESSION_UNKNOWN
IDLE_RECONCILABLE
EFFECT_UNKNOWN
TERMINAL_ACCEPTED
```

These are census descriptions, not a new persisted lifecycle.

- [ ] **Step 3: Protect the Codex/H0 canary**

Record its visible current step and reserve its host/privileged-bootstrap/H0 paths. If exact GitHub carrier is not yet published, keep it `ACTIVE_LOCAL_CARRIER / IDENTITY_PENDING_RETURN`; do not search-and-replace it with another worker.

- [ ] **Step 4: Detect collisions**

At minimum compare:

```text
#170 ↔ CCR/Session Truth paths
#174 ↔ RuntimeBinding/Wake paths
#178 ↔ Agent Relay service/contract paths
#184 ↔ Claude preflight/OCR paths
#6182 ↔ Agent OS parser/Linear plan paths
Codex H0 ↔ capacity acquisition/privileged host/bootstrap paths
```

- [ ] **Step 5: Return the release matrix**

Every lane gets exactly one of:

```text
CONTINUE_EXISTING_OWNER
SOL_REVIEW_REQUIRED
SAFE_TO_COMMISSION_SAME_CARRIER
HELD_COLLISION
WAIT_FOR_ADMIN
TERMINAL_NO_ACTION
```

- [ ] **Step 6: Record material discoveries**

Use the correct existing Agent OS workstream. Do not create `WS:MASTERMIND-OS` or a new lifecycle store.

---

### Task 3: Reconcile and accept or repair Session Truth #170

**Carrier:** Mastermind #170 / `sol/session-truth-r1-20260827`  
**Existing operation:** `session-truth-r1-20260827-sol-001` plus the already-declared same-carrier repair key.

**Interfaces:**
- Consumes: current protected cross-plane source law and current Agent OS/GitHub/Linear/Slack/Executive snapshots.
- Produces: accepted `mastermind.session_truth_receipt.v1` with truthful deterministic semantic identity and required-source behavior.

- [ ] **Step 1: Review current head before commissioning**

Pin current head, comments, hosted checks and the two explicit open Sol rulings. Never create a second R1 carrier.

- [ ] **Step 2: Freeze the semantic-owner repair**

Preferred design:

```text
Agent OS owns a positive direct-record semantic projection/digest
→ Session Truth consumes that owner-produced identity
→ volatile acquisition/freshness fields remain visible but do not define record identity
```

Do not keep expanding a downstream blacklist of arbitrary `--now` fields without an owner-level law.

- [ ] **Step 3: Write RED-first discriminators on the same carrier**

Prove:

```text
same direct records across different acquisition clocks → identical record semantic identity
real authored record change → different identity
freshness/staleness can change visibly without pretending records changed
required PR→Linear binding + Linear unavailable → required source unavailable / modification not safe
optional Linear unavailable → unknown/degraded, never fabricated absence
```

- [ ] **Step 4: Implement the smallest accepted owner/consumer change**

If Macro Agent OS must emit the digest, use one separately reviewed Macro carrier or an exact cross-repo plan; do not silently import a floating Macro implementation into #170. If the existing Agent OS schema can already provide a positive semantic projection, reuse it.

- [ ] **Step 5: Run exact-head hosted tests and independent review**

Include strict JSON/non-finite rejection and exact repo-qualified identity falsifiers already requested in #170 review history.

- [ ] **Step 6: Rerun the frozen real current-estate proof twice**

Two unchanged owner observations at different acquisition clocks must produce byte-identical semantic projection/hash. Every source unavailable case remains typed unknown/degraded/refused according to scope.

- [ ] **Step 7: Sol accepts/merges and records exact next action**

Merge does not authorize R2-R7. Record the immutable current-estate receipt and capability state.

---

### Task 4: Reconcile and complete the Linear project portfolio path

**Carriers:** Macro #6182 / MAS-65, MAS-64, MAS-66.  
**Owner:** `WS:AGENT-OS` / MAS-27.

**Interfaces:**
- Produces first: deterministic zero-network `linear_portfolio_plan.v1` on Macro main.
- Then produces: dedicated app actor + project-only read/diff/apply canary and real portfolio dry run.

- [ ] **Step 1: Reconcile Macro #6182 on the same carrier**

Pin current Macro main and #6182. Determine why GitHub currently reports non-mergeable. Preserve the seven-file P0 contract. Do not rebuild the compiler.

- [ ] **Step 2: Repair only material current-base conflicts**

Current main wins outside the seven reviewed files. Never force-push over unknown branch movement. Rerun proof only when current parser/schema/P0 paths or proof law materially changed.

- [ ] **Step 3: Merge and verify P0 on Macro main**

Record exact merge SHA, compiler command, receipt and discrepancy census. Close MAS-65 only after main verification.

- [ ] **Step 4: Present the one exact MAS-64 admin action**

Provision `Mastermind Portfolio Projector` with app attribution and least privilege. Store no secret in Slack/Linear/GitHub/chat. Run the isolated create/read/update/no-op/conflict canary.

- [ ] **Step 5: Build MAS-66 project-only adapter**

Default dry run. Exact `WS:<KEY>` identity, one managed description block, optimistic reread, effect-unknown reconciliation, idempotent partial replay. No issue/comment/document/member/Executive/Slack/GitHub mutation.

- [ ] **Step 6: Run real portfolio dry run and Sol review**

Report would-create/update/noop/conflict items. Linear false-green remains a projection defect, never owner truth.

- [ ] **Step 7: Arm first real project projection only by separate Sol decision**

No continuous schedule or wave/issue projection yet.

---

### Task 5: Reconcile and complete Worker Presence / Agent Relay

**Carrier:** Mastermind #178 / `worker-presence-dialogue-wp1-20260827-sol-001`.  
**Owner:** `WS:CHAIRMAN-CONTROL-ROOM`.

**Interfaces:**
- Produces: accepted production-inert WP-1 contract/engine/service first; later production Agent Relay and company-dialogue capability through separately gated waves.

- [ ] **Step 1: Determine current active owner**

Read latest Slack thread, PR comments, head and CI. If an acknowledged owner is still executing Task 4/proof, continue that exact session and do not commission another COO.

- [ ] **Step 2: Sol reviews exact current head**

Check V1 compatibility, closed V2 identity, no hidden persistence, no Wake/Executive mutation, no production Slack credential/message.

- [ ] **Step 3: Repair on the same carrier if required**

RED-first, exact path reservation only. Unexpected movement returns to reconciliation.

- [ ] **Step 4: Accept/merge WP-1 as `BUILT_NOT_PROVEN / PRODUCTION_INERT`**

Do not call this live dialogue.

- [ ] **Step 5: Release production Agent Relay A2/A3**

Use one dedicated least-privilege app/host principal and one exact non-authoritative canary, then prove a real already-active Sol↔Fable project dialogue.

- [ ] **Step 6: Build/accept WP-2 company-dialogue gateway**

Bind capability to current exact Attempt/Worker/session context. No provider-specific Slack gateway.

- [ ] **Step 7: Build WP-TW1 and WP-TW2**

Stateless deterministic classifier first, then adapter into existing Wake vocabulary. No watcher DB/cursor/queue/daemon.

- [ ] **Step 8: Retire temporary session watchers after production proof**

Prove no lost turn before removing scaffolding.

---

### Task 6: Reconcile and complete Wake #174 and later acknowledgement

**Carrier:** Mastermind #174 / `wake-pr3-native-transports-20260827-sol-001`.  
**Principal integrator:** Sol CTO / ChatGPT2 after exact-carrier reconciliation.

**Interfaces:**
- Produces first: truthful provider-native delivery with `DELIVERED_UNACKNOWLEDGED` maximum claim.
- Later produces: target reasoning-session ACK ingress and source resolution through accepted existing Wake law.

- [ ] **Step 1: Review current #174 head and canary evidence**

Check Codex exact-thread delivery, Claude verdict, production disarmed state, no new queue/session DB/scheduler.

- [ ] **Step 2: Repair or accept the transport-only carrier**

Do not synthesize ACK/source resolution. Do not absorb Capacity/OCR or Slack Agent Relay.

- [ ] **Step 3: Merge only the transport wave after Sol acceptance**

Record exact provider support/refusal.

- [ ] **Step 4: Freeze the later ACK/source-resolution wave**

Consume exact RuntimeBinding and provider/session target identity. Provider response cannot self-author target ACK.

- [ ] **Step 5: Integrate WP-TW source facts through existing Wake authority**

Repeated observation maps to the same source/obligation identity; effect-unknown remains same destination reconciliation.

- [ ] **Step 6: Prove one end-to-end return wake**

Real dialogue result → typed attention → Wake → exact Sol runtime → target ACK → source resolution, without Chris carrying the message.

---

### Task 7: Preserve and complete Capacity Fabric / current Codex H0 carrier

**Owner:** `WS:EXECUTIVE-CAPACITY-FABRIC`.  
**Current operator:** existing Codex/CEO canary session; no replacement until return.

**Interfaces:**
- Produces: exact H0 installed-host pass and two verify-only passes, then independent P0 census.
- Does not produce yet: capacity-aware routing, Claude provider call, automatic failover.

- [ ] **Step 1: Do not interrupt the current operator**

No Slack reassignment, no second branch, no overlapping H0/bootstrap files.

- [ ] **Step 2: Receive its exact continuation return**

Require carrier/branch/head, four changed files, RED-first evidence, independent review, hosted proof, host/admin steps remaining and explicit stop state.

- [ ] **Step 3: Sol adversarial review**

Check no credential exposure, generic privilege service, provider call, routing, hidden daemon or duplicate acquisition path.

- [ ] **Step 4: Run the single native H0 ceremony**

Action-time human confirmation where required. One operation/carrier. Any ambiguous effect stops for reconciliation.

- [ ] **Step 5: Run two verify-only passes**

Require exact `H0_INSTALLED_HOST_PASS_NOT_P0_ACCEPTED`, all realms disabled/unloaded, sockets absent, no login/provider work.

- [ ] **Step 6: Run independent CF2-P0**

Only accepted P0 releases CF2-I. Refusal remains a truthful stop.

- [ ] **Step 7: Continue CF2-I → RF1/HF1 → PF1 under existing plans**

Capacity ranks only already-eligible candidates. One broker/adapter lifecycle, no provider-specific scheduler.

---

### Task 8: Reconcile and complete Operator Continuity

**Current carrier:** Mastermind #184 for OCR-1 V3 first slice.  
**Owner:** Capacity Fabric + accepted OCR source law.

**Interfaces:**
- Produces: one canonical Claude preflight family, rotation-safe capacity identity, continuation binding, sustained Claude realm and safe read-only cross-realm rollover.

- [ ] **Step 1: Determine #184 current owner and review state**

If active, continue existing session. If returned/idle, commission same carrier only. Do not touch the current H0 canary's reserved paths.

- [ ] **Step 2: Accept OCR-1 slices in order**

Task 1/2 closed contract/refusal; Task 3 actual Worker-broker execution-context readiness; Task 4 realm-set verifier; Task 5 real admin provisioning/census. PF1 owns first model work call.

- [ ] **Step 3: Complete OCR-2C capacity identity**

No account-label/ordinal/name joins. Shared AI Provider Control remains the normalizer.

- [ ] **Step 4: Complete OCR-3 continuation capsule/RuntimeBinding**

One immutable PREPARED capsule per target Attempt; exact idempotency and effect-unknown law.

- [ ] **Step 5: Complete OCR-4A/4 after CF2-I/RF1/HF1/PF1 gates**

One concrete rich adapter factory per already-claimed realm; prove sustained real Claude operator.

- [ ] **Step 6: Prove OCR-5 positive and adverse rollover**

Automatic quota rollover only for canonically non-modifying work. Interrupted write/effect-unknown remains blocked.

---

### Task 9: Build the Executive Steward and Responsibility Matrix

**Prerequisites:** accepted Session Truth; current Control Room source path; OCR-3/5 continuity; Capacity projection; WP-2/Agent Relay and relevant Wake/attention facts.

**Files (follow current protected paths at implementation time):**
- Create: `control_plane/operator_continuity_projection.py`
- Create: `control_plane/executive_steward.py`
- Modify: `control_plane/chairman_control_room.py`
- Modify current governed local UI renderer
- Add focused tests and browser proof

**Interfaces:**
- Produces: source-attributed read-only `OperatorContinuityProjection`, `ExecutiveStewardSnapshot` and Chairman Responsibility Matrix.
- Never calls: Job create/requeue/claim/terminal transitions, Wake dispatch, Slack send, provider start/resume or capacity selection.

- [ ] **Step 1: Implement the pure continuity projection RED-first**

Pin `RUNNING`, `REBINDING`, `WAITING_CAPACITY`, `BLOCKED`, `TRANSPORT_DEGRADED`, `COMPLETED`, `UNKNOWN` and exact root/current joins.

- [ ] **Step 2: Add source-attributed continuity to CCR cards**

No synthetic overall lifecycle. Mismatched joins remain degraded/unjoined.

- [ ] **Step 3: Implement `ExecutiveStewardSnapshot`**

Required read methods:

```text
list_responsibilities
get_responsibility
get_current_runtime
explain_blocker
get_attention
```

- [ ] **Step 4: Build the Responsibility Matrix UI**

Show organizational phase, accountable roles, runtime, carrier, evidence, capacity, watcher/attention, intentional wait and exact links.

- [ ] **Step 5: Prove desktop/narrow and all failure states**

Use real production-safe data. Browser screenshots are product proof, not runtime authority.

- [ ] **Step 6: Prove the Chairman usability questions**

Chris can answer the ten questions from the spec without opening `#agent-dispatch`.

---

### Task 10: Add bounded Linear collaboration projection

**Prerequisite:** accepted project-only MAS-66 and explicit arming.

**Interfaces:**
- Produces: selected wave/gate issue projection and organizational updates/comments.
- Excludes: every Executive Job/Attempt, provider account, Wake obligation and raw Agent Relay protocol.

- [ ] **Step 1: Freeze V2 selection law**

Only major waves/gates/deliverables with exact Agent OS identity are candidates. Similar titles never join.

- [ ] **Step 2: Add deterministic issue/update desired-state plan**

Report-only first. Exact managed blocks and optimistic concurrency.

- [ ] **Step 3: Prove dry run against current portfolio**

No duplicate issues, no false completion, no raw machine chatter.

- [ ] **Step 4: Arm bounded apply after Sol review**

Human organizational discussions may sync to Slack; machine protocol never syncs into Linear.

---

### Task 11: Activate Grok Secretary and optional OpenClaw

**Prerequisite:** accepted Executive Steward read API.

**Interfaces:**
- Grok consumes: Steward + Linear/GitHub/Slack read context.
- OpenClaw consumes: allowlisted Steward reads first; later exact bounded actuator calls.

- [ ] **Step 1: Diagnose the current Grok preflight non-ACK**

Check service/runtime configuration and Slack event delivery without probing secrets or granting wider authority. Do not infer the bot is running because its Slack identity exists.

- [ ] **Step 2: Prove Grok read-only capability**

Answer exact questions from structured sources and surface source unavailable/disagreement states.

- [ ] **Step 3: Route only high-signal exceptions to `#ceo-control-room`**

No giant commissions or raw protocol dumps.

- [ ] **Step 4: Re-research and pin current OpenClaw installation/API**

No generic SSH or provider selection.

- [ ] **Step 5: Prove read-only unplug test**

Stopping Grok/OpenClaw changes zero canonical state and loses no queued work.

- [ ] **Step 6: Release bounded actuation separately**

One exact authorized operation, one carrier, effect-unknown reconciliation; no fallback.

---

### Task 12: Cut over operating modes and run final acceptance

**Interfaces:**
- Consumes: all accepted prior lane receipts.
- Produces: explicit Mode A→B→C→D→E promotion receipts and final Agent OS/Linear/GitHub closeout.

- [ ] **Step 1: Promote to SHADOW**

Control Room/Session Truth/Linear observe current manual carriers. Calibrate false positives/negatives.

- [ ] **Step 2: Promote to HYBRID**

New eligible work enters Executive OS; legacy/manual carriers remain labeled and finish in place.

- [ ] **Step 3: Promote to EXECUTIVE-FIRST**

Normal work routes through Executive/Capacity/OHF. Slack manual pickup becomes exceptional.

- [ ] **Step 4: Prove positive rollover journey**

Real Fable read-only work: realm A → quota terminal → realm B → same dialogue → result → Wake → Sol → closeout. Chris performs no message/account routing.

- [ ] **Step 5: Prove negative effect-unknown journey**

Write-capable/uncertain effect remains reconciliation-required with zero failover and truthful Control Room/Linear/Grok state.

- [ ] **Step 6: Promote to FULL FABRIC**

Chris normally uses Control Room + Linear + Grok. `#agent-dispatch` is backend plumbing.

- [ ] **Step 7: Durable closeout**

Update existing Agent OS workstreams/decisions/discoveries/handoffs, Linear projections, exact GitHub receipts and the one next action. Do not leave the final ruling only in chat.

## Program stop condition

The program is complete only after the Chairman usability ruler, positive production journey, adverse reconciliation journey and removal/unplug tests all pass on real current systems. A collection of green PRs, merged infrastructure, Slack delivery, Linear `Done`, QUEUED Jobs or fixture canaries cannot satisfy completion.