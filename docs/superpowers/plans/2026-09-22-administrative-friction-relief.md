# Administrative Friction Relief Implementation Plan

**Goal:** remove avoidable start/owner/CI/watcher stalls without weakening actual effect boundaries.
**Architecture:** amend existing procedure owners and native entrypoints; integrate runtime recovery
through the incumbent Executive/Workspace/COO/Dialogue paths, never a second controller.
**Tech stack:** Markdown source contracts and Python/pytest; existing Runtime and provider evaluation.
**Spec:** `docs/superpowers/specs/2026-09-22-administrative-friction-relief.md`.

## Global constraints

Current Chairman assignment authorizes the scoped source repair. Preserve same-operation effects,
source fences, current grants, independent review and release checks. A policy PR is not fleet rollout.
Runtime files and incumbent worker bindings are excluded from this first source slice.

## Review focus

Direct assignment versus retrieved packet; archived chat versus live Attempt; unknown liveness versus
expired lease; old-head CI versus current candidate; missing optional watcher versus required transport.
All have explicit negative cases in the spec and source-contract assertions in the regression file.

## Task 1 — coherent execution defaults (inline Sol execution)

- [x] Pin protected source and inspect all open-PR path overlaps.
- [x] Add `tests/test_administrative_friction_relief.py` before modifying procedure.
- [x] Run `python3 -m pytest -q tests/test_administrative_friction_relief.py --tb=short`;
  baseline observation: 12 assertion failures, not a model-behavior measurement.
- [x] Amend AGENTS.md, CLAUDE.md, DELIVERY_WORKFLOW.md, Dialogue and the existing INDEX,
  ACTIVE_EXECUTION and COLD_START skills with the scoped defaults in the spec.
- [x] Run the new tests and incumbent compatibility tests; record all observed failures below.
- [ ] Inspect `git diff --check` and exact staged paths; publish one coherent candidate PR.
- [ ] Obtain independent review and required CI, then use protected merge/readback before rollout.

## Task 2 — runtime integration and real adoption (incumbent owners)

- [ ] Reproduce the exact COO preclaim-unavailable and stored-cycle-block cases against the real
  Runtime fixtures; separate intended root-wide security blocks from avoidable lane-local waits.
- [ ] Add failing independence/fencing regressions before changing the incumbent selection/recovery
  functions. Keep dispatcher effect-unknown reconciliation and admitted path/budget checks intact.
- [ ] Exercise existing lease/successor and current-receiver return paths without manual parent ACK.
- [ ] Enroll accepted source into current Web/Claude/Codex producers; inventory Macro/Terminal and
  installed-consumer revisions rather than assuming this repository updates every surface.
- [ ] Run the spec's matched acceptance matrix through the existing evaluation owner; source tests
  alone cannot close adoption. Keep production and instruction evidence separate.

The remaining runtime task is bounded integration work, not a new registry or blanket permission grant.
No new human approval is needed for routine investigation already inside this commission; reserved
merge/release/authority gates remain local to those actual effects.
