# Web CEO Role-Adaptive Delegation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans for admitted implementation. Current source-custody and fabric placement rules override generic worktree/spawn examples.

**Goal:** Make Astra/Sol Web CEO sessions use different provisional working profiles while delegating bounded labor through the existing fabric and preserving useful Sol orchestration.
**Architecture:** One principal responsibility; concentrated-judgment and sustained-orchestration preferences; existing Executive/COO/Capacity/Dialogue execution only. Reuse #506, #591 and the existing external-fabric program rather than introducing another router, evaluator, brief compiler or owner.
**Tech Stack:** Existing Markdown Skillpack, JSON ScenarioPackets, Python/pytest and `scripts.ohf.fresh_sol_eval.ScenarioPacket`; existing Executive five-tool interface for the later live pilot.
**Spec:** `docs/superpowers/specs/2026-09-14-web-ceo-role-adaptive-delegation.md`.

## Global constraints
- Operation `web-ceo-role-adaptive-delegation-20260914-sol-001`; parent `WS:EXECUTIVE-CAPACITY-FABRIC`; Original design basis: Mastermind `bffe2ca8506346ea278c8ee469bf1ac30a4de008` with compatible Skillpack 1.0.1/bootstrap 1. The current protected integration base is intentionally not frozen in this plan; re-pin it from protected Git and the exact PR/evidence receipt before any modifying or enrollment action.
- Exactly five additive candidate paths: this plan, its spec, `docs/sol_skills/WEB_CEO_DELEGATION.md`, `research/fixtures/web_ceo_role_adaptive_delegation_2026-09-14.json`, and `tests/test_web_ceo_role_adaptive_delegation.py`.
- Repository-placement disclosure: `scripts/ohf/fresh_sol_eval.py` materializes every `docs/sol_skills/*.md`, so merging the candidate skill also places its bytes in a fresh-Sol evaluation bundle pinned at or after that merge. This is evaluation exposure, not production enrollment, INDEX enrollment, runtime authority, live delegation, or served-model proof.
- Do not edit INDEX, ModelRouter, AGENTS, existing source laws, provider settings, active worker branches or shared workspaces in this carrier. Existing owners must reconcile enrollment and source-law changes.
- Runtime identities, source grants, provider/budget eligibility and exact parent consumption are mandatory; source policy never arms them. No credential reads, new provider calls, installs or live effects are part of the five-file source package.
- Preserve MiniMax holds at their owner-defined scope for the exact model/harness/realm/host route while retaining truthful kit-path evidence; never promote a kit result to Executive admission or erase it with a broader hold. Preserve all incumbent #615/#623/other fabric repair ownership. Unbound review work is WAITING_CAPACITY, not an assigned worker or an executing Job.

## Task 1 — Materialize a reviewable instruction and scenario package
**Files:** the five additive paths above. **Consumes:** existing `ScenarioPacket(scenario_id, prompt, pass_requires)`. **Produces:** one candidate procedure and fourteen synthetic packets WCD01–WCD14, plus static compatibility checks. This is not a new evaluator or a model-behavior pass.
- [x] Write the spec from the current Chairman-approved direction, separating anecdotal timing from measured capability and preserving incumbent authority.
- [x] Write tests before the instruction/corpus: require all fourteen unique packets, the existing exact three-field dataclass contract, bounded prompts and explicit PASS/FAIL criteria; check the instruction's role/direct-work/continuity boundaries.
- [x] Observe RED because instruction/corpus artifacts are absent, not because a provider or optional test dependency is missing.
- [x] Supply the candidate skill and corpus. The data materializes directly as `ScenarioPacket(**row)`; no provider, model, account, Job or permission fields are accepted by this fixture contract.
- [x] Run `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider tests/test_web_ceo_role_adaptive_delegation.py --tb=short` in the managed operation workspace; require exit 0 and no skips/errors.
- [x] Verify exact five-path delta, JSON parse, `git diff --check`, and immutable source publication as DRAFT/HOLD. Preserve any dirty/local-only work; no automatic merge, installation or enrollment.

## Task 2 — Obtain one existing-owner review and source-integration disposition
**Owner:** incumbent fabric principal/Capacity and the existing non-author review lanes. **Consumes:** exact published candidate SHA and current #506/#591 heads. **Produces:** an attributed exact-artifact verdict and the current source-enrollment gate.
- [ ] Fresh-read existing review assignments before creating any review child. Reuse an assigned eligible reviewer; otherwise request placement through the existing owner with `PREFERRED_AVENUE: Terra`, `WHY NOT FABLE: bounded exact-source review; principal ambiguity is frozen`, and `CAPACITY_SELECTABLE / WAITING_CAPACITY / needs_placement` until actual assignment.
- [ ] Review gates: preserve the five-tool submission closure; no automatic model switch or extra COO authority; no hard timing promises; no mandatory dual-principal chain; complete bounded worker briefs; explicit exceptions; source/runtime/Web evidence separated; provider/credential/unknown-effect holds intact. A non-author reviewer must report actual artifact identity and test evidence, not certify from the PR title.
- [ ] Obtain the existing approved worker budget/cap and current eligibility from their owners before dispatch. A GLM/MiniMax preference is not admission; the specific MiniMax model/harness/realm/host route remains unavailable unless the incumbent canonical owner admits that exact route. No new metered executive layer is authorized.
- [ ] Reconcile #506 `18730195f431e9c42b46e6a832f8ef4739520eaa` and #591 `35530cc83eadee82ba41c1db7a38f12cf71429fa` against their actual current heads before relying on this snapshot. Preserve the same PRs/source writers; no duplicate duration fix or active-turn controller. Both need their actual independent/current-base release evidence, not this plan's endorsement.
- [ ] Align the governing routing laws through their existing owner so substantive Sol orchestration is recognized while standalone routine turns remain non-Pro by default. Incidental handoff/lifecycle steps inside substantive work must not force model-switch ceremonies. Until #506 or an accepted successor lands, apply the current closed `PRO_MODE_TASK_CLASS` set and 80–1440-minute complete receipt exactly. Never fake a class or duration estimate; an ineligible turn fails closed as `PRO_MODE_REFUSED / USE_NON_PRO_MODE`.

## Task 3 — Enroll through the existing procedural/brief consumers
**Owner:** protected #591 INDEX/ACTIVE_EXECUTION boundary plus #618 external-fabric instruction/brief integration owner. **Consumes:** accepted immutable skill and exact updated source laws. **Produces:** source-only procedure availability first, then a fresh session that actually receives the correct procedure through an existing producer.
- [x] Re-pin protected INDEX and reconcile #147/#504/#518/#591/#598 before enrollment. Operation `web-ceo-delegation-enrollment-20260915-sol-001` adds the WEB_CEO_DELEGATION pointer through the protected INDEX/ACTIVE_EXECUTION boundary, preserves one finalization controller and same-SHA loading, and uses one managed source workspace.
- [ ] Extend the existing external-delegation AGENTS/brief composition to carry the compact instruction without duplicating the role/brief compiler owned by #587–#590. Inputs remain objective, constraints and existing identity/grants; outputs remain existing bounded worker briefs/returns. No model/provider/account selector is added to `submit_ceo_intent`.
- [ ] Prove a fresh consumer loaded the exact enrolled instruction bytes, not stale Project memory. Distinguish INDEX/fleet enrollment from automatic directory inclusion in the fresh-Sol evaluation bundle: the latter is evaluation exposure, not production enrollment. Source merge does not edit ChatGPT Project settings or existing chat context; separately verify the intended Web/Project adoption path. Surface-specific permissions must remain intact.

This enrollment slice is source-only procedural enrollment. It does not prove Web adoption, Executive admission, worker execution, or parent consumption, and it grants no provider, credential, host, model-setting, reasoning-mode, lifecycle, retry or production authority.

## Task 4 — Prove the first useful delegated review loop
**Owner:** existing Executive/COO/Capacity/fabric principal, within its current authorized envelope. **Consumes:** live tool/auth readiness, exact review candidate, current receiver/custody/budget. **Produces:** real result plus lawful parent decision.
- [ ] Resolve current supported Executive client access. Require a non-fixture canonical read and the correct authenticated submission surface before submitting; saved configuration, shell reachability and prior connector availability are not proof. A missing/blocked tool stays a named lane boundary, never a reason for raw-socket/provider-spawn substitution.
- [ ] Use a useful exact-artifact review from Task 2, avoiding an already assigned review. Supply a stable operation key and bounded objective/profile/write/test constraints through the existing `submit_ceo_intent` contract. Preserve `dispatched=false` as admission, not worker failure or START.
- [ ] Observe actual canonical worker execution and bounded return. Validate exact candidate identity, claims/evidence, independence when required, budget accounting and the principal's real consumption. Issue the correct same-carrier acceptance/repair/continuation/STOP edge; shut down only this child's temporary watcher source when terminal.
- [ ] Exercise the relevant refusal boundary without real secrets: missing eligibility refuses before any effect; uncertain submission reconciles the same operation; disjoint safe work remains available. Do not widen into the incumbent #615/#623/credential repairs or duplicate the #629 offline canary.
- [ ] Record actual job/attempt/return/parent-consumption evidence through existing owners. If native worker execution works but exact Web parent consumption does not, classify PARTIAL and name that gap; never label the heterogeneous Web journey PROVEN_LIVE.

## Task 5 — Evaluate role fit on the real surface
**Owner:** existing fresh-session evaluation/observability owners and action-authoritative principal. **Consumes:** WCD01–WCD14 plus approved representative work. **Produces:** attributed behavioral evidence and a reversible routing decision, not a synthetic leaderboard.
- [ ] Use each JSON row as the existing `ScenarioPacket(**row)` for procedural evaluation; retain prompt/output identities with the existing evidence tools. The fixed native fresh-Sol model and its auth/no-MCP/no-plugin constraints remain unchanged; do not present native results as Astra/Sol Web Pro results.
- [ ] Run the actual Web-profile comparison only on authorized surfaces with actual model/mode/tool identity. Match task classes, acceptance criteria and review standards; do not send two live modifiers to the same candidate. Count accepted outcomes, routine principal interactions, worker/review spend, repair burden, human intervention, useful work versus waiting, and observed termination/truncation. Unknown usage is not zero.
- [ ] Reject early finalization while an in-scope useful dependency remains; also reject unnecessary duration padding, pure polling and duplicate work. Treat the reported timing ranges as hypotheses only. Do not change platform limits, automate unsupported Web model switching or create an account-allocation workaround.
- [ ] Promote, revise or reject the profile preference based on preserved quality and useful progress. Small samples establish feasibility only. Falsifying the Sol/Astra preference must be allowed without rebuilding the execution system.

## Handoff and completion boundary
Preserve the incumbent principal `agent-fabric-end-to-end-fable-integration-20260913-sol-001` and its exact carrier `C0BSBM78V1N/1789324397.992989`. This additive source operation does not seize that office, its active children or its Agent OS writer. Deliver one bounded source/evidence intake to that owner; a buried intake is not a child assignment. Have the incumbent records writer include the Chairman correction, exact candidate/proof, unresolved enrollment/live-transport gates and next action in the next canonical checkpoint.

Source package completion means exact source publication and current review disposition. Operational completion requires actual enrollment and a useful admitted worker result consumed by the intended parent. Comparative-role acceptance additionally requires genuine Web evidence. Missing source review, client permission, provider eligibility or parent binding is a real gate; preserve useful independent work and stop only at the actual remaining boundary. Do not claim background work from an unassigned Slack request.

## Author execution receipt
The managed source workspace was allocated by mmx-workspace for this operation at the pinned base. The tests-first run (native process 37292) returned exit 1 with absent-corpus/procedure assertion failures. After materializing both artifacts, the same bounded command (process 59701) executed all 31 historical checks and returned exit 0. The process IDs and draft-PR observations in this section are author-local execution assertions, not facts independently recoverable from the five source files. That historical focused count was later independently reproduced. The pre-enrollment repaired-head focused suite collected and passed 44 tests, including structural non-enrollment, no-bypass and closed Pro-class guards. These are static instruction/data-contract checks against the existing ScenarioPacket import, not provider execution, Web Pro behavior, enrollment or production proof.

## Live integration delta after initial publication
The incumbent Fabric carrier was freshly read through Sol ruling `1789412997.054509` after the first candidate was published. HF1-Q CORE #630 is now merged at `1ee9b8b3` and owns the protected heterogeneous per-WORK-step placement core: v1 stays compatible, Model Router fixes the lawful tier, and Capacity ranks eligible candidates only inside that tier. Re-read the merged protected text at enrollment rather than relying on this carrier-attributed summary. This operation must not duplicate that owner or mutate the public five-tool submission contract. The first useful single-qualified-lane pilot need not wait for heterogeneous placement; multi-provider-under-one-parent proof still requires actual admitted execution and parent consumption, not the merge alone.
The incumbent T3 report also exposed reserved-child capacity pressure: planning must count future review/repair descendants under current COO policy, not just active execution slots. The candidate skill now states that constraint explicitly; this is a planning requirement, not a budget/limit change. #615 and #623 retain their incumbent repair lanes. The carrier evidence is attributed integration state, not source execution or production proof performed by this operation.

The useful exact-source review in Task 2 may use an already-approved available manual/operator route before effects, within current placement/custody/budget law; it need not wait for Task 4 Web Executive client restoration. Record its actual manual delivery, pickup, START and result separately from Executive lifecycle. This exception never permits rerouting a platform-blocked inspection through another actor, inventing a direct spawn path, or changing the carrier of a started/effect-unknown operation.

Source publication: draft PR #632 was created from the first immutable candidate `09389f101c3197e48646d2baced5c84a31055d47`. After the review/repair-reservation and lawful manual-route clarifications, the bounded static suite ran again (79417) with all 31 checks and exit 0. A manual read-only exact-source review of old head `83d44e5c2714f0594b81c5dbe3b99a5b5ae7fea9` returned PASS 90/100 with zero contradictions and no blocking findings, independently reproducing 31 focused and 122 sibling checks. That verdict is exact to the reviewed old head; its nonblocking findings drove the present repair and do not certify the repaired head until a new exact-head review completes. At #632 source-package publication, procedure enrollment was pending. This enrollment slice closes only the same-pinned INDEX procedure-availability step; Executive admission, live Web execution and parent consumption remain pending.
