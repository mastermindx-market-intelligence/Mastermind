# Mastermind OS — integrated rollout and product acceptance contract

Date: 2026-09-16
Operation: `mastermind-os-rollout-contract-20260916-sol-001`
Organizational home: existing `WS:CHAIRMAN-CONTROL-ROOM` / `project-active-build-control`
Program CEO: Sol, assigned by the live Chairman in this conversation.
Execution integration: incumbent Fable; reported and carrier-confirmed Claude8 seat `aa22a3d2-2778-41c7-b61a-6a0e1a81e6c3`.
Execution root: `agent-fabric-end-to-end-fable-integration-20260913-sol-001`.
Procedure/source pin: Mastermind `8ba7deedde164c90298d3e88785d98e02fa5e2d2`, Skillpack 1.0.1 / bootstrap 1.
Disposition: proposed current rollout contract plus executable, offline product reference; NOT_INSTALLED, NOT_PRODUCTION_ACCEPTED.

## 1. Mandate and the two completion journeys

Mastermind OS is the standalone operating product for the autonomous Mastermind organization. Agent Fabric is its execution subsystem, not the whole backend and not the whole product. Sol owns the integrated outcome, architecture, sequencing, compute/delegation policy within existing grants, adversarial review, rollout and final product acceptance. Fable retains its existing execution-integration responsibility and the custody of its current children. No new Fable root or Executive lifecycle is created by this record.

Human journey: direction -> understandable company state -> exact program -> actual work and responsible office -> permitted conversation -> dependency and evidence -> correctly addressed decision or intervention -> observed receipt -> useful result. Chris must not reconstruct this from numbered accounts, Slack archaeology or a graph of guessed relationships.

Machine journey: admitted objective -> accepted plan -> eligible capacity commitment -> qualified tools/workspace -> real work -> independent review and bounded repair -> exact parent consumption -> next permitted action -> durable evidence and learning. This must continue with all viewers closed, within existing authority. A reserved decision remains pending when its decision-maker is unavailable; silence never transfers authority.

The moat is the useful combination of company context, correct identity, accepted dependencies, qualified execution, accountable results, recovery and learning. Receipts support that capability; an impressive receipt browser is not the product.

## 2. Explicit scope and retained boundaries

Included in the full program: the product shell; decision-first orientation; program/mission workspaces; organizational and execution relationships; conversations and consultations; bounded actions and reconciliation; Agent OS/portfolio/evidence integration; provider and compute resources; tools and browser/computer/workspace capabilities; continuity and recovery; operational assurance; evaluation/learning; remotely usable web delivery; and later a native Mac client using the same backend contracts.

This is an integration umbrella over current work, not permission to rewrite every subsystem. Necessary new producer fields, owner-side transactions, finite read resources and UI consumers are real engineering work. Existing owners keep their canonical stores and effect boundaries. Domain products such as Prophet, BioCatalyst, research and paper portfolios may expose capabilities through OS navigation, but their data, validation and financial authority do not move into this program.

Do not create a second Job store, session registry, transcript archive, graph authority, credential owner, capability catalog, queue, retry policy, scheduler, freshness clock, portfolio synchronizer or universal invoke endpoint. Disposable presentation indexes may be rebuilt from owner facts. One product does not require one process, one database or one privileged service.

## 3. Corrections to the earlier assessment

The old twenty-section integrated blueprint on Mastermind #595 is significant architecture evidence, not nothing. #595 was closed unmerged on 2026-09-14; that alone does not prove its product vision was rejected. Its closure event has no recorded rationale, and the adjacent #612 source-law text explicitly said not to auto-close Live Fabric. This contract neither reopens #595 nor silently treats it as protected law.

A Figma prototype existed. The earlier conclusion should be “the design was unfinished/paused,” not “no Figma had been made.” The last recorded file is `GfH3jNfel8F2cv7ZdTtiXt`; later conversation-first frames superseded earlier graph-first frames. Present canvas contents and design-writer custody are not reverified here. Preserve the last explicit Figma pause; this operation creates no new Figma file and edits no existing frame.

No terminal STOP found on a program is not proof a provider process is running or that work is monitored. Claude8 pickup/checkpoints establish an incumbent reported principal, not a permanent live heartbeat. The latest read checkpoint reports `WATCH_UNAVAILABLE`; do not manufacture unattended supervision from it.

The currently active DF1 Today stream is independently owned. Its current work must be consumed, not rewritten. The recorded native-workspace reference mentioned in prior conversation is a separate unverified artifact candidate until its exact source and owner are recovered; it must not be mistaken for an installed authenticated content resource.

## 4. Authority and responsibility map

| Concern | Owner retained | OS responsibility |
|---|---|---|
| Company strategy and reserved powers | Chairman and accepted strategy/authority owners | Explain and address; never infer new grants |
| Integrated product thesis and acceptance | Sol / existing CCR responsibility | Freeze contracts, sequence dependencies, accept real journeys |
| Job/Attempt/Worker/Event, claims and effects | Executive OS | Read exact facts; commands return through the existing owner |
| Runtime implementation integration | Existing Fabric Fable and current child owners | Supply finite producer contracts and real execution evidence |
| Workstreams, decisions, discoveries, handoffs | Macro Agent OS | Exact references, correction through its existing writer |
| Model suitability and resource commitment | Model Router, Provider Control, Capacity and Executive claim | Explain eligibility, uncertainty and spend without a second selector |
| Native session and current responsibility | RuntimeBinding / SessionTarget / continuity owners | Keep office, session, Attempt and host distinct |
| Dialogue, returns, consumption and attention | Company Dialogue / Relay / Wake | Show each stage separately; do not infer consumption |
| Company-state composition | Steward / Control Room | Add finite source-qualified resources through the existing boundary |
| Source, CI, review and release evidence | GitHub and the owning deployment/proof path | Exact candidate and installed revision, not merge-is-live |
| Portfolio and collaboration projection | Existing Agent OS-to-Linear / Workroom owner | Exact Project/WS and selected Issue/operation joins |
| Workspace, host and tool effects | Workbench, fleet and accepted capability owners | Bounded operations only after current eligibility |
| Learning and evaluation | Existing evaluation/evidence owners | Measured outcome/repair/reliability; no self-promoted routing score |

An umbrella CEO does not become the action target for every already-STARTed child. Existing child Sols and writers remain authoritative until their own accepted transfer/release. Fable must not receive conflicting repair commands from an OS experience worker.

## 5. Twelve lenses; not twelve new databases or dashboard tabs

Organization, programs, execution, conversations, dependencies, authority, capabilities, fleet/resources, evidence, continuity, attention and learning are lenses over the same exact references.

The first product preserves the protected decision-first hierarchy: Today, Programs, Ask Sol, with explicit Advanced access. A Program opens a workspace. The workspace offers conversation/context, work, connections and evidence. Fleet, tools, service diagnostics, detailed continuity and evaluation live in Advanced until separately usable. Powerful graphs are available where they answer a question, not the mandatory landing page.

Every displayed relationship declares its kind: organizational accountability, execution containment, accepted prerequisite, consultation, transport delivery, result consumption, evidence support or resource placement. Never draw these as interchangeable arrows. A design prerequisite is not an admitted runtime dependency. A native helper is not automatically another Executive Job. Missing joins remain unjoined.

## 6. First real integrated user workflow

The first installed OS proof must let Chris open an actual program, select one admitted mission, identify its accountable principal and qualified child, see supported safe output with source coverage, inspect the exact review/result, and distinguish returned from consumed and accepted. It must expose one meaningful interruption or pending decision and its correct repair/decision owner.

This first read workflow does not wait for every provider, every host, a native Mac wrapper or a complete design board. It also does not depend on new source writers taking over #653, #677, #688, AD-RET2, CCTX-1, PF1 or DF1. It consumes their interfaces as they qualify.

The first modifying user workflow follows as a separate admitted capability: one precisely addressed message or decision through an existing command owner, a known receipt or explicit uncertainty, and same-operation reconciliation. No generic send-anything or hidden shell fallback. Read-only status must remain available after modifying authority is removed.

## 7. Finite producer-to-consumer contract inventory

This table fixes questions to close at each existing owner. It does not define a new universal wire envelope.

| Resource | Required source facts | Real consumer and refusal |
|---|---|---|
| Program orientation | exact workstream/portfolio join, recorded outcome and next action, section coverage | Programs; missing join is unjoined, missing coverage is not zero |
| Mission observation | canonical root/child/Attempt identity, plan revision, source generation, role and current state | Workspace; no runtime facts inferred from Slack or PR labels |
| Visible content/history | authorized session/turn/item identity, safe content, ordering, correction, retention and coverage | Conversation; no second stdout reader, scraping or consumption per viewer |
| Relationship view | owner-issued endpoint refs, relation kind, accepted revision when required, correction/removal | Connections; absent or guessed relationships never execute work |
| Decision/preflight | current decision owner, target, scope/grant, exact candidate, effect/status owner | Decision panel; changed target/source forces fresh preflight |
| Result/evidence | exact artifact, review, repair revision, return and parent-consumption facts | Evidence; CI, merge, deployment and acceptance remain separate |
| Fleet/capability | canonical host/worker refs, qualified tool generation, current entitlement/resource observations | Advanced; wrappers/accounts do not multiply entitlement |
| Continuity/learning | owner checkpoints, pending obligations, real acceptance and evaluation receipts | Recovery/learning; no fabricated resume, success rate or uptime |

The first integration return must identify the exact current callable/resource contract, producer revision, field-level availability, security boundary and test fixture for the mission observation and visible-content rows. An approximate function name or a “compatible” JSON object does not qualify it.

## 8. Time, null, correction, coverage and consistency

Record source occurrence time, observation time and generation separately when provided. Never refresh old evidence by adding a new browser timestamp. A snapshot is not current merely because it was fetched recently.

Use current owners' validity and coverage classifications. An incomplete list can show known items but cannot claim an exact total. Unknown quota is not unlimited, no known worker is not zero workers, and an absent decision feed is not all-clear.

A multi-owner view must expose its observation vector and mismatches. It is not a global atomic company snapshot unless an existing owner proves that property. Apply bounded joins on exact identities and accepted revisions; do not elect truth by majority vote.

Corrections preserve identity and expose that an earlier claim is no longer supported. Missing retained history becomes an explicit gap. An item disappearing from a snapshot does not imply completion or cancellation. Revocation removes protected content and invalidates the read grant; a cached page must not keep serving it as authorized current data.

Owner cursor semantics remain authoritative. Browser reconnection is transport only; `Last-Event-ID` is not an acceptance receipt. Reconnect must respect the source generation, grant and retention floor. If resumption is unsupported, reacquire a bounded snapshot with an explicit gap rather than replaying modifying commands.

## 9. Security and interaction

Public/remote clients consume existing authenticated, scoped resources. They never acquire a direct Runtime database handle, arbitrary path/host/endpoint, credential store or raw provider transcript access to render a page. Preserve privilege separation and closed resource/action families.

Reads must not start/resume a provider, drain controller notifications, acknowledge an obligation, claim work, renew a modifying lease or keep a worker alive. A thousand viewers must not create a thousand schedulers or provider event readers.

Untrusted titles, evidence summaries and model text render as text. No HTML interpretation or instruction execution. A suggested plan remains a suggestion until its owning authority admits it. Hidden reasoning, secrets, unrelated conversations and unrestricted tool payloads are never needed for the default product.

Provider-surface message entry, when separately qualified, is one semantic fill or clipboard paste under the host isolation contract, never character-by-character typing or Tab discovery. This reference workspace performs no provider interaction.

## 10. Delegation and compute policy

Retain Fable for cross-owner execution integration and supervision of its admitted implementation children. Route bounded normal engineering/research/review to the least-scarce capable eligible worker; use stronger independent review where the consequence warrants it. No extra Fable simply because the product is important.

Prefer Fable-owned worker lifetimes for work that outlives a Web turn, but require an actual supervisor/return mechanism before calling them monitored. If the existing mechanism cannot receive a result, retain a typed pending obligation; do not start a detached child merely to increase activity.

Concurrency is bounded by independent ready work, current eligible provider/host capacity, source custody, approved spend including review/repair, and integration capacity. A worker slot, account login or model label alone satisfies none of those gates. No unapproved paid overflow, recursive helper spawning or account rotation to evade limits.

Sol continues principal work while children execute and consumes useful returns without an artificial all-done barrier. Repeated no-delta activity triggers a changed approach, not more status prose. Neither a long turn nor high token consumption is a success metric.

The Chairman selected Pro for this responsibility. Exact served model/mode and an hour-long turn are not asserted as runtime attestation. Acceptance is the observable result, never elapsed time.

## 11. Release increments and distinct acceptance gates

| Increment | Independently useful outcome | Retained owner / completion proof |
|---|---|---|
| R1 Orientation | Real decision-first Today and exact Programs entry | Existing DF1/CCR; complete/partial/unknown and real browser task proof |
| R2 Mission workspace | One real mission, supported content, exact principal, relations and evidence | Product consumer + Fabric producers; authenticated real input, visible output, revocation/gap tests |
| R3 Accountable intervention | One exact message/decision with receipt and safe reconciliation | Existing action owner; duplicate/conflict/lost-response negatives and no blind resend |
| R4 Unattended delivery | Work -> review -> repair -> parent consumption while viewers are closed | Existing Fabric/COO/Dialogue/Wake; real workers and exact obligation consumption |
| R5 Whole-company breadth | Portfolio, tools, fleet, continuity and learning in the same product | Existing owners, per-lens useful tasks; no disconnected dashboards |
| R6 Remote and native adoption | Supported remote web and signed Mac client share qualified contracts | Existing auth/deployment/client owners; separate network/native proof |
| R7 Sustained acceptance | Representative multi-program, multi-provider, multi-host operation and recovery | Sol-led acceptance; interruption campaign, measured quality and Chairman usability |

These increments overlap only where interfaces and source custody permit. R1 and the R2 consumer contract can advance while admission repairs are held. Native packaging never substitutes for web usefulness. R2 is an intermediate delivery, not a narrowed definition of Mastermind OS.

## 12. Required acceptance campaign

Use actual relevant production inputs for final acceptance. Seeded failures are fault drills, not organic quality evidence.

A. Chairman orientation: complete zero, known subset, missing admission source, stale snapshot and correction are understood without opening raw machine diagnostics.
B. Mission: real objective/root/child/output/review are joined by exact refs; runtime and company authority stay distinct.
C. Conversation: partial/full/redacted/unavailable history, multiple viewers, revocation, reconnect gaps and corrected content do not disturb execution.
D. Intervention: correct office versus exact-session target; refused stale/wrong target; one applied request; lost response reconciles with no duplicate effect.
E. Autonomy: material BLOCKED/DECISION_REQUEST reaches the exact authorized parent; a lawful response is consumed by the same logical responsibility; viewers may be closed.
F. Recovery: compaction, principal loss, host interruption, exhausted capacity and a stale predecessor preserve responsibility and uncertainty. No effect-unknown failover.
G. Integration: a portfolio entry opens its actual work; an artifact/review/deployment opens the corresponding evidence; a missing join remains visible.
H. Resources: shared entitlement across lanes is counted once; unknown usage, expired offer, grant revocation and hold generation changes fail closed where required.
I. Accessibility: semantic navigation, known-target keyboard activation, focus restoration, no color-only state, readable contrast and useful equivalent list views.
J. Security: source text cannot execute; forbidden payloads are absent; reads are effect-free; identity/permissions are not inferred from labels.
K. Learning: measure accepted useful outcomes, repair burden, attention latency, orphan rate and cost per accepted result through existing owners; unknown baselines remain unknown.
L. Rollout: exact reviewed source, installed release, actual client path, production proof and product acceptance are all individually recorded.

Proposed service-level targets require baseline measurement before promotion. Do not invent availability percentages or present prototype timing as production performance.

## 13. Narrow implementation in this operation

Build only an offline reference of Today -> Program -> workspace -> typed connections/evidence, with explicit recorded and synthetic scenarios. It must contain no command/network/provider connection, no persistent company state, and no imported private conversation.

This reference is intended to test interaction/copy/negative-state design, not R1/R2 installed completion. Browser acceptance is currently blocked in the sandbox and has not been established. It is a reviewable companion to the current contract, not a replacement for the older recorded-workspace candidate or Figma. The latter sources remain subject to exact-owner recovery before implementation reuse.

Exact six-path ceiling:
- `docs/superpowers/specs/2026-09-16-mastermind-os-integrated-rollout-design.md`
- `docs/superpowers/plans/2026-09-16-mastermind-os-reference-workspace.md`
- `research/mastermind_os/README.md`
- `research/mastermind_os/reference_workspace.html`
- `research/mastermind_os/test_reference_workspace.py`
- `research/mastermind_os/reference_acceptance.json`

No shared runtime, auth, provider, CI guard, Control Room, DF1, native observer or Figma source is edited. No install, service start, provider call, credential, grant, Executive Job or release effect is authorized by this reference implementation.

## 14. Current dependency and source ledger

The following are observations at the stated reading epoch, not perpetual live state:

- Protected source: `8ba7deedde164c90298d3e88785d98e02fa5e2d2` (#650 transport-core merge). Source protection does not establish installed remote execution.
- #595: closed/unmerged at `5f247996103339d045072e3ffe2ea4271e4cd881`; integrated blueprint and historical Figma evidence preserved.
- #600: existing open Draft orchestration plan, `a5c4f0f4c9561874ded59abf9a9466fe33734538`; incumbent execution root retained.
- Protected #612: Web CEO/operational autonomy division and Live Fabric contract; no UI/lifecycle authority transfer.
- #677 H3e, #653, #688 and packet03/04/05/06/09 each have current owners and holds on the Fabric carrier. This operation does not redo their investigations or repair their code.
- DF1: own `chairman-control-room-df1-implementation-20260907-sol-001` carrier `C0BSBM78V1N/1788790898.605029`; current posted reducer/browser progress is attributed evidence, not this operation's test result.
- Macro #7120: existing pending CCR continuity carrier, head `cc3c393cd149913ab58d1f406d2fa78a36001718` at the read epoch; do not create a competing CCR handoff writer.
- Macro #7181: existing Fabric continuity writer; do not overwrite it with this product contract.
- Prior supplementary native source-census batch was platform-blocked before execution. It was not retried through another host or tool; no findings are inferred from it.

## 15. Durable continuation and exact next action

Publish this reference and contract on one Draft/HOLD carrier. Cross-reference through the existing #595 product history, #600 execution integration and Macro #7120 continuity carriers; comments are intake evidence, not merged organizational truth or target consumption.

Next accepted implementation boundary: obtain the incumbent producer owner's exact mission-observation and authorized visible-content/history contract, source revision and custody; pair it with the current DF1/CCR consumer and the retained recorded-workspace candidate. Then freeze ONE authenticated real mission-workspace vertical, using the existing compositor and action owners. Do not create a universal schema or wait for unrelated provider breadth.

At every continuation preserve accepted/held source identities, actual active children, unresolved effects and next action through their existing owners. New source work needs current collision proof and one accountable writer. Completed substrate and terminal children remain do-not-redo.

## Sources

Internal source links are pinned or explicitly mutable evidence:
- https://github.com/mastermindx-market-intelligence/Mastermind/tree/8ba7deedde164c90298d3e88785d98e02fa5e2d2
- https://github.com/mastermindx-market-intelligence/Mastermind/pull/595
- https://github.com/mastermindx-market-intelligence/Mastermind/blob/5f247996103339d045072e3ffe2ea4271e4cd881/docs/superpowers/specs/2026-09-13-mastermind-os-integration-blueprint.md
- https://github.com/mastermindx-market-intelligence/Mastermind/pull/600
- https://github.com/mastermindx-market-intelligence/Mastermind/blob/8ba7deedde164c90298d3e88785d98e02fa5e2d2/docs/EXECUTIVE_WEB_CEO_AUTONOMOUS_DELIVERY_AMENDMENT.md
- https://github.com/mastermindx-market-intelligence/Mastermind/blob/8ba7deedde164c90298d3e88785d98e02fa5e2d2/docs/superpowers/specs/2026-09-07-chairman-control-room-decision-first-experience-design.md
- https://mastermindxgroup.slack.com/archives/C0BSBM78V1N/p1789552938737199?thread_ts=1789324397.992989&cid=C0BSBM78V1N
- https://mastermindxgroup.slack.com/archives/C0BSBM78V1N/p1789553710002729?thread_ts=1788790898.605029&cid=C0BSBM78V1N
- https://github.com/mastermindx-market-intelligence/macro/pull/7120
- https://github.com/mastermindx-market-intelligence/macro/pull/7181

External primary standards checked 2026-09-16:
- https://www.w3.org/TR/WCAG22/ — keyboard, focus, status, reflow and non-color-only accessibility requirements; no conformance claim from this record.
- https://html.spec.whatwg.org/multipage/server-sent-events.html — browser reconnect and Last-Event-ID semantics; these are not company acceptance semantics.
- https://reactflow.dev/learn/advanced-use/accessibility — later graph implementation candidate; built-in support still requires application-specific keyboard, focus and labeling work. No library dependency is added by this operation.

## 16. R2 contract review and reference repair — current continuation

The current protected source/procedure observation is `e8803ba3d3ee928d150d7dcac1a1e2bad2dc0d48`. Required Skillpack, routing and dialogue sources were reloaded at that same commit and are byte-identical to the previously read procedure. Source movement is not installed-runtime proof.

The incumbent R2 freeze #704 at `8b94141106200f0c48b6d033cca19a8007ef0031` returned useful producer archaeology but is not accepted. Exact-head review `5228415542` requires seven repairs. The pure producer counterexamples are bound to `fabric_job_view.py` blob `c69e44785e28ee191639b7af05d0fe5e22fbf24d`, evidence digest `777d203f8e7348a882b4e93e80076ce45986b98863f2379e81e873b8852712af`: Job COMPLETED is aliased to result ACCEPTED without independent review, also remains ACCEPTED with a missing payload, and an old root can coexist with submit-arm false. These are synthetic-object tests of the actual pure producer, not production mutations.

Consequently, R2 must keep execution, transport/consumption and exact-artifact product acceptance separate. Missing acceptance-owner data remains NOT_PROJECTED. DISARM does not erase already-admitted work. The strict new `(work_ref, root_job_id)` query is allowed only on the new mission routes; existing DF1 routes and all exact-pair, scope, generation, size and encoding checks remain unchanged. The local mission tree is a partial R2A step, not a redefinition of authenticated-user or full R2 acceptance. The approved bounded authorized hot-window reader stays in the first live journey; full available authorized history remains a later existing-owner integration.

The review also requires real source-pin correction, bounded/correction-safe acquisition and redaction, and one actual entry from the existing Program/autonomy drawer. A copied label map, raw source path, hand-constructed URL or per-viewer full-table scan cannot substitute for the approved experience/authority contract. #704's incumbent native writer retains its one-file source custody; Claude8 confirmed relay at `1789593485.305139`. No repaired-head acceptance or new implementation START is established here.

Within #702, the bounded reference now has a shared pure six-node/seven-edge model, SVG connection paths, an equivalent list, and explicit focus recovery changes. Fifteen source-only checks and three discriminating mutation kills qualify that limited source behavior; the extra two regressions close the independent-review environment-epoch blocker and centralize all application focus calls through the tested connected-target guard. The thirty-nine real-browser cases remain unverified; no policy change, cross-host navigation retry, new dependency, Figma modification, live provider action or installation occurred. The reference remains BUILT_NOT_PROVEN and cannot close R2.
