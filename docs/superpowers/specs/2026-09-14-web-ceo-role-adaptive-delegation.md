# Web CEO Role-Adaptive Delegation — Architecture and Pilot

Date: 2026-09-14. Operation: `web-ceo-role-adaptive-delegation-20260914-sol-001`. Existing parent: `WS:EXECUTIVE-CAPACITY-FABRIC`. Original design basis: Mastermind `bffe2ca8506346ea278c8ee469bf1ac30a4de008` with compatible Skillpack 1.0.1/bootstrap 1. The current protected integration base is intentionally not frozen in this architecture source; re-pin it from protected Git and the exact PR/evidence receipt before any modifying or enrollment action. Status at this enrollment slice: **PROCEDURE ENROLLMENT CANDIDATE / SOURCE-ONLY / PRODUCTION INERT**. Operation `web-ceo-delegation-enrollment-20260915-sol-001` registers this companion in the same-pinned INDEX; the enrollment becomes canonical only when that exact source candidate is protected.

Repository-placement disclosure: the current fresh-Sol evaluator materializes every `docs/sol_skills/*.md`, so protected source places these bytes in a fresh-Sol evaluation bundle pinned at or after that merge. That automatic placement remains evaluation exposure, not production enrollment, runtime authority, live delegation, or served-model proof. Procedural enrollment is separate and exists only through the same-pinned INDEX candidate in this operation.

## Outcome and current Chairman decision
Chris approved principal-first, worker-assisted Web CEO execution and directed implementation, with a correction: his unverified experience is that Astra Pro tends to concentrate high-level thought in roughly 30–60-minute runs, while Sol Pro sometimes sustains useful COO-level orchestration for 100–200 minutes. These are user-reported observations, not measured limits, guarantees, benchmarks, service-level agreements, or minimum-duration instructions. The outcome is more accepted capability per paid/included resource envelope, not more elapsed time or agent activity.

## Decision: asymmetric defaults, reversible by evidence
Prefer Astra for concentrated architectural decisions, ambiguity resolution, high-value synthesis and adversarial judgment; when the same turn independently qualifies for Pro mode under controlling law, prefer Astra Pro. Prefer Sol for sustained, decision-bearing orchestration: maintaining the ready work frontier, adjudicating returns, resolving integration conflicts, routing bounded repairs and advancing the next authorized dependency; when that turn independently qualifies, prefer Sol Pro. Both may do either job when current capability and evidence favor it. Neither name grants a role, lease, model attestation, provider account, authority, or reasoning-mode admission. Included non-Pro reasoning remains the default. A Pro request must carry the complete exception receipt, cite one allowed `PRO_MODE_TASK_CLASS`, satisfy the governing duration and stop-condition rules, and classify the actual turn as eligible; missing or ineligible receipts fail closed as `PRO_MODE_REFUSED / USE_NON_PRO_MODE`. Routine lifecycle, status, routing, polling, mechanical edit, test and simple-review turns do not inherit Pro eligibility from a larger program. Do not fake duration or force Astra -> Sol -> workers on every project, transfer a productive incumbent solely because of its model label, or require Astra permission for each ordinary Sol decision.

## One organization and one execution system
The action-authoritative principal is the existing responsibility/RuntimeBinding owner. A Sol session performing COO-level work acts within that responsibility and delegation envelope; it is not a new COO service or a replacement for the incumbent Fable integration principal. Executive OS retains Jobs/Attempts/Workers/Events and admission. Existing COO control owns admitted operational progression; Model Router fixes the first lawful suitability tier, Capacity Fabric ranks eligible candidates only inside that tier, and Executive OS atomically claims the worker; provider control owns quota and credential status; Dialogue/Wake owns exact continuation; GitHub owns source/evidence; Macro Agent OS owns organizational continuity. No new router, scheduler, queue, agent tree, budget ledger, profile database or transcript store.

## Hard boundaries and flexible execution
Delegate independently executable investigation, implementation, validation and review when handoff plus risk-adjusted review and expected repair cost less principal effort, or when concurrency materially shortens useful delivery. Retain necessary principal judgment, tightly coupled ambiguity, unique approved access and small critical-path interventions. Write a brief exception reason for substantial retained worker-suitable work. Reassess at a change of task character or repeated mechanical loop. Delegate complete bounded outcomes, not individual commands. Workers may complete the approved investigate/build/check/repair cycle but may not expand scope, spend, provider eligibility or authority. An admitted Sol orchestration mission can contain routine lifecycle steps; a standalone status/ACK/polling turn remains routine and is not promoted merely by program prestige.

## Completion, continuity and economics
This companion is enrolled by the same pinned INDEX candidate as `ACTIVE_EXECUTION.md`. ACTIVE_EXECUTION remains the sole active-turn, no-delta and finalization owner; this companion adds detailed delegation procedure only, while the current accepted COO cycle/control-service owners remain controlling. An accepted subtask is not completion of the approved program. While useful unblocked work remains, advance it; do not poll or duplicate a worker's assignment to appear busy. A repeated failure requires a new hypothesis/evidence or a bounded repair/escalation decision, not hundreds of identical reruns. Only existing runtime authority may continue admitted work outside a Web turn. Preserve one exact operation/carrier; EFFECT_UNKNOWN blocks its retry or reassignment, not provably independent safe work. Preserve exact parent/successor fencing and never claim a particular browser session will survive.

## Integration seams and source ownership
#506 owns the pending duration-floor correction in ModelRouter and the three governing routing documents. Consume that same candidate or its accepted successor; do not fork its change. Until then, the protected 80–1440-minute closed-class Pro gate remains controlling; any later 1–1440-minute field is a planning-estimate compatibility constraint, never a generation-time target. Protected #591 supplies ACTIVE_EXECUTION, INDEX law 23 and the single no-delta/finalization owner. This bounded enrollment operation extends that same INDEX boundary after current-base review; it does not reopen or duplicate #591. #630 is merged at `1ee9b8b3` and owns the heterogeneous per-WORK-step placement core; re-read its protected merged text at enrollment rather than relying on an earlier handoff summary. #618's Astra External Fabric Delegation design and #612/#629 own the existing submission/offline operational path. #587–#590 own complete worker briefs/craft; #594 and Macro provider control own economics. Preserve all current source custody. The original #632 source package changed none of those active files or live settings. This enrollment slice changes only the same-pinned INDEX/companion records and static contract; it changes no runtime, router, provider, credential or live setting.

## Readiness and pilot boundary
Workstation connector exposure is session-scoped and must be re-censused from the effective schema before use. A successful Studio Direct or Remote Desktop Commander filesystem/process call proves only that connector's current host/workspace path; absence from one effective schema does not prove the connector read-only or globally unavailable. Neither condition proves authenticated Executive submission, provider admission, parent consumption, or live Web-to-worker completion. Protected subscription bindings, reviewed harness admission and actual native kit execution must still be reconciled separately; none proves the others. MiniMax evidence and holds are route-scoped: truthful catalog or kit-path proof does not establish a reviewed Executive realm, binding, admission, or autonomous route, while an Executive hold must not erase narrower proven kit evidence. Reconcile the exact model/harness/realm/host route with its incumbent owner before use; this source operation reads no credential and performs no provider call. Prefer a currently qualified economical GLM or other eligible lane within an existing approved budget, not a hard-coded provider override.

## Observable pilot and falsifier
First useful pilot: use an existing eligible review lane to obtain an exact-artifact review of the duration/continuation dependency or this instruction package, return bounded evidence, and have the lawful principal consume it and issue the next permitted edge. Reuse an already-assigned reviewer rather than duplicate it. The complete Web-to-fabric claim requires real authenticated admission, actual worker START/result, independent verification where required, and actual parent consumption. A Slack intake, fixture result or native shell run alone cannot pass it. The supplied fourteen ScenarioPackets test procedural choices only; the native fresh-Sol harness pins its own model/surface and cannot prove Web Pro behavior or the Sol/Astra duration hypothesis.

## Evaluation and release decision
Use matched task classes and predeclared acceptance criteria; record actual served model/mode, tool/harness generation, task difficulty, accepted artifacts, principal routine interactions, worker/review spend, repair count, human interventions and observed stop reason. Separate useful reasoning/work from waiting, external gating and observation truncation. Unknown identity/usage remains unknown; never reconstruct hidden Pro compute. Compare Astra-led and Sol-led samples without mandatory extra principals, hidden transcripts or duplicate modification. Initial samples are feasibility evidence, not a performance conclusion. Promote only if quality is preserved and accepted progress/principal burden improves; otherwise adjust role preference or granularity. Production rollout additionally requires protection of this exact enrollment candidate, current source review and fresh Web-session proof.
