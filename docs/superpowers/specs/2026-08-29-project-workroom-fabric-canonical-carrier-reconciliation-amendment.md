# Project Workroom Fabric — Canonical Carrier and Planner-Source Reconciliation Amendment

**Date:** 2026-08-29  
**Owner:** Sol, AI CEO  
**Chairman:** Chris  
**Status:** `SPEC_ONLY / CHAIRMAN-AUTHORIZED / RECORDS_ONLY`  
**Canonical operation:** `mastermind-project-workroom-fabric-20260829-sol-001`  
**Canonical carrier:** Mastermind PR #240 / `sol/project-workroom-fabric-20260829`  
**Superseded operation after this amendment lands:** `project-workroom-convergence-20260829-sol-001`  
**Superseded carrier:** Mastermind PR #233; closed-unmerged PR #232 remains metadata history only  
**Current protected procedure/source:** `mastermindx-market-intelligence/Mastermind@7b8f3ca580f9872ca8ddf60f90c6022ce4a18e6b`  
**Skillpack:** `mastermind.sol_skillpack.v1` v1.0.1 / bootstrap-major 1  
**Linear:** MAS-231 / MAS-236 are the selected current program projections; MAS-220 becomes duplicate/superseded projection  
**Organizational parent:** existing `WS:CHAIRMAN-CONTROL-ROOM`

This amendment resolves the duplicate Project Workroom architecture families discovered during current-source recovery. For carrier identity, current protected-source interpretation, WR-P0 input/provenance law, Initiative comparison, shadow/apply semantics, action emission and supersession, this amendment has narrow precedence over:

- `docs/superpowers/specs/2026-08-29-project-workroom-fabric-design.md`;
- `docs/superpowers/specs/2026-08-29-project-workroom-fabric-wr-r0-amendment.md`;
- `docs/superpowers/plans/2026-08-29-project-workroom-fabric-rollout.md`;
- the five records on Mastermind PR #233.

All other accepted Product Workroom outcome, canonical ownership, public-internal V1, least-privilege Slack principal split, exact identity, effect-unknown, no-rebuild, dialogue, Steward, rollout and production-proof laws remain unchanged.

This records amendment creates no Slack channel, app, Canvas, List, Workflow, Linear Initiative/Project/Issue mutation, Agent OS mutation, Executive Job/Attempt/Worker, RuntimeBinding, credential, host effect, service, worker assignment or production capability.

---

## 1. CEO canonical-carrier ruling

The current Project Workroom program has one canonical architecture and rollout carrier:

```text
operation = mastermind-project-workroom-fabric-20260829-sol-001
PR        = Mastermind #240
branch    = sol/project-workroom-fabric-20260829
Linear    = MAS-231 / MAS-236
```

The older overlapping family becomes superseded history after this exact amendment is present on #240:

```text
operation = project-workroom-convergence-20260829-sol-001
PR        = Mastermind #233
Linear    = MAS-220
```

Closed-unmerged #232 remains draft-metadata history only.

This ruling is not based on newest timestamp. #240 is selected because it incorporates the stronger current-platform and adversarially reviewed V1:

- public-internal projector-created Workrooms first;
- private Workrooms deferred;
- no implicit channel adoption or `channels:join`;
- static channel-read-only Home Canvas, with standalone-plus-bookmark fallback;
- Control Room as dynamic current truth;
- optional app-owned, channel-read-only Radar List with `todo_mode=false`;
- no undocumented native tab, bookmark-folder or channel-template dependency;
- optional/deferred Workflows;
- dedicated Workroom Projector separate from Agent Relay;
- narrow public-channel/Canvas/bookmark/optional-List scope boundary;
- exact project/workroom/operation/runtime/proof separation.

#233 also contained valid load-bearing planner and source-provenance rulings. Those are adopted explicitly below before #233 is closed. No valid unique ruling is discarded by recency.

After the supersession receipt:

- do not reopen #232 or #233;
- do not reuse `project-workroom-convergence-20260829-sol-001`;
- do not implement from #233 file paths or private-by-default assumptions;
- do not create another Workroom architecture carrier;
- use #233 only as historical evidence for the clauses explicitly adopted here.

---

## 2. Current protected-source reconciliation

PR #240 was last reviewed against protected `dfd69451dce5e186ce05f65446023fbe21f07a58`. Protected Mastermind subsequently advanced by one commit to:

```text
7b8f3ca580f9872ca8ddf60f90c6022ce4a18e6b
```

through merged Agent Relay enrollment release PR #238.

The protected movement adds exactly:

```text
ops/executive_os/a2_agent_relay_enrollment.py
tests/test_a2_agent_relay_enrollment.py
```

It is path-disjoint from #240's records files. It is semantically relevant to the Workroom dependency graph:

```text
Agent Relay enrollment source implementation = PROTECTED / BUILT_NOT_PROVEN
credential enrollment / service activation    = NOT PROVEN LIVE
single-channel real dialogue canary            = NOT PROVEN LIVE
multi-workroom Relay                           = NOT BUILT
```

Therefore:

- remove stale statements that describe enrollment source as still awaiting merge;
- do not describe #238's merge as an installed, credentialed, armed or production-proven Relay;
- WR-D0/WR-D1 remain gated on the real Agent Relay qualification, activation and exact canary laws;
- WR-F0/WR-R0/WR-P0 records and pure planning may continue independently when their own gates pass.

The #240 branch must preserve history and reconcile to protected `7b8f3ca...`; no rebase, reset, force-push or replacement source branch is authorized.

---

## 3. Capability-truth correction

Fresh GitHub default-branch and open-carrier census establishes:

```text
Workroom architecture / rollout source            SPEC_ONLY
WR-R0 platform/API/current-estate research         RESEARCH / RECORDS_ONLY
WR-P0 pure desired-state compiler                  NOT BUILT
WR-A0 Workroom Projector client                    NOT BUILT
WR-A1 Workroom Projector app/credential boundary  NOT BUILT
WR-C0 live inert Slack canary                      NOT BUILT
WR-SURF1 managed Home/Radar actuator               NOT BUILT
WR-D0 multi-workroom Relay                         NOT BUILT
WR-D1 correct-workroom parent ensure               NOT BUILT
WR-L0 Linear Project/Issue/workroom join            NOT BUILT
WR-STEW Workroom Steward projection                NOT BUILT
real Project Workroom pilot                        NOT BUILT
small fleet / production cutover                   NOT BUILT
```

Protected or open non-Workroom dependencies keep their independently recorded state. In particular, Agent Relay enrollment source being protected does not advance any Workroom capability above.

Local scratch files, generated status JSON, unpushed branches, chat summaries and machine-local release receipts are advisory only. They cannot advance GitHub implementation truth. Linear MAS-236 and any other projection must not call WR-P0A, A0, A1, C0 or later implementation built unless an exact repository carrier and accepted immutable evidence exist.

No Workroom implementation START is authorized by this correction.

---

## 4. Exact WR-P0 canonical inputs

WR-P0 is a pure deterministic compiler. It does not reparse Agent OS and does not infer canonical eligibility from Linear or Slack.

The exact input family is:

```text
mastermind.project_workroom_policy.v1
linear_portfolio_plan.v1
linear_initiative_plan.v1
linear_initiative_snapshot.v1
mastermind.project_workroom_slack_snapshot.v1
```

Interpretation:

- `linear_portfolio_plan.v1` is the exclusive deterministic Agent OS → Linear Project desired-state owner;
- `linear_initiative_plan.v1` is the accepted strategic Initiative desired-state/membership owner;
- `linear_initiative_snapshot.v1` is normalized observed live Initiative/Project membership readback;
- the Slack snapshot is observed remote state only;
- the Workroom policy selects stable presentation strategy only.

WR-P0 must not:

- parse `agentos/workstreams/**` directly;
- duplicate workstream status selection;
- create or update an Initiative;
- decide Project lifecycle;
- treat Linear `In Progress` as canonical eligibility;
- infer membership from Initiative or Project names;
- use Slack channel names as identity;
- call a network;
- write Slack or Linear;
- store live IDs in a new registry.

The controlling compiler interface is:

```python
def build_workroom_plan(
    *,
    project_plan: Mapping[str, object],
    initiative_plan: Mapping[str, object],
    initiative_snapshot: Mapping[str, object],
    slack_snapshot: Mapping[str, object],
    policy: Mapping[str, object],
) -> dict[str, object]: ...
```

The old `agent_state` parameter in the parent rollout plan is superseded. Canonical workstream state enters only through `linear_portfolio_plan.v1`.

The emitted plan and receipt must carry and verify:

```text
project_plan_semantic_hash
initiative_plan_semantic_hash
initiative_snapshot_observation_hash
slack_snapshot_observation_hash
policy_semantic_hash
workroom_plan_semantic_hash
```

Changed semantic input must change the plan digest. Input ordering alone must not.

If the Initiative compiler is not accepted/protected or the normalized live snapshot is unavailable, WR-P0 may emit a typed evidence hold but may not manufacture Initiative readiness.

---

## 5. Exact Workroom policy and Initiative comparison

The policy schema remains:

```text
mastermind.project_workroom_policy.v1
```

Top-level keys are exactly:

```text
schema
source_records
workspace_id
rollout_mode
workrooms
```

`source_records` is defined in §6.

Each `workrooms[]` row contains only stable presentation strategy:

```text
responsibility_ref
initiative_key
initiative_name
promotion_tier
visibility_policy
preferred_channel_slug
surface_set
retention_policy
rollout_mode
```

It must refuse runtime, worker, provider, session, attention, retry, completion, Slack channel ID, Linear Project ID, Initiative ID, Job ID, Attempt ID, branch, PR or mutable status fields.

### 5.1 Exact initial shadow corpus

The first policy corpus contains exactly these six shadow-only candidates:

| responsibility_ref | preferred_channel_slug | initiative_key | initiative_name |
|---|---|---|---|
| `WS:CHAIRMAN-CONTROL-ROOM` | `chairman-control-room` | `autonomous-ai-organization` | `Autonomous AI Organization` |
| `WS:AGENT-OS` | `agent-os` | `autonomous-ai-organization` | `Autonomous AI Organization` |
| `WS:RATES-INFLATION-COMMAND` | `rates-inflation` | `global-markets-regimes-risk-command` | `Global Markets, Regimes & Risk Command` |
| `WS:BIOCATALYST-CORE-PRODUCT` | `biocatalyst` | `institutional-company-event-intelligence` | `Institutional Company & Event Intelligence` |
| `WS:FINANCIAL-INTELLIGENCE-FABRIC` | `financial-intelligence` | `institutional-company-event-intelligence` | `Institutional Company & Event Intelligence` |
| `WS:STOCK-IDENTITY` | `stock-identity` | `canonical-intelligence-substrate-learning` | `Canonical Intelligence Substrate & Learning` |

For every row:

```text
visibility_policy = PUBLIC_INTERNAL
rollout_mode       = SHADOW
```

The exact core surface set is:

```text
CHANNEL
HOME_CANVAS_STATIC
LINEAR_PROJECT_BOOKMARK
CONTROL_ROOM_BOOKMARK
```

The optional surface is:

```text
RADAR_LIST
```

Working Notes and Workflow intake are not core WR-P0 promises. They remain later optional surfaces after exact platform and audience proof.

These six rows are calibration candidates, not channel-creation authority. A parked, excluded, unresolved or warning-held workstream remains a negative-control row and emits no live create/update action.

### 5.2 Exact Initiative law

Policy `initiative_key` and `initiative_name` are expected stable strategic identities. Live Initiative IDs remain snapshot facts and never enter static policy.

The planner compares each selected row against the accepted Initiative plan and normalized live snapshot:

- exact key/name/membership agreement → Initiative check passes;
- Initiative objects absent → `initiative_rollout_pending`;
- expected Initiative missing → `initiative_key_missing`;
- more than one match → `initiative_key_ambiguous`;
- name differs → `initiative_name_mismatch`;
- Project membership absent → `initiative_membership_missing`;
- Project belongs to multiple Initiatives → `initiative_membership_ambiguous`;
- wrong Initiative → `initiative_membership_wrong`;
- accepted Initiative plan/snapshot digest mismatch → hard refusal.

The Workroom planner never repairs those states.

### 5.3 Strategy refusal codes

Required hard policy failures include:

```text
policy_wrong_schema
policy_unknown_key
policy_runtime_field_refused
policy_duplicate_responsibility_ref
policy_duplicate_workroom_ref
policy_duplicate_channel_slug
policy_missing_initiative_identity
policy_unknown_initiative_key
policy_initiative_name_mismatch
policy_private_visibility_refused_v1
policy_non_shadow_initial_corpus_refused
```

---

## 6. Atomic source-provenance law

A single design path is insufficient. The Workroom policy must carry one immutable atomic source set:

```text
source_records:
  repository
  protected_revision
  paths
```

The repository is exactly:

```text
mastermindx-market-intelligence/Mastermind
```

The protected revision must be the exact 40-lowercase-hex protected merge commit that contains the canonical #240 source set. Until #240 has a protected merge commit, WR-P0 implementation START remains held.

The exact sorted, duplicate-free source paths are:

```text
docs/superpowers/specs/2026-08-29-project-workroom-fabric-canonical-carrier-reconciliation-amendment.md
docs/superpowers/specs/2026-08-29-project-workroom-fabric-design.md
docs/superpowers/specs/2026-08-29-project-workroom-fabric-wr-r0-amendment.md
```

The rollout plan remains the execution DAG and is not included as source-law authority in policy.

Required failures:

```text
policy_source_records_invalid
policy_source_repository_mismatch
policy_source_revision_invalid
policy_source_paths_mismatch
policy_source_paths_duplicate
```

The compiler must not discover source law by directory scan, filename similarity, newest timestamp or branch name.

---

## 7. Portfolio eligibility and warning law

For each policy `responsibility_ref`, WR-P0 resolves it exactly in `linear_portfolio_plan.v1`.

### 7.1 Active Project

Exactly one row in `active_projects` may be planning-eligible, subject to exact Project, Initiative, Slack and warning checks.

Normal Workroom actions require an exact immutable observed Linear Project ID. A missing or ambiguous Project ID emits no Workroom create/update action.

### 7.2 Review candidate

Exactly one row in `review_candidates` yields:

```text
portfolio_workstream_requires_review
```

and zero create/update actions.

### 7.3 Excluded Project

Exactly one row in `excluded_projects` yields:

```text
portfolio_workstream_ineligible
```

and zero channel, Canvas, Radar or bookmark create/update actions.

If an exact already-bound Workroom exists, WR-P0 may emit only:

```text
would_archive_after_acceptance
```

as a non-mutating review candidate. It may not archive or reactivate.

### 7.4 Missing or ambiguous identity

Missing from all Project-plan collections yields:

```text
portfolio_workstream_missing
```

Presence more than once or across collections yields:

```text
portfolio_workstream_ambiguous
```

Both emit zero actions.

### 7.5 Target-specific warnings

Project-plan warnings naming the exact target are projected into the row and hold live action:

```text
existing_project_binding_missing
existing_project_binding_ambiguous
project_name_drift
project_status_drift
project_lifecycle_drift
generated_state_disagrees_with_direct_record
typed_gate_source_missing
```

Unrelated warnings do not contaminate another workstream.

A refused/malformed Project plan is a hard WR-P0 failure.

---

## 8. Planning eligibility is not apply eligibility

WR-P0 must separate:

```text
planning_eligible
apply_eligible
```

A row may be planning-eligible when exact canonical and observed checks pass.

A row is apply-eligible only when all planning gates pass and:

```text
rollout_mode in {CANARY, ACTIVE}
```

`SHADOW` is never apply authority.

Every initial row emits:

```text
shadow_mode_no_apply
apply_eligible = false
```

even when all observations are otherwise clean.

The summary contains distinct counts:

```text
eligible_workroom_count
apply_eligible_workroom_count
shadow_workroom_count
held_workroom_count
```

`eligible_workroom_count` must never be interpreted as safe to mutate Slack.

---

## 9. Closed action-emission semantics

The deterministic action vocabulary is:

```text
would_create_channel
would_rename_channel
would_update_managed_purpose
would_create_home_canvas
would_update_managed_canvas_block
would_create_project_radar
would_update_managed_radar_rows
would_add_linear_bookmark
would_add_control_room_bookmark
would_archive_after_acceptance
noop
```

Deterministic precedence is the order above unless implementation freezes another explicit tested order.

Rules:

- review, excluded, missing, ambiguous or unbound Project rows emit zero normal create/update actions;
- a missing safe URL omits that bookmark action and records a typed failure;
- never emit `url = null`, empty URL, guessed URL or unreviewed scheme;
- channel-name drift on an exact immutable channel ID emits `would_rename_channel`;
- channel-purpose drift remains a separate `would_update_managed_purpose`;
- a manual rename never changes Workroom identity;
- pure WR-P0 may report observed drift but may not claim a between-read-and-write race;
- `remote_changed` / `manual_remote_change` belongs to WR-A0/WR-C0 immediate pre-write reread;
- incomplete Slack public-channel census cannot prove absence and cannot emit an authoritative create action;
- duplicate or malformed managed markers fail closed;
- policy and snapshots remain zero-network inputs.

Required typed holds include:

```text
linear_project_missing
linear_project_ambiguous
initiative_rollout_pending
initiative_membership_missing
initiative_membership_ambiguous
initiative_membership_wrong
slack_snapshot_incomplete
duplicate_workroom
workroom_marker_invalid
control_room_resource_missing
surface_capability_unavailable
shadow_mode_no_apply
```

---

## 10. Required WR-P0 discriminating corpus

WR-P0 must use RED-first tests proving at least:

```text
literal workroom_ref vectors and no silent normalization
non-WS identity refusal
complete atomic source_records at one protected merge
old/single-path source key refusal
missing/extra/duplicate source path refusal
exact six policy rows and Initiative mappings
runtime/provider/session/completion policy field refusal
linear_portfolio_plan.v1 is the only workstream eligibility input
wrong Project-plan schema or hash refusal
same workstream across Project-plan collections refusal
active Project positive planning path
review candidate emits zero create/update actions
parked/done/killed emits zero create/update actions
missing/ambiguous Linear Project emits zero unbound Workroom actions
target-specific warning holds only its target
Linear In Progress cannot override excluded canonical state
accepted Initiative plan/snapshot digest agreement
missing/ambiguous/wrong Initiative membership behavior
shadow row can be planning-eligible but never apply-eligible
CANARY or ACTIVE is required before apply eligibility
eligible/apply-eligible/shadow/held counts are distinct
zero/one/multiple/malformed Workroom marker behavior
incomplete Slack census refuses authoritative absence/create
renamed channel with same ID/marker remains bound
name drift emits would_rename_channel
purpose drift is a separate action
missing Control Room URL emits no null bookmark
manual_remote_change is absent from pure WR-P0 semantics
input row order does not change byte output or digest
network/socket monkeypatch proves zero network
```

The exact current-checkout suite must be wired through an existing Macro CI owner. Do not create a second Workroom CI plane.

Acceptance remains:

```text
BUILT_NOT_PROVEN / PRODUCTION_INERT
```

No Slack or Linear mutation exists from WR-P0.

---

## 11. Superseded #233 clauses and artifacts

The following #233 clauses are explicitly not adopted:

- private Workrooms by default;
- the `proj-` prefix as source law;
- Mastermind `control_plane/project_workroom_plan.py` as the WR-P0 implementation owner;
- a separate Workroom strategy schema where #240's policy schema now governs;
- stale current-estate claims such as zero Initiatives as durable truth;
- stale statements that Agent Relay enrollment source is unmerged;
- the old operation key and carrier as a parallel active program;
- any old file census or protected source pin as current.

The following #233 concepts remain adopted through this amendment:

- exact Initiative key/name comparison;
- exclusive reuse of `linear_portfolio_plan.v1`;
- atomic multi-record source provenance;
- planning/apply separation;
- zero actions for review/excluded/missing/ambiguous identity;
- explicit rename action;
- omission of unsafe/null URL actions;
- actuator-only remote-change detection;
- deterministic action order and discriminating tests.

After #240 contains this amendment, #233 is closed unmerged with an explicit supersession receipt. Its branch and commits remain historical evidence and are not deleted or force-modified.

---

## 12. Updated carrier and dependency ledger

### Canonical Workroom carriers

```text
#240  canonical architecture / rollout / reconciliation source
#242  stacked WR-R0 platform research; remains separate and must reconcile after #240
#6661 Agent OS decision/handoff projection; requires current-source/fact correction before release
```

### Historical/superseded

```text
#232  closed-unmerged draft metadata history
#233  close unmerged as superseded after this amendment
MAS-220 mark Duplicate / superseded by MAS-231
```

### External prerequisites

```text
Macro #6662  canonical main-red repair; must reach accepted terminal green
Macro #6658  Initiative desired-state/drift compiler; same-carrier blockers must close
Initiative live apply/readback  owned by the Initiative session
MAS-64/MAS-66  dedicated Linear Projector actor/apply
MAS-189  selected Issue/comment/update projection
Agent Relay activation/canary  existing owner; #238 source merge alone is insufficient
OCR-6 / Steward  existing exclusive compositor
```

### Dependency-ordered continuation

```text
1. protect current #240 source after exact-current-base CI/review
2. retarget/reconcile #242 to protected master and accept its exact research
3. reconcile/release Macro #6661 Agent OS records
4. accept #6662 and #6658, then consume the Initiative post-apply readback
5. commission WR-P0 on one fresh Macro carrier
6. accept WR-P0 as production-inert
7. build/accept WR-A0 and WR-A1 under their separate security gates
8. run one exact inert public-channel canary
9. add Home/Radar and exact Workroom dialogue/Linear/Steward joins
10. run three-Project, adversarial multi-operator and small-fleet proof
```

No step authorizes its successor automatically.

---

## 13. Completion boundary

This amendment makes one Workroom source-law family recoverable and removes a duplicate architecture carrier.

After it lands on #240:

```text
canonical architecture carrier = known
unique planner/source laws      = preserved
duplicate carrier               = eligible for explicit closure
implementation capability       = unchanged
production capability           = unchanged
```

It does not make #240 protected, #242 accepted, WR-P0 built, a Slack app installed, a channel created, Initiative rollout complete, Agent Relay live, or the Chairman's Workroom experience production-ready.

The Project Workroom program remains incomplete until the real user journey is proven through exact Linear Project, Slack Workroom, selected operation thread, Executive runtime, GitHub proof and Steward/Control Room composition with zero duplicate effect, silent orphan, wrong-thread continuation or false completion.
