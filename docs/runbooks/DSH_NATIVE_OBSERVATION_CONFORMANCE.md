# DSH N0 native-observation conformance

## Mission and state

Operation: `harness-convergence-n0-native-observation-20260917-sol-001`.
Parent research: Mastermind #687, design commit
`64c024e4f675296c189fb482a6ec70547877d9c2`, plan
`docs/superpowers/plans/2026-09-17-dsh-native-observation-conformance.md`.
Source-intent receipt: #687 comment `5711628157`.
Protected implementation/procedure base:
`55a54fdaecef9cbf434492c3cce3b369dbc69b9b`, Skillpack 1.0.1/bootstrap 1.

The current Chairman recovery/continuation assigns Sol this bounded harness
source candidate. #600/#660/HF1/OHF/capability and Agent Evaluation retain their
existing ownership. No shared-owner adoption, worker START, or independent
review is asserted. Direct work uses the exposed attended source tools, not a
raw provider-spawn fallback or a second execution queue.

Before this candidate, the N0 observer/consumer existed only as a plan and
source-method probes. This candidate supplies original, runnable TypeScript
observation and one-handle coordination code, a strict Python consumer of the
existing OHF gate, and source-level/cross-language conformance tests.

**State: native laboratory vertical demonstrated; BUILT_NOT_PROVEN for production.**
The original `6601e39` source proof used contract doubles. The September 17 native
qualification below now mounts the exact upstream DSH/Cordis core, creates a real
Agent, captures this observer's output, and passes those bytes to the existing
OHF consumer. Ten native cases include setup-time OS denials and cleanup failure.
Independent source review, shared-owner adoption, and operational admission remain
outstanding. No live provider task, Executive Worker route, or rich adapter is
accepted. The positive consumer uses explicitly synthetic host/Executive identity;
it is not process-to-host or provider-origin attestation.

Current procedure: `b731149296a9d837d426730813f68d5acc6133ac` (Skillpack 1.0.1).
Bounded native qualification and seventh-path authorization: #756 comment
`5721660513`. This keeps the original operation/branch and existing owners.

## One bounded source surface

- `experiments/harness_convergence/dsh_native/observe.ts`: original exact-Agent
  collector; no loader, subprocess, provider prompt or company admission.
- `experiments/harness_convergence/dsh_native/fixture_host.ts`: one-handle
  coordinator and adapter for public `agents.create/setup`, given a Context
  that the qualified native owner already composed.
- `experiments/harness_convergence/dsh_native/ohf_consumer.py`: pure private
  payload intake and existing OHF comparison; no new gate or runtime command.
- `tests/harness_convergence/dsh_native_observation.spec.ts`: source tests with
  explicit contract doubles; its `--emit` mode runs the real original collector.
- `tests/harness_convergence/test_dsh_native_observation.py`: strict intake,
  original TypeScript-to-existing-Python comparison and negative-suite wrapper.
- `experiments/harness_convergence/dsh_native/mounted_fixture.ts`: fixed actual
  upstream core composition and ten native assertion cases; a laboratory driver,
  not a second Agent loop, package service or production process supervisor.
- This runbook.

No control-plane, existing native proxy, Agent Eval core/corpus, dependency lock,
workflow, credential, account, route, deployment or organizational record changes.
Research #687 remains records-only. A2 #692 is separate and unchanged.

## Trusted inputs, observation and proof limits

`createObserver` receives the exactly owned handle, native registry Context,
installed native selection reference and separately supplied owner recipe.
These are trusted same-process fixture-owner arguments, never model-selected
paths, arbitrary public callbacks, provider credentials or user JSON.
The owner recipe is an expectation, not a source of permission.

The observer calls public `tools.schemas(agent)` and `tools.get(name, agent)`
on that exact handle, not a global or newest-session lookup. It binds schema
order and actual registered definition/executable/render references to the
fixture's original definitions. Same-name/same-schema replacement and in-place
implementation changes refuse. `run_code` refuses the first native-only profile.
Native presentation is inferred from the pinned public tool-view semantics and
absence of its reserved transport; no private `modeFor` API is used.

Loader entries must exactly match the flat admitted recipe in order and be
active. Groups, disabled/pending/missing/unexplained entries refuse. Source
module and implementation digests associate these observations with the
**separately admitted supply recipe**; this collector does not hash a loaded
binary or claim that Loader diagnostics authenticate code origin. Qualification
must supply the immutable package/build/source association independently.

The installed selection is read and resolved through the native LLM service,
then selection, Agent identity, tool definitions and composition are checked
again after the await. Unknown/failed resolution is not a retained catalog
success. This is native effective configuration, not response-reported physical
model identity and not a promise that a later dispatch cannot drift. No call to
`stream`, `query`, `prompt`, or model work is made by these functions.

The private `dsh-native-observation/n0` payload is bounded transient evidence,
not a persisted canonical schema or ABI. It binds existing attempt/worker/
process-generation/native-session identifiers but allocates none of them.
Limits: 65,536 UTF-8 bytes, depth 32, 4,096 JSON nodes, 64 tools, 128 composition
rows, 128 properties/array elements, 8,192 string units. N0 admits safe integral
JSON numbers only; oversized, cyclic, accessor-bearing, non-JSON, duplicate-key
or malformed values refuse rather than truncate. ASCII object keys preserve
cross-language canonical ordering. Supplied evidence uses the existing pure
Agent Eval secret-shape rejector; no ambient secret inventory is read.

`compareNativeObservation` accepts bytes plus an independently supplied frozen
`NativeBinding`, requested OHF profile, and trusted host observation. It does
not accept a caller-provided ALLOW or completeness flag. Nested mutable binding
aliases, stale IDs, altered tool/schema/source digests, or missing fields refuse.
Host-observed capabilities/extensions cannot be cleared by a native payload.
Required unknown host facts refuse; optional unknown metadata is not globally
vetoed. The N0 closed-fixture recipe is the only context in which empty extension
inventories are used. This is not a general method for proving arbitrary
processes lack plugins or MCP.

The consumer derives the backend-private effective-config digest from the
validated native model/settings/tool/composition projection. It does NOT echo
a requested digest or trust an unrelated host config value. Configuration
identity excludes attempt/session IDs. Native token-setting or other config
drift reaches the existing `compare_launch` config refusal. Model mismatch,
sandbox refusal and final launch decision remain owned by the existing OHF
comparator. The result reports comparison evidence and `first_work_started=False`;
it submits no command or Job and grants no execution permission.

## Setup, cancellation and cleanup

`nativeFixtureFactory` uses the already-provisioned native Context and the real
pinned `installModelSelection` supplied by its trusted owner. It snapshots the
binding/recipe before setup awaits, installs native presentation and two
artificial in-memory tools before publication, and passes the creation signal
through the public factory. The qualified Context must contain only the fixed
minimal native composition plus a provider-free LLM resolver/adapter.
The coordinator does not provision that Context, discover packages or install DSH.
The separate fixed mounted fixture now composes the real upstream core services
from externally qualified supply; it never fetches or installs anything.

`withOwnedFixture` holds an observation until the owned Agent handle is disposed
and no longer registered. Capture failures still dispose; cleanup failures
return `CLEANUP_UNSETTLED` rather than a success. A cancelled setup or teardown
cannot publish late success. Unexpected errors are translated to bounded codes
without echoing supplied error text. A factory failure without a returned
handle is `SETUP_FAILED_EFFECT_UNRESOLVED`; source code cannot claim the
unobserved factory rolled back. The native factory/process owner must reconcile.

A settled handle is NOT proof of OS process-group, MCP/network or persistence
cleanup. The result explicitly scopes cleanup to the owned Agent handle.
`provider_turns_started=0` counts this coordinator's own work-turn invocations;
`provider_calls_observed=null` states that upstream provider activity was not
measured. Do not turn that null into a global zero-provider assertion.

## Historical source proof and retained regressions

Environment observed: MacBook, Node 26.8.1, CPython 3.12.14, pytest 9.1.1.
No package installation, DSH application launch or provider call was used for
these source tests. TypeScript was executed by Node type stripping; a compiler
check against the native dependency/type closure was then outstanding; the
subsequent strict native check below closes that laboratory compatibility gap.

- Initial TypeScript/Python suites were RED while implementation was absent.
- Additional RED cases exposed and repaired host-capability laundering,
  optional-unknown over-rejection, setup signal loss, raw error leakage,
  cancellation during teardown, native effective-config echo, permissive digest
  coercion, and mutable factory binding/recipe capture.
- Current TypeScript source suite: **29 passed, zero failures/skips**.
- Current selected Python suite: **110 passed, zero failures/errors/skips**;
  this includes 45 new source-conformance tests and 65 existing OHF contract/wire
  tests. Two Python tests execute original TypeScript: actual produced JSON
  reaches the existing comparator, and the complete TypeScript negative suite
  is checked. Do not add those executions again as independent native trials.
- Eight targeted forbidden mutants each failed its intended assertion: omitted
  Agent scope, removed implementation check, host-capability laundering, missing
  binding check, missing late-cancel check, native-config echo, mutable binding
  alias, and mutable recipe alias. Source bytes were restored after each probe.

Reproduction on an already prepared source-test environment:

```sh
node --test --test-reporter=tap tests/harness_convergence/dsh_native_observation.spec.ts
PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest \
  --noconftest -p no:cacheprovider -q \
  tests/harness_convergence/test_dsh_native_observation.py \
  tests/test_ohf_p1a_operator_harness_contract.py \
  tests/test_operator_harness_wire.py
```

`--noconftest` deliberately excludes unrelated global fixtures; this is selected
source proof, NOT full repository CI. The Python wrapper explicitly skips the
TypeScript path without Node >=24; such a skip is missing cross-language proof,
not conformance success. No skip occurred in the observed local run.

## Frozen native source identity

Official pinned upstream:
`deepseek-ai/deepseek-harness@0d1f50007f9bca3f52b06e1c3074fa14d5fb0720`.
The inspected root is `@deepseek-ai/dsh-root` version `0.1.6-alpha.1`, package
manager `pnpm@11.7.0`, Node constraint `^22.19.0 || >=24.0.0`.
Native source lock Git blob: `cdf7dd28775dc60a82f63a1766bdd141ce0073db`.
Native source lock SHA-256: `ca131858949bd12b2acfc227b1af7dfa3c8d65e74b234824d5c741e6421010a1`.
Root package SHA-256: `5dec5c24e746dac82e3da6084fb3a14d0e85ad9154570c7fd457974112f38b08`.

These identities alone were not an installed bundle or native grant. The bounded
laboratory preparation below now binds a separate official Node binary, exact
source/build/package bytes and an OS-confined, provider-free core fixture under
the attended process tool. This does not provision a production principal or
relax the existing operational supply/capability owners. No floating installer,
lifecycle install script or stock ACP application is used.

## Acceptance and exact continuation

Keep the implementation Draft until exact-head repository/security checks and
independent source review are consumed. Shared-owner adoption is separate from
request/delivery. No previous reviewer request or Fable thread proves START.
N0 source construction is independent of A2 #692 release; do not bundle their
source or repeat the unchanged teardown/A2 tests.

Sol's next acceptance action is exact-head independent review and existing-owner
adoption of this native-proven laboratory candidate, with current-base release
checks. No Worker/OHF route may be registered from this experiment. A subsequent
real read/research Worker must use existing provider/principal/admission/process
owners and prove actual input, tool work, result and parent consumption. #660's
real-resume requirement remains unchanged; N0 is not that rich operator.

No branch replacement, force push, extra registry, package service, runtime
loop, credential store or permission system is authorized. Unknown effects stay
on their original carrier. Preserve the existing research and use this exact
source candidate for any repair. A source/test checkpoint is not product or
native acceptance; the parent harness mission remains active.


## Real native laboratory qualification — September 17, 2026

This section supersedes the old source-only execution ceiling, not any review,
production, provider-origin or operational-admission requirement. Sol directly
performed the bounded test on the retained managed MacBook workspace. No new
worker, dispatcher, persistent service, provider account or control plane exists.

### Exact supply and isolation

The exact upstream codeload archive has SHA-256
`61ea8b495bec3f1debcfd58b95350edb2d48548493e0af4111244399dc29dd22`.
11,227 regular members matched the retained exact Git tree. The excluded
`apps/desktop/scripts/windows-sign.cmd` is the sole archive-byte mismatch; its
CRLF form is explained by the pinned `.gitattributes` Windows override and is
outside the selected fixture. No native source was normalized or patched.

The initial dependency/peer closure is 25 workspace packages. The upstream
TypeScript reference graph additionally requires the attachment type project:
26 projects, 368 retained original files after that expansion. Every retained
original file and all 12 external package contents were unchanged after execution.
The source package graph and compiler project graph are different measurements.
The compiler uses each native project's own configuration, then our strict
integration project references those generated declarations. Flattening vendor
source under unrelated strict options was rejected rather than weakening the
native source or hiding diagnostics. No `any` cast, type stub or suppressed native
compatibility error substitutes for the passing full selected native type check.

Official Node 26.8.1 macOS ARM64 archive SHA-256:
`6e577fd0d9db776db82306629e441a9dace416702622aebdd171c9dfaa41f4d2`.
Executable SHA-256:
`ebd2d552c7bebde593dd0390530963ad28de56bccde6ce387cdbe55fb0b6fb8e`.
Publisher HTTPS checksum and native codesign verification passed for Node.js
Foundation `HX7739G8FX`. OpenPGP verification is NOT claimed. This is a pinned
laboratory toolchain, not a recommendation that it is the latest production
security release. Node and package license/notice bytes are retained in supply.
No binaries or third-party source are copied into the Mastermind repository.

Fixed external artifacts (SHA-512 integrity also matched the exact upstream lock
before extraction; no install/postinstall/prepare scripts ran):

| Package | Archive SHA-256 |
| --- | --- |
| `@esbuild/darwin-arm64@0.28.1` | `5d64cc9bc527d598450b5f8d47ff293eb9f3aea38dd9eff67fd55d228c5ccb43` |
| `@standard-schema/spec@1.1.0` | `a7cb7268be280ab518d450f8c3b07c86f23417225c0ab53691ed59b3067cceaf` |
| `@types/node@22.20.0` | `94e07dec077aef58bf4e9837e486c6f63345e1131c69ca294404a740bd9da286` |
| `argparse@2.0.1` | `27903847fc8215e6fc5a33e81490f7baba66403f8aade33771b988cca097728c` |
| `esbuild@0.28.1` | `eb8ef756f8299d16d5c8b35678606d715ba29923f500db7b37c181310eed40a5` |
| `js-yaml@4.2.0` | `50bafc4aa4cb263b2d3b3f430535ed8d49294eb107796e767d76a5ca475aca66` |
| `node-addon-native-custom-loader@0.1.6` | `aec357cef05357b0c13611fc91cb61c16010bd347b0c0b9f8edde3f8fe375171` |
| `node-addon-require-builtin@0.1.6` | `1135999d5de99ff6cb9c1366afd3d83550d8a3d66851ec3c070a88ad0244dd6d` |
| `node-addon-require-builtin-darwin-arm64@0.1.6` | `6885119ba5dba806523862414595df24c650a2d9b61d629237a741fa7cdb84b7` |
| `typescript@6.0.3` | `33cd0ee1beaa8c9e9d15a9da836c62ddea4c34a42d7c2d349dbc80d94165d22a` |
| `undici-types@6.21.0` | `cfa1a9a5e18988850e548f4f6b629c58da32dc32bebb116e58ea301e516fd16d` |
| `zod@4.4.3` | `ee38f17f533fd500610685a483ae2f413c26f4eb33a51684314563c8d60f279c` |

The source bundle uses exact esbuild 0.28.1 for macOS ARM64. The optional
`node-addon-require-builtin` is external and unmounted: Loader's documented
no-internals fallback is used. Fixed builtin entries require no dynamic import,
HMR or native-addon load. This is not the full DSH CLI/ACP/web application.

The OS sandbox is deny-default before Node or any DSH plugin loads. It allows
read-only supply/system libraries, the literal filesystem root directory required
by Node startup, and writes only under one designated scratch directory. It does
not permit network or child-process creation. A broader diagnostic read policy
was used only for signed Node `--version`, never for DSH or fixture code. Final
canaries produced EPERM for outside read, outside write, child execution and TCP
connection; the positive scratch write succeeded. Setup-time native cases repeat
network/child/write denials inside actual Agent creation. The scratch directory
contains only the public write canary and a compiler-generated Node compile cache;
no claim of zero OS diagnostics or zero scratch artifacts is made.

Native process environment is exactly PATH, HOME, TMPDIR, LANG and TZ, with no
inherited credentials or user configuration. The fixture has a 384 MiB V8 heap cap
and a 20-second parent timeout. Every observed process settled and was waited;
none required retry/failover or left a live native child. The ten normal cases
exited zero with empty stderr. Code is never run outside confinement to obtain a
passing native result.

### Native compatibility and behavior

The first strict native compilation exposed real integration mistakes in the
source-only candidate: nullable Loader fields, branded native session IDs,
optional abort-signal shape, unchecked indexed values, and the actual root-Fiber
cleanup API. The corrections stay in the native composition and original
observer/coordinator; no common OHF, upstream, route or policy file changed.
Type-only public native interfaces replace guessed signatures. A narrow port
adapter brands IDs and verifies the exact registry object before invoking native
tool APIs. It never supplies a fabricated inventory.

The mounted fixture uses the actual Cordis Loader, AgentRegistry, AgentLoop,
SessionStore, ToolRuntime, SystemPrompt, SessionProjectionRegistry and LlmRuntime.
Only the provider is deliberately artificial: it resolves the fixed model and
counts/rejects all generation. Two in-memory tools are fixture functions, not
filesystem/shell/browser tools. Native Agent/session/service removal is observed,
not inferred from a disposer returning. The conservative source error
SETUP_FAILED_EFFECT_UNRESOLVED remains in failure cases even though this isolated
lab independently proves rollback and final process termination.

Actual native JSON reaches the existing Python OHF comparator. Its positive host
attestation and Executive-like identity strings remain explicitly synthetic; the
experiment does not authenticate provider origin or prove operational process-to-
host binding. Seven corrupted/missing input controls refuse through the same
consumer. All cases retain first_work_started=false.

Three deliberate native mutants (omitting Agent scope, removing implementation
identity checking, and permitting late cancellation) each failed its intended
real-native assertion. Source bytes were restored; the final suites passed.

Final verification: 29 TypeScript source tests, and **120 selected Python tests
with zero failures/errors/skips**, including ten actual native process cases and
65 retained OHF tests. The Python suite also runs the source TypeScript tests;
do not count those repeated executions as additional independent native trials.
The native tests are opt-in: without prepared supply they explicitly skip with
"mounted DSH proof NOT executed". Hosted source CI alone is not native proof.

### Reproduction from qualified supply

`SUPPLY` is the retained, owner-prepared source/package/binary directory; it is not
an arbitrary provider home. `WORKTREE` is this exact implementation candidate.
No command below installs dependencies or calls a provider. Preserve the original
upstream tsconfig files and their project references.

```sh
# Original upstream declarations, each under its own native project settings.
cd "$SUPPLY/source"
"$SUPPLY/node/bin/node" --max-old-space-size=768 node_modules/typescript/bin/tsc \
  -b vendor/loader vendor/cordis vendor/include vendor/cosmokit vendor/schemastery \
  packages/core/agent-loop packages/core/agent packages/core/tools packages/llm/llm \
  packages/session/session-persistence packages/session/session-projection \
  packages/settings/settings --emitDeclarationOnly --pretty false

# Run via the qualified build sandbox (read-only inputs; build-directory writes).
/usr/bin/sandbox-exec -f "$SUPPLY/build.sb" \
  "$SUPPLY/source/node_modules/@esbuild/darwin-arm64/bin/esbuild" \
  "$WORKTREE/experiments/harness_convergence/dsh_native/mounted_fixture.ts" \
  --bundle --platform=node --format=esm --target=node26 \
  --tsconfig="$SUPPLY/source/tsconfig.base.json" \
  --outfile="$SUPPLY/build/mounted_fixture.mjs" \
  --metafile="$SUPPLY/build/metafile.json" --external:node-addon-require-builtin

# The strict integration config references all 26 native declaration projects;
# typeRoots is the supplied @types directory. No skip of our source is permitted.
/usr/bin/sandbox-exec -f "$SUPPLY/typecheck.sb" "$SUPPLY/node/bin/node" \
  --max-old-space-size=768 "$SUPPLY/source/node_modules/typescript/bin/tsc" \
  --project "$SUPPLY/native-typecheck.json" --pretty false

# Actual native processes and the real existing comparator, not stored-output tests.
cd "$WORKTREE"
MMX_DSH_NATIVE_SUPPLY="$SUPPLY" PYTHONDONTWRITEBYTECODE=1 \
  PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest --noconftest -p no:cacheprovider -q \
  tests/harness_convergence/test_dsh_native_observation.py \
  tests/test_ohf_p1a_operator_harness_contract.py tests/test_operator_harness_wire.py
```

Before the native command, the existing qualified preparation must regenerate
`build-inputs-receipt.json` from the exact esbuild metafile plus input SHA-256s,
verify the bundle SHA-256, and construct `native-recipe.json` from the fixed seven
builtin entries and original fixture implementation digest. recipe_digest is
SHA-256 of the canonical complete build-input record, not a name/version claim.
The tests verify the Node and bundle bytes and the current three owned TS source
identities before running. Boundary receipts describe the proven OS policy;
they are not portable authority grants. Fresh supply/platform changes require
fresh boundary tests, not blind receipt replay or unconfined execution.

### Compact immutable proof receipt

The following is development evidence, not a sealed evaluation holdout or an
operational admission record. Native output hashes bind exact output bytes;
synthetic host controls do not become authenticated production observations.

```json
{
  "scope": "N0_REAL_PINNED_NATIVE_LAB_NOT_PRODUCTION",
  "upstream_commit": "0d1f50007f9bca3f52b06e1c3074fa14d5fb0720",
  "bundle_sha256": "0cd96dbab5ca255a259727dd6744ecafb5fca4309d0ae92a812bcd81a527b10b",
  "build_inputs_sha256": "4553b245128d7617e65fc3ac8643ffed94cb199c18f604f14533568dbc4eb81e",
  "build_input_count": 164,
  "native_observation_sha256": "dac35668b419df66c7e963d251612032c8c1aab955b69f4eb91c5c7fb61482d9",
  "recipe_digest": "sha256:0e6e7c1aafa8af3a4b409d5675c73b7d8fa4dc37959a7cb19b6cc5d6a36c4269",
  "policy_sha256": "4732726e2c5a5d469adadde8b667263c73e86d52ffe4288fc66e40a6dc9a4763",
  "native_cases": [
    {
      "case": "positive",
      "decision": "OBSERVATION_PRODUCED",
      "exit_code": 0,
      "native_generation_calls": 0,
      "native_agent_absent": true,
      "native_session_absent": true,
      "native_services_absent": true,
      "setup_effect_denial": null,
      "stdout_sha256": "df1705be4fe20907828249953b1f55ac835d3f70dbc189b58e08287e3d6ba89e"
    },
    {
      "case": "ambient-tool",
      "decision": "TOOL_CENSUS_MISMATCH",
      "exit_code": 0,
      "native_generation_calls": 0,
      "native_agent_absent": true,
      "native_session_absent": true,
      "native_services_absent": true,
      "setup_effect_denial": null,
      "stdout_sha256": "a89fc6997ec91803f4c7d607bbb973f6163635ad8e0d81fedc6fa8f134626763"
    },
    {
      "case": "implementation-drift",
      "decision": "TOOL_IMPLEMENTATION_DRIFT",
      "exit_code": 0,
      "native_generation_calls": 0,
      "native_agent_absent": true,
      "native_session_absent": true,
      "native_services_absent": true,
      "setup_effect_denial": null,
      "stdout_sha256": "b6fb98c2b04d10238724ca1b7cbf582ea8051c23fd7b20e71bfee29cf0a8195f"
    },
    {
      "case": "cancel",
      "decision": "CANCELLED",
      "exit_code": 0,
      "native_generation_calls": 0,
      "native_agent_absent": true,
      "native_session_absent": true,
      "native_services_absent": true,
      "setup_effect_denial": null,
      "stdout_sha256": "359621ab52f1fcf583f2ac5650cc190377b4465787f75daf164ef702c43c41b1"
    },
    {
      "case": "late-cancel",
      "decision": "CANCELLED",
      "exit_code": 0,
      "native_generation_calls": 0,
      "native_agent_absent": true,
      "native_session_absent": true,
      "native_services_absent": true,
      "setup_effect_denial": null,
      "stdout_sha256": "2f540d43f668fd902559dcc7f96918d0f1de74785a795587e43b41a31cd03794"
    },
    {
      "case": "setup-failure",
      "decision": "SETUP_FAILED_EFFECT_UNRESOLVED",
      "exit_code": 0,
      "native_generation_calls": 0,
      "native_agent_absent": true,
      "native_session_absent": true,
      "native_services_absent": true,
      "setup_effect_denial": null,
      "stdout_sha256": "ebb0623644fb3181fbc9ce9a7d825ed413e30619221cc32d9082172ef9818944"
    },
    {
      "case": "cleanup-failure",
      "decision": "CLEANUP_UNSETTLED",
      "exit_code": 0,
      "native_generation_calls": 0,
      "native_agent_absent": true,
      "native_session_absent": true,
      "native_services_absent": true,
      "setup_effect_denial": null,
      "stdout_sha256": "9758587422c729196c713dc035964ddfacb9ae5fb158cffc5450553c90a3db91"
    },
    {
      "case": "setup-subprocess",
      "decision": "SETUP_FAILED_EFFECT_UNRESOLVED",
      "exit_code": 0,
      "native_generation_calls": 0,
      "native_agent_absent": true,
      "native_session_absent": true,
      "native_services_absent": true,
      "setup_effect_denial": "EPERM",
      "stdout_sha256": "c3d5b39e3d3c51c1282cf832089b0a67236076cf96e58af180bb6d5f8a0e082a"
    },
    {
      "case": "setup-network",
      "decision": "SETUP_FAILED_EFFECT_UNRESOLVED",
      "exit_code": 0,
      "native_generation_calls": 0,
      "native_agent_absent": true,
      "native_session_absent": true,
      "native_services_absent": true,
      "setup_effect_denial": "EPERM",
      "stdout_sha256": "453c58240a1d5a869f04c2ad18b832ad99ac7ac0db3bee635fa9e577819487d5"
    },
    {
      "case": "setup-write",
      "decision": "SETUP_FAILED_EFFECT_UNRESOLVED",
      "exit_code": 0,
      "native_generation_calls": 0,
      "native_agent_absent": true,
      "native_session_absent": true,
      "native_services_absent": true,
      "setup_effect_denial": "EPERM",
      "stdout_sha256": "f7e4256bde731efb9b08583519d01f34f27bc99e69db28e7deb9643790672116"
    }
  ],
  "consumer_cases": [
    {
      "case": "actual_native_output_with_separate_synthetic_host",
      "decision": "ALLOW",
      "allows": true,
      "first_work_started": false
    },
    {
      "case": "wrong_native_process_generation",
      "refusal": "OBSERVATION_IDENTITY_MISMATCH",
      "allows": false,
      "first_work_started": false
    },
    {
      "case": "unknown_native_census",
      "refusal": "TOOL_CENSUS_MISMATCH",
      "allows": false,
      "first_work_started": false
    },
    {
      "case": "native_schema_order_changed",
      "refusal": "TOOL_IDENTITY_MISMATCH",
      "allows": false,
      "first_work_started": false
    },
    {
      "case": "native_model_mismatch",
      "decision": "REFUSE_SERVED_MODEL_MISMATCH",
      "allows": false,
      "first_work_started": false
    },
    {
      "case": "native_config_drift",
      "decision": "REFUSE_CONFIG_DRIFT",
      "allows": false,
      "first_work_started": false
    },
    {
      "case": "host_mcp_observation_not_erased",
      "refusal": "HOST_EXTENSION_CONFLICT",
      "allows": false,
      "first_work_started": false
    },
    {
      "case": "unknown_host_sandbox",
      "decision": "REFUSE_UNATTESTABLE",
      "allows": false,
      "first_work_started": false
    }
  ],
  "native_mutation_cases": [
    {
      "mutation": "omit_exact_agent",
      "native_case": "positive",
      "exit_code": 1,
      "discriminating_assertion_failed": true,
      "mutant_bundle_sha256": "ddc5802c639e2e05f6e66206e487be05e947584c0cd7bd371375993dc4b336fb"
    },
    {
      "mutation": "erase_implementation_check",
      "native_case": "implementation-drift",
      "exit_code": 1,
      "discriminating_assertion_failed": true,
      "mutant_bundle_sha256": "fbba1546d9309fec1077ae5cdc503a0e59d4c2d207d649d48df08bdc06680211"
    },
    {
      "mutation": "allow_late_cancel",
      "native_case": "late-cancel",
      "exit_code": 1,
      "discriminating_assertion_failed": true,
      "mutant_bundle_sha256": "1831c25df604be2011bc4a3c626042b47f4bbe5390d14bac963a02a99b667fc5"
    }
  ],
  "integrity": {
    "verified_upstream_files_post_execution": 368,
    "external_packages_unchanged": 12,
    "node_binary_unchanged": true,
    "source_and_native_tests": {
      "tests": 120,
      "failures": 0,
      "errors": 0,
      "skipped": 0
    },
    "runtime_files": [
      "allowed.txt",
      "tmp/node-compile-cache/v26.8.1-arm64-8d7ad2ee-501/b280dd1a"
    ],
    "all_native_processes_waited_terminal": true
  }
}
```
