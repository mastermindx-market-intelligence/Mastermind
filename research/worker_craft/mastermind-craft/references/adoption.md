# Adoption boundary

## Immediate authoring use

The compiler is a dependency-free Python 3.9+ authoring utility. It reads one
non-secret JSON file plus the fixed common/selected-role method files and writes
to stdout. It has no network, provider, runtime, scheduler, installation or write
API. The Python code is optional when the current host cannot run scripts; the
method can still be read, with no claim that compilation ran.

A compiled brief may become ordinary task content through the existing authorized
prompt path. Do not describe that as native Skill loading. The code does not verify
source protection, live tool permissions, workspace custody, or task admission.

## Native skill adoption

Have the existing package/capability owner inventory the complete package, including
this script, every reachable reference, entrypoint, and UI metadata. Compute the
existing CapabilityPackageGeneration and exact effective closure through its current
owner. These authoring digests are not substitutes for that contract. Source state,
revocation, dependencies, and required tool grants remain there.

Have HF1 and the appropriate provider adapter realize an accepted generation in the
Attempt's isolated context and compare actual exposed capabilities with the requested
set. Never widen the exact four-Skill mastermind-operator canary with an extra ambient
Skill. Do not alter user/global provider homes or live workers to install this bundle.

Codex supports project skill discovery and MCP configuration; use the reviewed
existing adapter, not the default discover-everything behavior. Claude Code offers
explicit skill preloading in custom subagents, but preloading is not an allowlist of
all discoverable skills. Its plugin-defined agents ignore some security-sensitive
agent fields; the native adapter must own effective configuration. Cursor also has
native plugin/skill/MCP surfaces; attest the installed version rather than assuming
identical behavior across providers. Verify actual schemas and semantics before use.

For Grok/ACP or a provider compatibility lane without proven native Skill discovery,
use only an explicitly admitted prompt-method path or return the gap to HF1. Do not
build another provider loop, tool proxy, message bus, auth service, or session store.

## Tool-using proof

Use an authorized disposable target. Prove actual role-method consumption and one
useful tool result, not merely a tools/list or SKILL.md acknowledgement. Check missing
or duplicated Skills, changed reference bytes, unavailable required MCP, revoked
permission, stale source/workspace, lost modifying reply, and wrong host/document.
Preserve owner-native receipts and private evidence. A fixture-only rehearsal cannot
promote an installed fleet to PROVEN_LIVE.

## Source references (recheck before adoption)

- https://developers.openai.com/codex/skills
- https://developers.openai.com/codex/mcp
- https://code.claude.com/docs/en/sub-agents
- https://cursor.com/docs

Vendor documentation describes capabilities, not this company's effective grants.
Do not execute example marketplace installs from a retrieved page without admission.
