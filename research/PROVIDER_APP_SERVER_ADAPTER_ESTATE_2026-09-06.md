# Direct-provider adapter estate and delivery disposition

**Operation:** `mastermind-provider-app-server-adapter-fabric-f0-20260906-sol-001`

**Original carrier:** `C0BSBM78V1N/1788731079.486769`

**Authoring continuation:** `1788757510.809509`

**Evidence cutoff:** 2026-09-07, 05:01 UTC for the implementation inventory below.

**Protected source:** Mastermind `87fcea41a0357ff59c615ecced73645364a42567`, tree `4665dd63913a089078cb94a32d974e2f65230d10`.

**Procedure:** same-commit Sol Skillpack 1.0.1 / bootstrap major 1.

**Disposition:** records-only architecture candidate; `PARTIAL / END_TO_END_NOT_PROVEN` for the overall capability. This file does not admit a worker or change a production route.

## 1. The job, rather than the brand

The Chairman supplies an outcome once. A qualified frontier supervisor decomposes it, selects economical eligible work through existing owners, receives attributable results, requests bounded repairs, and finishes with visible user or machine value. The responsibility survives a conversation ending. Routine account selection, prompt carriage and session hunting must not remain Chairman work.

The machine job is to preserve the exact authority, parentage, workspace, provider realm, effect and evidence at every transition. The moat is the accumulated trustworthy execution-and-review loop around Mastermind's product and intelligence, not a new wrapper around three provider brands. More running agents, longer turns and more tokens are not independently accepted output.

This F0 resolves interface selection and existing-owner composition only. It does not promise a three-week completion date, a worker-hour multiplier, hardware capacity, or production readiness. Those hypotheses need measured trials through the real path.

## 2. Source estate, not an empty architecture

The following entries were read from the exact protected commit above. A module's local registration disclaimer is not a company-wide deployment census.

| Existing source | Verified role | Honest capability boundary |
| --- | --- | --- |
| `control_plane/worker_adapter.py` | `WorkerExecutionAdapter`: start, collect, cancel and validation floor | Existing sealed-worker interface; not a new rich-session API |
| `control_plane/operator_harness_contract.py` | `OperatorHarnessAdapter`, observed identities, operations, epochs, cursors and candidate results | Existing rich-operator contract; provider/session observations do not allocate Executive identities |
| `control_plane/operator_harness_orchestrator.py` | Stateless composition over `RuntimePort`; committed intent precedes adapter mutation | Source implementation exists; registration and production proof must be established separately |
| `control_plane/codex_operator_adapter.py` | Existing Codex App Server implementation, process/attestation/result/attention mechanics | `BUILT_NOT_PROVEN` at this record's provider-proof ceiling; do not build a replacement |
| `control_plane/executive_orchestration_principal.py` | Closed process, provider-home and placement observations | Source-owned evidence, not model-selected authority |
| `control_plane/executive_orchestration_result.py` | Closed role envelopes, lineage, review and aggregation validation | Reuse these validators, never promote a prose PASS marker |
| `control_plane/model_router.py` | Deterministic suitability and logical execution shape | Atomic Worker selection remains with the existing claim owner |
| `control_plane/session_targets.py` and `runtime_binding_projection.py` | Exact binding and read projection | Unknown explicit targets refuse; labels and newest sessions cannot elect a writer |

Useful immutable blob anchors are `7723db9407e6a8e50e58caa295aabe2c733678ff` (sealed interface), `54df0b7a5abf0b713d5e37a40059802b1a894f08` (rich contract), `9a3dddc2d68aa0965262690750164e7b80770d45` (orchestrator), `1351ef00da5ae97b0b87b34e6c5883f2dfbfc4ea` (Codex adapter), and `a1386a56ee588a223ca982069c87e741909f768d` (result validation). These are dated implementation identities, not permanently required future hashes.

## 3. Current delivery work to consume, not duplicate

| Carrier | Observation at the cutoff | Next owner / preserved boundary |
| --- | --- | --- |
| Mastermind #350, CAP-S1 | Merged as `ef02058ba9356808e41937dab054f00043f89c1e`; source writer and finite release settled | Source package only; consumed unsuccessful provider attempt remains NO_REPLAY, default V4 not promoted |
| Mastermind #513, native roles | Merged as protected `87fcea41a0357ff59c615ecced73645364a42567` | Seven read-only research-role source descriptors repaired; installed parsing and engineering-worker fanout are not proved |
| Mastermind #471, HF1-A | Existing Draft at `205cd12a5ed0103500659168fb59740a70255a1e`, based on protected 87; same retained writer | Common contract and package closure are existing work. Consume its actual final checks, review and release; do not reimplement it |
| Mastermind #455, PF1-F0 | Existing Draft at `523a1e9cb88c0521e455b8a34eda60e08a75a65b`; current-source START `1788757104.764579` issued to already-ACKed receiver | Four-path provider-free protocol repair; delivery of START is not evidence of its consumption or a real Claude turn |
| Mastermind #508, plural lane observation | Existing Draft at `3b0e97e7133153ecf4cc2539936f2dc9d7ca929f` | Existing review evidence must be reconciled by its owner; no second reader or weakening of singular-target ambiguity checks |
| Mastermind #514, census read budget | Existing Draft at `54ed74d4c1b9a590330f2ebd373ff616c5b169d0`; independent review `5127146964` accepted and reviewer stopped | Separate current integration/release; no repeat semantic review just for disjoint ancestry |
| Personal first-read E1, root `1788719305.919709` | Existing controller and fourteen-path source child, not a production outage | Complete current source corrections and authenticated consumer proof through that owner; never replay old Task 1 from an attachment |

These statuses are a point-in-time inventory. Action-time reads supersede them. They neither reserve all related paths indefinitely nor release a sticky source writer merely because a PR is green or a process is absent.

## 4. Accepted provider research and corrections

### Codex: retain the existing rich adapter

C0's corrected return on `1788731200.353049` was accepted/stopped at `1788742017.574439`. It established a supported App Server candidate and source seams, not a current selected Worker or admitted provider turn. RuntimeBinding/Event and result consumers are existing source, not NOT_BUILT merely because this census did not observe their live use. The reported generated-schema residue after a refused cleanup remains retained; no retry is authorized here.

The official [App Server reference](https://learn.chatgpt.com/docs/app-server) documents bidirectional JSON-RPC, default stdio JSONL, version-specific schema generation, session/turn operations and terminal events. The selected transport stays stdio; other newly documented transports are not automatically admitted. An interrupt acknowledgement is not a terminal interrupted turn. Account/read and rate-limit surfaces are candidates to qualify, not evidence of this deployment's stable account identity, current quota or served model. This document makes no initialization or provider request.

### Claude: preserve the first sealed-worker vertical

A0's corrected result `1788745582.594099/1788745619.659779` was accepted with direct qualifications at `1788746032.711459`; actual STOP consumption `1788746205.531929` and Secretary suppression `1788746290.869559` close that research child. Those corrections do not replace PF1 with a new SDK implementation or reopen A0.

The official [programmatic guide](https://code.claude.com/docs/en/headless) supports foreground `claude -p`. It also describes non-bare discovery of project hooks/MCP, a different bare-mode authentication boundary, and unfinished turns after SIGTERM. Thus flags or process exit alone cannot establish the intended isolated execution contract. The first Mastermind vertical remains the existing PF1 foreground, no-session-persistence contract; richer persistence and native fanout are later capabilities, not silently inherited from the provider.

The [Python reference](https://code.claude.com/docs/en/agent-sdk/python) documents `AssistantMessage.model` and optional rate-limit information. They are usable observation candidates, not guaranteed fresh populated quota or authenticated Worker-account proof. [CLI reference](https://code.claude.com/docs/en/cli-reference) documents JSON auth status; stable same-worker credential binding still requires qualification. Do not conflate a separate status process, a model label or an email with that binding.

Pinned SDK source `anthropics/claude-agent-sdk-python@a8b1e285f97f8dbcb7b10226d74ba0d551b493f4`, [subprocess transport](https://github.com/anthropics/claude-agent-sdk-python/blob/a8b1e285f97f8dbcb7b10226d74ba0d551b493f4/src/claude_agent_sdk/_internal/transport/subprocess_cli.py), establishes bundled-before-system CLI lookup. PATH-free success proves neither in-process execution nor the selected executable. Any later SDK adoption must qualify both distribution and actual binary under the existing closed PF profile. This is not a permanent prohibition on separately authorized native verification.

The [Claude-plan Help Center article](https://support.claude.com/en/articles/15036540-use-the-claude-agent-sdk-with-your-claude-plan) carries a June 15 update pausing the proposed SDK-credit change. Do not use the superseded table to force a metered-API default. Actual entitlement and intended internal use remain existing economic/auth-owner questions; neither this billing observation nor F0 permits third-party login resale, token extraction, or a new credential shortcut. Preserve the protected PF1 dedicated-principal/native-Keychain route rather than importing an example environment token.

A0's entire original retrieval interval is source-reported and unverified; its post-message segment is inconsistent. Package absence was only scoped to the inspected interpreter. These qualifications remain, without another correction loop.

### Grok: one neutral ACP implementation, not a Grok control plane

G0's corrected S1-S6 at `1788742738.431569/1788742769.030499` were accepted/stopped at `1788743111.708899`; actual consumption `1788743280.805389` and suppression `1788743387.506599` close it. The full corrected artifact is 12,516 bytes, SHA-256 `b50c8ed9b20e6c13f8b5842435dbc183dcd8f90042d1604fc5b10b75f934ef3d`, as independently reported by the existing Integrator. P1-P9 remain unvalidated written proposals, not adopted executable acceptance criteria.

Official [Grok headless scripting](https://docs.x.ai/build/cli/headless-scripting) documents `grok agent stdio`, integer ACP wire version 1, advertised authentication methods and session setup. Rust crate semver is not the wire major. Documentation examples that advertise broad filesystem/terminal abilities or choose authentication from environment presence are not Mastermind capability grants or placement policy.

The [ACP prompt-turn contract](https://agentclientprotocol.com/protocol/v1/prompt-turn) is bidirectional: the client must handle permission requests as well as updates. Cancellation requires `session/cancel`, cancellation of pending permission requests, continued update handling, and a cancelled terminal response to the original prompt before the existing cleanup/fencing phase. Killing a subprocess is not remote cancellation or a proof that billing ceased. Missing terminal evidence remains effect-unknown.

The [Build enterprise reference](https://docs.x.ai/build/enterprise) documents team restriction and API-key-auth disablement as same-Build-realm controls. They are enforcement candidates, not installed identity proof. API key/management identity cannot attest a cached-token worker by analogy. [API security guidance](https://docs.x.ai/developers/faq/security) and [hosted tools](https://docs.x.ai/developers/tools/overview) must not be conflated with Build CLI policy or an admitted managed coding workspace. No suitable hosted coding-workspace/App Server interface was established by G0; this is not a universal absence claim.

Preserve `research/MASTERMIND_EXECUTIVE_CURSOR_GROK_AUTH_ACP_SOURCE_LAW_2026-08-25.md` section 6: one neutral ACP adapter plus manifests under existing owners. `provider_worker_slots.py` is a Codex-specific inventory, with more constraints than its regex. Widening the regex is not a safe Grok placement slice.

The five original wrapper help/version probes remain qualified: no model/auth effect was observed, but comprehensive effect absence was not established. Do not reconstruct history by replaying them. Installed negotiation, account, cancellation, usage, region and session-load remain unproved. A capability advertisement is not functional resume proof; absence of one configuration restriction does not guarantee acceptance. GUI presence invalidates an absence precondition but is not causal evidence of GUI dependence.

## 5. First useful result and remaining unknowns

The integration target is one admitted responsibility with two bounded children, correct quality/capacity selection, one attributable return, one independently assessed repair, and one exact leader continuation consumed through the existing Wake/RuntimeBinding path. The visible consumer must show truthful missing, stale, failed and effect-unknown states. A successful fixture, completed research report, or source merge cannot substitute for that journey.

There is no new requirement to re-open the completed provider research or postpone already-admitted HF/PF/Personal repairs for this record. The first missing transition must be established within the actual chosen lane: source completion, deployment identity, account qualification, provider execution and leader consumption are separate predicates, not one generic 'adapter unavailable' status.

## 6. Provenance and source-writer reconciliation

At 04:59-05:01 UTC this Web author verified absence of the declared F0 branch locally and remotely, no target-file instance in 180 registered worktrees, and no four-path overlap in complete stable before/after inventories of 49 open PRs including previous filenames. This supports starting the original four-path authoring continuation; it is not a census of every unregistered directory or a future collision guarantee. No existing worker's source was adopted.

The original four-path F0 record is the [design](../docs/superpowers/specs/2026-09-06-provider-app-server-adapter-fabric-design.md), [delivery plan](../docs/superpowers/plans/2026-09-06-provider-app-server-adapter-fabric.md), this estate, and `tests/test_provider_app_server_adapter_source_law.py`. Tests verify record coherence and source-seam existence only. Independent review, current-base CI, source release, installed proof and final acceptance remain distinct.

External pages above were re-read during this September 7 authoring continuation where used for the selected interface; dated accepted child evidence is explicitly attributed separately. The named SDK commit is immutable; unversioned documentation remains retrieval-time evidence and must be qualified against the actual future executable. Current action-time law and source owners outrank this dated inventory.
