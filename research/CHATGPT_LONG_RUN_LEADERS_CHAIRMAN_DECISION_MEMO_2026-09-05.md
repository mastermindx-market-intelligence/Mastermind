# Chairman Decision Memo — ChatGPT Web as a Long-Run Mastermind Leader

**Originated:** 2026-09-05  
**Current-state correction:** 2026-09-06  
**Carrier:** Mastermind Draft PR #490  
**Parent operation:** `chatgpt-pro-long-run-leaders-research-20260905-sol-001`  
**Status:** `CHAIRMAN_DIRECTION_RECORDED / ARCHITECTURE_CANDIDATE / RESEARCH_ONLY / NO_RUNTIME_OR_INSTALL_AUTHORITY`

## Chairman direction now recorded

Adopt the following company direction:

> Make ChatGPT web a durable **leader** of governed work, not an unrestricted logged-in desktop worker. Give exact, short-lived **operator** contexts Codex-like file/process and optional desktop capabilities through existing Mastermind owners. Treat conversations as replaceable working memory and the responsibility/evidence/execution fabric as persistent.

The Chairman has also selected the fastest useful near-term product slice:

```text
authorized Web CEO
→ list/read one real Mac Studio scratch file
→ create/edit/read back one disposable file
→ run one real short shell command with exit 0
→ run one intentional exit 7 and preserve it accurately
→ refuse one out-of-scope file
→ disconnect/revoke the pilot
→ prove the existing Executive connection is unchanged
```

This `WEB_CEO_FILE_SHELL_FS0` priority supersedes the earlier sequencing statement that every Workbench file/process slice must wait for the Business Steward read canary. The two are now **parallel, independently gated verticals**. Neither may create a duplicate lifecycle, permission store, process owner, tunnel, app, backend, retry plane or source carrier.

## Binding product rulings

### 1. Do not install Chat On Steroids defaults into a primary authenticated Chairman environment

Approve only pinned-source, disposable, non-sensitive pattern evaluation. Do not adopt its prime/worker/session registry, local transcript history, automatic compaction or goal loop as Mastermind lifecycle, memory, authority or continuation.

Selective adoption remains eligible for:

- batched reads and bounded continuations;
- structured patch ergonomics;
- durable process handles;
- separate Core/Desktop discovery surfaces;
- invocation-time permission enforcement;
- observation-bound desktop actions;
- explicit partial and effect-unknown results.

FS0 must reuse one already-installed suitable backend when proven or select one pinned backend. Do not build both OpenClaw and DesktopCommander adapters. Do not use `@latest`, vendor remote pairing, telemetry-default acceptance, unrestricted shell-as-sandbox claims, credential access or global configuration overwrite.

### 2. Preserve one experience with existing federated authority

No new Mastermind control plane is approved.

- Executive OS owns lifecycle and CEO admission.
- Agent OS owns durable organizational work.
- RuntimeBinding/Capacity/Wake own exact surface, placement and attention.
- GitHub owns source and immutable evidence.
- Steward/Control Room compose company reads.
- Code Intelligence owns governed global discovery and exact-worktree semantics.
- Company Dialogue owns bounded worker returns.
- Web-Sol and Worker Browser own exact browser/computer actuation.
- Existing local executor and Integration owners retain file/process/tunnel identity for FS0.

The proposed Workbench is a facade over an existing exact Attempt/provider or attended pilot context. It is not a lifecycle, process manager, permission registry, shell authority service or evidence database.

### 3. Separate leader and operator capability profiles

**Leader:** current procedure, company context, evidence, bounded root admission, child state, material returns, review and closeout. No default broad filesystem, shell, desktop, clipboard, credential, account or installation authority.

**Operator:** exact host-injected operation/Job/Attempt/worker/workspace/source/path/network/secret binding. May receive bounded file/process tools and, later, optional disposable desktop tools. The model cannot choose operation identity, privileged actor, host, root, source ref, account, profile, capability generation or effect state.

**FS0 attended pilot:** a deliberately narrow Web-CEO surface over one disposable allowlisted directory and bounded command policy. `ATTENDED_ONLY` is not autonomous fleet authority. A directory allowlist is not shell containment; command policy, environment, timeout, output boundary, process identity and reconciliation are separate controls.

### 4. Every mutator requires pre-effect identity and loss-safe reconciliation

Canonical `operation_key` is host-derived and never caller/model-selected. A non-authoritative client correlation value may not choose authority, owner, idempotency or reconciliation scope.

Every proposed modifying action must:

1. receive an owner-issued immutable action reference before effect;
2. bind that reference to the exact operation, Attempt/capability generation, target, payload digest, preconditions and expiry;
3. consume it at most once;
4. expose read-only reconciliation through the same pre-effect reference;
5. return the one canonical prior result or `EFFECT_UNKNOWN` after duplicate delivery;
6. block retry and provider/account/backend/carrier failover while effect remains unknown.

This applies to workspace patch, process launch, process input, cancellation and desktop action. A general process read is not sufficient to reconcile a launch whose response was lost before the caller learned `process_ref`.

### 5. Continue the Business Steward read path in parallel

The first independently useful **company-read** capability remains:

```text
one selected Business cockpit
→ current reviewed package/app generation
→ authenticated source-attributed Steward read
→ explicit degraded-state behavior
→ another successful read after access-token expiry/refresh
→ rollback/readback
→ two Personal-Pro controls unchanged
```

Protected PR #500 now provides U1 binding/enrollment **source tooling only**. Its truthful state is:

`SOURCE_PROTECTED / U1_BINDING_AND_ENROLLMENT_TOOLING / PRODUCTION_INERT`

Do not commission another U1 source-publication worker. Remaining gates are current-base resolution of the truthful partial Steward S1 carrier PR #463, immutable package/app generation, actual publication/import/install, endpoint and OAuth/resource/subject/client configuration, one useful cockpit read, post-expiry refresh, independent evidence acceptance and rollback/readback.

Future desktop/fleet/autonomous-write proof must not become an invented prerequisite for this bounded read-only step. Conversely, Steward completion does not prove FS0, local shell, exact wake or stale-session fencing.

### 6. Personal Pro receives leadership parity through governed delegation

Personal Pro does not need unsupported custom-MCP writes to lead difficult programs. Preserve the accepted route through protected source, approved reads, supported native action, dedicated Relay/CeoIngress, Executive OS, exact external workers, GitHub evidence, Dialogue returns and Web-Sol continuation.

Do not disguise mutation as read/fetch. The parity target is project leadership and acceptance, not identical direct-computer entitlement.

### 7. All autonomous-action claims must cover every reachable mutator

A stale chat refused at a Mastermind endpoint may still possess an independently authorized native GitHub, Slack, Linear, shell, provider or desktop path. Therefore classify every reachable route for the claimed configuration as:

- `ENFORCED`;
- `DENIED`;
- `ATTENDED_ONLY`, a supported human-confirmed route excluded from the autonomous claim; or
- `UNKNOWN`, which blocks the stronger claim.

App registration metadata, installation scope, access-token scope, repository selection, ChatGPT product action availability, approval behavior and organizational authority are separate evidence layers. Approval prompts are not technical permission revocation. Never prove this by attempting an unauthorized production action.

### 8. Long-running execution and exact ChatGPT wake remain different claims

A worker/process may continue after a reasoning turn. A client may reconnect to a result. Neither proves that the exact inactive ChatGPT leader can be awakened. Preserve durable execution in existing providers/Attempts and return through existing Dialogue/Wake. Exact ChatGPT continuation/rotation remains separately gated by semantic readiness and RuntimeBinding succession.

### 9. Spend Pro turns on frontier judgment, not polling or mechanical labor

Use the current protected Pro-mode exception law. High-value uses include cross-system architecture, hard diagnosis, adversarial judgment and long-horizon synthesis. Handoffs, ACK/START/STOP, polling, monitoring, routing, simple tests and mechanical edits remain non-Pro work.

Optimize accepted capabilities per scarce reasoning turn, recovery quality, intervention rate, duplicate/unknown effects and real consumer completion—not elapsed turn duration or a raw zero-error metric.

## Current parallel release lanes

### Lane A — `WEB_CEO_FILE_SHELL_FS0` (Chairman-selected quickest useful slice)

1. Existing Personal-MCP Cockpit/Integration owner reconciles whether FS0 or an equivalent child/effect already exists.
2. Only a positive no-existing-effect result permits one concrete least-scarce executor to be bound on the canonical owner-selected child carrier.
3. Prefer Terra for bounded engineering; use CTO Sol only for the authority-sensitive seam. Do not consume a new Fable principal for a frozen single-host vertical.
4. Reuse one suitable existing local executor or select one pinned backend; no dual implementation.
5. Prove fail-closed absent/malformed configuration, traversal/symlink denial, stale preimage refusal, bounded output, timeout versus completion, and no blind retry.
6. Run real Mac proof through the supported private MCP/tunnel path: list/read, create/edit/readback, exit 0, exit 7, out-of-scope refusal.
7. Disconnect/revoke the pilot and prove the existing Executive connection remains unchanged.
8. Publish exact source/config/package/runtime/proof receipts and keep capability `NOT_PROVEN_LIVE` until the real producer-to-consumer path succeeds.

### Lane B — one authenticated Business Steward cockpit

1. Resolve PR #463 on current protected source without reviving its terminal builder.
2. Derive immutable package/app material through protected U1 tooling.
3. Publish/import/install under separate action-time gates.
4. Configure endpoint and OAuth/resource/subject/client identity.
5. Produce useful nonempty source-attributed reads and honest `DEGRADED` states.
6. Repeat after access-token expiry/refresh.
7. Obtain required independent evidence acceptance.
8. Roll back/revoke/read back while two Personal-Pro controls remain unchanged.

### Later lanes — separately admitted

1. exact synthetic repository/worktree read;
2. prepared patch and reviewed validation recipe with loss-response reconciliation;
3. durable result return and exact wake/unavailable proof;
4. disposable observation-bound desktop action;
5. exact succession with semantic readiness and stale-route coverage;
6. one consequential full program reaching a real product/machine consumer and durable closeout.

No worker receives “build the whole workbench.”

## Current evidence and gates — superseding recovery correction

This section supersedes older “current,” “next action,” and recovery language in this memo while preserving those statements as dated history.

- Protected Mastermind at the start of this correction was `467a81e84b08a7f1c3cdb9a410b2f7857816675d`, compatible Skillpack 1.0.1/bootstrap-major 1.
- PR #490 remains Draft/Hold. Author-side semantic review comment `5558973712` preserves the product direction but requested bounded repairs; it is not independent acceptance.
- The Workbench contract is now revision 2 in this branch: operation identity is host-derived; every mutator has a pre-effect action reference and dedicated reconciliation; reads and modifying effects are distinct; source selection is host-bound; `ATTENDED_ONLY` is normalized. This is still `SPEC_ONLY`.
- The 24-task artifact is now explicitly `EVALUATION_TEMPLATE_NOT_PREREGISTERED_NOT_EXECUTABLE`. It preserves all 24 tasks but freezes no fixture pairs, answer keys, assignment, graders, denominators, exclusions, statistics, budget, authorization or owner acceptance. No task may be run from this carrier.
- Connector issue comment `5552174789` proves one real GitHub issue-comment write through the observed path and exposes app registration/configuration metadata. It does not prove the complete effective installation/token grant, repository selection, every product-exposed action or stale-session reachability.
- The old records lookup `chatgpt-pro-long-run-evidence-reconciliation-20260905-sol-001`, root `C0BSBM78V1N/1788668858.166259`, never received ACK/START/RESULT and was terminally closed on the same thread at `1788694916.617049`. Do not pick it up late or create a replacement. Historical canary/reviewer/artifact claims remain `DELIVERY_UNVERIFIED / NOT_RECOVERED`, not proven nonexistent.
- PR #490 comment `5558890710` records the FS0 plan, a 216-line implementation plan and a disposable helper with an observed 22-test red run followed by 22 passing tests. No Mac/tunnel/app/permission/production proof, source branch or implementation PR was created by that work.
- The existing FS0 owner path is `personal-mcp-cockpit-integration-20260906-sol-001`, carrier `D0BTAKPHX8S/1788689346.571769`, Cockpit task `01a06f73-d4cf-7933-8268-d3c9644bc63d`, Integration task `01a06f72-aaae-77f1-a3fb-28f5d05c107a`. The proposed child `web-ceo-workbench-fs0-20260906-sol-001` remained `NOT_SUBMITTED` at the last verified evidence.
- A transport-only continuation pointer was posted and read back at `C0BSBM78V1N/1788695066.321929`. Its effect is `NONE`; it is not child creation, ACK, START, source ownership or execution.
- PR #463 remains the separate truthful partial Steward S1 carrier. Its old builder is terminal and must not be revived. Current-base composition, checks/review and release remain with existing Integration.
- Protected PR #500 eliminates “finish U1 source publication” as a current action. U1 is protected tooling but still production-inert.

## Explicitly rejected

- one giant Mastermind super-MCP;
- unrestricted logged-in-user shell for an unattended leader;
- default desktop/clipboard access;
- plugin-owned task, process, session, transcript, permission or retry stores;
- title/newest-tab identity;
- automatic account hopping after START;
- automatic context compaction as authority transfer;
- treating an MCP task handle or Slack delivery as exact ChatGPT wake;
- primary-host installation of an unsigned or unreviewed third-party binary;
- model-selected operation, actor, root, source ref, account, profile or effect state;
- blind replay after a possibly applied file/process/input/desktop effect;
- calling source merge, green CI, tool presence, package installation or rendered output production acceptance.

## Approval effect

Chairman direction determines product priority. It does **not** by itself merge PR #490, accept its architecture, install third-party software, publish an app, create an Executive operation, assign a worker, widen a runtime schema, change account permissions, mutate a browser profile, deploy a service or prove production.

**Exact next action:** the already-existing Personal-MCP Cockpit/Integration owner reconciles its canonical carrier and either returns a typed terminal/inaccessible/effect-unknown receipt or binds exactly one executor and originates the lawful FS0 child. In parallel, existing Steward/Integration owners continue the independently gated one-cockpit read path. PR #490 remains Draft/Hold until repaired current-head records receive independent architecture review.
