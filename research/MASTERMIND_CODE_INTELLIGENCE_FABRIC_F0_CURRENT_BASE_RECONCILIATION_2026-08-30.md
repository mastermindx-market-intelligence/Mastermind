# Mastermind Code Intelligence Fabric — F0 Current-Base Reconciliation

**Status:** `SPEC_ONLY / RECORDS_ONLY`  
**Parent operation:** `mastermind-codeintel-fabric-20260830-sol-pro-001`  
**F0 carrier:** Mastermind PR #276 / `sol/code-intelligence-fabric-f0-20260830`  
**Initial architecture source window:** protected `Mastermind@8f0babf473e6e4e8efce697014bd48c594227d94`, `macro@ca31568272d06f2472f65946d2435517611f31dc`  
**Current protected release base:** `Mastermind@28d365cceaef6efb0a26e0ac9af51ead44695d60`  
**Current Macro observation:** `macro@ede7e065a90b294e9835e98e5326a84e1c14d038`

This record reconciles F0 with protected Mastermind movement and one machine-experience omission found during exact-head release review. It creates no implementation, MCP server, semantic backend, Zoekt index, capability profile, worker, Attempt, RuntimeBinding, SessionTarget, Sol action target, host process, credential, service, Slack/Linear projection, Control Room component or production capability.

For current release-base identity, the intervening exact Sol action-authority boundary and the optional Control Room projection, this record is more recent and specific than the historical pin headers in the F0 architecture, benchmark charter, supply-chain amendment and C0/Z0 plans. It does not replace their product architecture, tool contracts, benchmark law, rights law or wave boundaries.

## 1. Current Skillpack compatibility

The current protected Skillpack was atomically reloaded from `Mastermind@28d365cceaef6efb0a26e0ac9af51ead44695d60`:

```text
schema = mastermind.sol_skillpack.v1
skillpack_version = 1.0.1
minimum_bootstrap_major = 1
bootstrap_major = 1
```

The required `INDEX`, `COLD_START`, `RECONCILE_STATE`, `REVIEW_RETURN` and `CLOSEOUT` sources remain byte-identical to the initially loaded compatible procedures. Modifying release review remains available under the current protected procedure.

## 2. Exact protected movement

Protected Mastermind advanced by one commit from `8f0babf473e6e4e8efce697014bd48c594227d94` to `28d365cceaef6efb0a26e0ac9af51ead44695d60`.

The exact changed-file set is:

```text
A control_plane/sol_action_target.py
A tests/test_sol_action_target.py
```

No Skillpack, capability-registry, Codex Operator Adapter, workspace-binding, F0 record, benchmark, C0-plan, Z0-plan or supply-chain file changed.

## 3. Material architecture ruling

The new `mastermind.sol_action_target.v1` resolver composes two existing authorities without persisting either one:

```text
root-job CEO SessionTargetRegistry alias
+ complete current RuntimeBinding snapshot
-> one exact action-authoritative Sol target or typed fail-closed state
```

It accepts no Slack principal, provider/model claim, title, recency, health, caller-supplied alias or inferred sister Sol. Missing, unknown, contradictory or duplicate exact target evidence does not elect another actor. The current implementation is `BUILT_NOT_PROVEN / PRODUCTION_INERT`; target transfer and production enforcement remain separate gates.

This strengthens the F0 no-rebuild boundary:

- Code Intelligence Fabric is evidence-only and never elects an action-authoritative Sol;
- CodeIntel never creates, persists, transfers or repairs `SessionTargetRegistry` or `RuntimeBinding` state;
- CodeIntel never uses repository/worktree recency, semantic-engine availability, provider identity or model context to choose a Sol session;
- an exact CodeIntel workspace binding proves only which source tree a current Attempt-local analyzer may read; it grants no Sol action authority;
- future CI1/CI4 integration must consume current exact Attempt/workspace and capability evidence only after the existing action-authority/runtime gates select the acting session where those gates apply.

The new source therefore does not invalidate the two-plane architecture. It adds a current canonical owner that future CodeIntel integration must not duplicate.

## 4. Machine-first experience and Control Room boundary

The initial customer remains the authorized machine workforce. C0 through CI5 must prove the worker and reviewer journeys before any human-facing surface becomes a rollout dependency.

A later Control Room projection is optional and may be admitted only after benchmark evidence shows an independently useful operator job. Its V1 affordances are bounded to evidence already produced by existing owners, such as:

```text
implementation repository
exact relevant source SHA
CodeIntel freshness/coverage state
open exact code or PR evidence
find owning symbol
link a runtime diagnostic span to its attested code revision
```

The projection must preserve these boundaries:

- Control Room displays or invokes a bounded read journey; it does not become the CodeIntel index, semantic backend, source-of-truth store, workspace registry or query administrator;
- no model or human UI control selects an arbitrary filesystem root, worker worktree, Attempt, endpoint, executable or Serena project;
- no code editing, shell, merge, release, index administration, repository enrollment, credential management or lifecycle transition enters the surface;
- exact source SHA, freshness, coverage, truncation, local-candidate-versus-remote state and canonical-verification requirement remain visible rather than collapsed into a green badge;
- the existing Control Room product and authority architecture remains the owner of its UI, data acquisition and failure-state presentation;
- Sourcebot or another browser may be evaluated separately for a real human browsing job, but Control Room is explicitly **not** to become a Sourcegraph/Sourcebot clone.

A Control Room wave requires its own future bounded commission, real-data reference composition, empty/loading/stale/degraded/unauthorized/failure states, browser proof at relevant breakpoints and a named user capability. F0 creates no such wave and makes no UI live.

## 5. Macro movement reconciliation

Macro advanced from the F0 source window `ca31568272d06f2472f65946d2435517611f31dc` to current observation `ede7e065a90b294e9835e98e5326a84e1c14d038` through two commits whose changed paths are confined to rendered `site/**` output.

The Agent OS workstreams, decisions, discoveries and handoffs read for F0 are unchanged. The original exact Macro source window remains the reproducible archaeology pin; current Macro main is recorded separately rather than pretending rendered publication movement changed organizational truth.

## 6. Collision and release consequence

The protected source movement is adjacent but path-disjoint from PR #276. It adds no conflicting CodeIntel implementation or program carrier. Current Linear and Slack duplicate sweeps remain empty for Code Intelligence / Serena / Zoekt, and the open-PR collision review shows no other carrier changing the F0 records paths.

PR #276 incorporated protected `28d365cceaef6efb0a26e0ac9af51ead44695d60` into the same branch carrier before this amendment so hosted CI evaluates the actual combined tree. No force update, replacement PR or cross-carrier retry is permitted.

After this amendment, release requires:

1. fresh exact-head CI success;
2. fresh protected-base-to-head census containing exactly the six F0 records files;
3. no implementation/runtime/config/host changes from this carrier;
4. mergeability and expected-head guarded merge;
5. post-merge protected SHA verification;
6. durable Macro Agent OS workstream/decision/discovery/handoff referencing the exact accepted Mastermind merge SHA.

## 7. Capability truth after this reconciliation

```text
F0 architecture and reconciliation records       SPEC_ONLY / RECORDS_ONLY
Code Intelligence Fabric runtime                 NOT_BUILT
C0 semantic falsifier                            NOT_BUILT
Z0 discovery falsifier                           NOT_BUILT
current Sol action-target resolver                BUILT_NOT_PROVEN / PRODUCTION_INERT
CodeIntel Sol/session/workspace election          REJECTED_BY_DESIGN
Control Room CodeIntel projection                 NOT_BUILT / OPTIONAL AFTER PROOF
Control Room as code-browser clone                REJECTED_BY_DESIGN
```

## 8. Exact next action

Run and review exact-head CI and the six-file current-base census on the amended existing PR #276 carrier. Only a clean PASS permits expected-head merge of the records-only F0 carrier. After accepted merge, create the durable Agent OS records, then place C0 and Z0 through current capacity procedure without turning missing placement into Chairman labor.