# Personal-Pro Mastermind Executive surface convergence

Date: 2026-09-05. Author: Sol / Astra, current Chairman-delivered program assignment.

**Capability: SPEC_ONLY for this architecture/proof packet. Program: PARTIAL. Personal-Pro production parity is not accepted.** This is a records-only study and evidence-gated architecture freeze candidate, not implementation or a production release. It preserves existing MAS-48, Executive, Agent OS and Business surface owners. No new runtime program row, queue or lifecycle is created.

## 1. Outcome and source pin

Personal Pro is the primary CEO cognition and routine operating surface. Chris should recover company state, commission bounded work, inspect canonical receipts and returns, review implementation, and continue/stop work without routine Business switching. Business remains only where an actual account/admin/direct-MCP/break-glass capability requires it. Larger Personal-Pro reasoning capacity is the Chairman's economic input, not a quota measurement made in this study.

The investigation independently fetched protected Mastermind `master` and rechecked it before source recording:

- Mastermind: `b0f85b0f1c66825ecec4bf6ce32dcbfac0c14b93`.
- Skillpack: `mastermind.sol_skillpack.v1`, version `1.0.1`, minimum bootstrap major `1`; compatible.
- Macro organizational snapshot: `8eabf462ddadbc92a81dc6a9f92202602a4d88d4`.

INDEX, COLD_START, RECONCILE_STATE, COMMISSION_WAVE, WORKER_AVENUE_ROUTING and CLOSEOUT, plus the routing addendum and dialogue law, were read from that same Mastermind commit. Re-pin before a later modifying edge; these hashes are an observation, not a permanent current pointer.

Precedence remains current Chairman intent, current protected procedure/constitutional owners, accepted component contracts, then this bounded proposal and retrieved evidence. An unmerged record cannot release C1, S0-R1, B2, C2, a host, a credential or a production write.

## 2. Findings that change the next action

### 2.1 Do not infer Personal-Pro write restrictions from one document

Current official Developer documentation includes Pro in full read/write MCP eligibility. The Help Center still says Pro custom MCP is read/fetch only. These are conflicting official descriptions, not an account result. Preserve the handoff's A/B/C/D test; do not preselect Slack as permanently necessary. Developer-mode permissions and a scanned modifying tool are not production authority. [O1, O2]

### 2.2 The existing MCP is the first candidate, not a new backend

Protected `integrations/executive_mcp/{server,adapter,schemas}.py` and `scripts/executive_mcp.py` already own exactly:

| Tool | Declared purpose |
|---|---|
| `executive_state` | Read |
| `executive_inbox` | Read |
| `executive_job` | Read |
| `ceo_intent_status` | Read |
| `submit_ceo_intent` | Modify |

The server advertises the same five-tool manifest in readonly mode. The gateway, not a client hint, refuses production submission with `production_write_disabled`. The CLI supports readonly and fixture modes, not a production-write arm. Therefore Pro receiving all five tools would neither require a manifest rewrite nor make production CEO submission available.

### 2.3 A connected MCP may still read the wrong runtime

At the pinned source, readonly `GatewayConfig.runtime_root` defaults to the selected repository root. Rich state/inbox also compose a boot packet from repository/Macro roots. The installed Executive runtime has a separately owned root. The current host mapping must be positively proved; this source observation alone does not prove the deployed host is misconfigured.

A fresh response timestamp is not proof of current backend data. The read canary must bind source SHA, server mode, schema generation, canonical runtime/root mapping, independently current grounding and real existing Job/intent identities. Never create an empty runtime/database to make a read appear successful. Missing, stale, fixture or permission-denied state must remain visible.

### 2.4 Rich orientation is explicitly not admission hot state

The protected `EXECUTIVE_OS_PERSONAL_PRO_RELAY_STATE_TRANSPORT_AMENDMENT_2026-08-20.md` already explains this distinction. Rich MCP orientation can include Agent OS prose and slower collection; the dedicated hot-state projection supplies service/admission/runtime/transport readiness without those dependencies.

Consequently, a working `executive_state` call alone cannot remove `SOL_STATE` from write preflight. Any replacement must supply the accepted grounding, generation, admission, freshness, degradation and transport-reconciliation semantics. The accepted amendment's initial maximum state age for a new write is 120 seconds; this study does not widen it or replace current executable validation with the conceptual example.

### 2.5 The existing ready tunnel was a fixture, not production proof

The complete accepted BSC host carrier `C0BSBM78V1N/1788507455.090679` records a September 4 census: profile `mastermind-executive-canary` was healthy/ready but served fixture-mode Executive MCP from checkout `7191702e`, not then-current protected source. That operation was stopped at `1788520565.242799`; its evidence ceiling was loopback/test-issuer read proof, not a live ChatGPT-to-production journey.

This is a historical host receipt, not a new September 5 host observation. Do not repoint, restart or reuse its identity without ownership/generation reconciliation. Current OpenAI guidance permits one tunnel to be associated with multiple organizations/workspaces; diagnose association visibility before proposing another tunnel. Tunnel permissions and ChatGPT developer-mode access are separate. [O3]

### 2.6 BSC-E1 is reusable but is not already an HTTP MCP server

PR #363 merged as `24fa9bc4acfbffb77f09193dd50d1ee8f90bcbf8`. The authenticated stateless Executive app reuses `ExecutiveMcpGateway` for reads and dedicated CeoIngress for its intended submit path. Its current transport is REST ASGI, not MCP JSON-RPC.

PR #469 is OPEN/DRAFT at `f6e17a949579191c631a6edce8280de468c026aa`, tree `93b9d1760e5c6ab220df9e84aae82bcb0f9da798`. It adds protected-resource metadata, not an MCP adapter or production deployment. A fresh GitHub review-list read returned no submitted reviews. Its original source writer was released by a separate cleanup receipt; that does not grant this program branch ownership or merge authority.

Keep #469 and the BSC owners intact. Do not make Business Auth0/Steward packaging a predecessor of the simpler stdio Personal-Pro read test. If the HTTP app route is later necessary, reuse the existing authenticated app and schemas; do not pretend REST is MCP or create another API policy/backend.

### 2.7 Personal plan privacy needs explicit account qualification

Personal-account model-improvement controls differ from Business defaults. Verify the intended Personal account's data controls and the Project's information-clearance boundary before routine company-data operation; do not assume Business defaults transfer. Permission settings govern approval behavior, not provider rights or source access. No privacy, app-permission or sharing setting was changed here. [O4, O5]

## 3. Current capability ledger and unresolved effects

| Capability | State | Evidence ceiling / exact remaining proof |
|---|---|---|
| Fresh protected Skillpack and GitHub source recovery in this session | PROVEN_LIVE | Real connected reads at the exact pin; no Executive state claim. |
| Organizational recovery via Macro Agent OS files | PROVEN_LIVE | Read existing workstream/decision/discovery records; no new Agent OS backend needed. |
| Native Slack read, ordinary handoff send and readback | PROVEN_LIVE | New census carrier below; not a framed CEO command or receiver pickup. |
| Existing five-tool Executive MCP implementation | BUILT_NOT_PROVEN | Source exists; actual Personal-Pro production reads not tested. |
| Current-session direct Executive tool access | DARK_OR_DISCONNECTED | No Executive tools exposed; directory lookup returned no matching Mastermind plugin. This is not proof no private app exists elsewhere. |
| PR-A dedicated CeoIngress implementation | BUILT_NOT_PROVEN | #100 protected; local/hermetic admission is not production networking. |
| B1 hot-state implementation | BUILT_NOT_PROVEN | MAS-108 records #106/#114 merged and implementation complete. Do not rebuild it. |
| C1 outbound production source | BUILT_NOT_PROVEN | #155 protected; original Step-D effect remains unknown. |
| Visible production Slack hot-state read lane | DARK_OR_DISCONNECTED | Complete current #sol-runtime history returned no `MMX/SOL_STATE_V1` message. |
| BSC-E1 authenticated production app journey | BUILT_NOT_PROVEN | Accepted loopback/test-issuer evidence only; no live account/backend parity. |
| Final S0-R1 three-seat experiment | NOT_BUILT | Native grant remediation and fresh verifier qualification remain required. |
| B2 inbound production Slack transport | NOT_BUILT | Held behind actual accepted predecessors and explicit release. |
| C2 production CEO-intent canary | NOT_BUILT | Held; no canonical production Job or receipt was created here. |
| Personal-Pro routine end-to-end CEO workflow | PARTIAL | SaaS orientation/coordination works; canonical runtime/control/return proof remains missing. |
| Second Executive/Slack queue or retry database | REJECTED_BY_DESIGN | No implementation commissioned. |

### C1: recovered, not reconciled

Operation: `autonomy-c1-native-production-proof-20260827-sol-001`.
Carrier: `C0BSBM78V1N/1787889177.672699`.
Original receiver: Claude4 and its original RuntimeBinding.

The complete thread has no conclusive Step-D return. MAS-109 says `EFFECT_UNKNOWN / SAME_RUNTIME_RECONCILIATION_REQUIRED`. Steps B/C had host effects. No new receiver may infer Step D executed or did not execute, rerun preparation/install, re-enroll credentials, publish state, or create a replacement C1 operation.

Current complete channel read found no state publication. Current member census returned Chris, ChatGPT1/2/3, the Executive Relay and Secretary. Those facts neither settle Step-D host effects nor authorize membership changes. Apply current Chairman membership rulings rather than resurrecting an obsolete rights-drift blocker.

### S0-R1: exact existing authorization, not the ChatGPT connector

MAS-112's latest record and `DSC-PERSONAL-PRO-INGRESS-PRINCIPAL-GAP` identify the remaining gate as an over-granted installed fixture token, despite narrowed requested scopes. Required bot scopes are exactly `chat:write` and `groups:history` for the same fixture app `A0BS2DMVDC4`, bot `U0BST4WG996`, workspace `T0BRD2AQXQV`, channel `C0BRUL9F2V7`.

The existing native sequence is revoke the old fixture grant, authorize the SAME fixture app with reduced scopes, update the protected credential through the native secret-safe boundary if needed, then STOP. Fresh verifier qualification precedes the sole final three-seat S0-R1 proof. Do not uninstall the native ChatGPT Slack connector, weaken the verifier, invent S0-R2, or post another executable probe now.

The grammar remains the exact first-line discriminator, one canonical single-line JSON object, and only the reviewed optional platform-attribution trailer. A successful ordinary Slack handoff is not S0-R1 proof.

## 4. Linear and ownership reconciliation

| Record | Current read | Disposition |
|---|---|---|
| MAS-48 | In Progress / PARTIAL, but body says B1 Todo and C1 not built | Parent description is stale against child/source evidence. Correct projection without changing release gates. |
| MAS-75 | Done / BUILT_NOT_PROVEN, #100 protected | Completed implementation; its old next-action R0 is historical. |
| MAS-105 | Done / SPEC_ONLY, accepted #99 architecture | Do not reopen original F0 or imply its completion proved production. |
| MAS-108 | Done / BUILT_NOT_PROVEN, #106/#114 protected | C1, not another B1 build, owns next production-read work. |
| MAS-109 | In Progress / BUILT_NOT_PROVEN, effect unknown | Original binding/carrier reconciliation still owed. |
| MAS-112 | Todo; final proof not run | Native fixture grant then verifier then S0-R1. Older Agent OS prose saying In Progress is historical projection. |
| MAS-102 | Todo / held | B2 is not released. |
| MAS-101 | Todo / held | C2 is not released. |

Organizational continuity stays in Macro `agentos/`. Existing Personal-Pro decisions affect `WS:AGENT-OS`; BSC/Control Room owners retain their own scope. Do not overwrite that workstream's unrelated MAS-28 next action or create a semantic program mapping merely for this study.

The scoped collision census includes protected #99/#100/#106/#114/#155/#363, current #469, its existing review/cleanup carriers, the BSC host and Business browser carriers, and related open BSC/host-context/continuity source families. It is not a claim that every repository PR or current host worktree was independently audited. Before any code wave, repeat a path-level and actual-host occupancy census.

## 5. Frozen boundaries and evidence-gated route selection

The invariant architecture is:

```text
Personal-Pro Sol: primary cognition and user interaction
  -> GitHub: source and implementation evidence
  -> Macro Agent OS: organizational decisions/discoveries/handoffs
  -> Executive read edge: canonical runtime orientation, with explicit freshness
  -> one selected authorized modifying carrier
     -> existing dedicated CeoIngress
     -> existing Executive OS admission/lifecycle
  <- canonical receipt/status and existing worker-return/dialogue owners

Business: only verified administration, exceptional direct-MCP tests and break-glass
Linear: selected projection
Slack: transport and hot-state visibility, never the queue
```

**First read-edge candidate is A: existing stdio MCP through Secure MCP Tunnel.** B, the authenticated Executive app adapted to actual MCP transport, is fallback reuse only when justified. C, a thin read-only manifest projection, is permitted only if a controlled test attributes whole-server refusal to the modifying tool. Neither fallback gets a backend/store of its own.

The read canary has separate axes:

1. Client entitlement/manifest behavior: A/B/C/D below.
2. Server generation/mode and canonical-root fidelity.
3. Runtime/admission and transport freshness coverage.
4. Account/workspace information clearance and approval behavior.

| Client result | Next decision |
|---|---|
| A: unchanged manifest permits four reads; write not exercised | Reuse for qualified reads. No write-entitlement conclusion. |
| B: platform blocks modify invocation | Reuse reads; retain the gated Slack-write candidate. Distinguish platform refusal from gateway refusal. |
| C: controlled evidence proves the mixed manifest causes whole-server rejection | Smallest read-only projection of the SAME schemas/gateway; first exclude association, auth, tunnel, SDK and malformed-schema failures. |
| D: actual account permits a modifying invocation | Prove inert/fixture behavior and confirmations first. Existing server still has no production-write arm. Formally compare a bounded CeoIngress-backed adaptation with B2 before selecting normal production transport. |

A readonly `production_write_disabled` response, if separately authorized and observed, can demonstrate client-to-server reachability of a write tool; it cannot prove a successful modifying workflow or production authority. Never mislabel that server refusal as a Pro plan refusal.

**Read-plane ruling:** direct qualified MCP is the candidate primary rich orientation path. Retain current `SOL_STATE` write-preflight law until an accepted transport-neutral equivalent is proved and a protected amendment changes that law. Later, Slack state may become hot-health/fallback telemetry; it must not silently become write-ready during MCP failure.

**Write-plane ruling:** Slack is a conditional candidate, not a permanent platform fact. If D and canonical production authority are proved, propose retirement of unnecessary normal Slack admission complexity under the existing owners. Do not keep two automatically interchangeable armed paths. Old C1 effects still require reconciliation even if a future architecture retires its product role.

No second Job/Attempt/Worker/Event system, ingress policy, operation namespace, retry ledger, queue, replay DB, session registry, watcher, Agent OS, quota store or public endpoint for convenience. No generic Operator dispatcher from Slack, direct Relay SQLite writes, fake read tool with hidden effects, GitHub command bridge, secret copying, or same-operation carrier failover.

## 6. Bounded execution DAG

Wave labels below are plan labels, not Executive Job identities or new worker claims.

| Wave | One observable mission | Gate / owner / stop |
|---|---|---|
| R0 host census | Current owner/mode/root/workspace and permitted live-read receipt or typed blocker | One read-only carrier commissioned below; no configuration effects. |
| R1 Personal account canary | Actual fresh Personal chat scans unchanged readonly MCP and invokes four reads | After owner/root qualification and native account access; stop with A/B/C/D discrimination and visible receipts. |
| R2 optional minimal read repair | Repair only a positively demonstrated manifest or canonical-read binding defect | Only after R1/root falsifier; existing gateway owner; one useful read vertical, no second backend. |
| C1 reconciliation | Resolve original Step-D effect using original RuntimeBinding/status evidence | Existing operation/carrier only; do not replay. Production continuation needs its own accepted edge. |
| S0 grant qualification | Exact installed fixture grant and fresh verifier result | Existing native credential owner; stop before final experiment. |
| S0-R1 final proof | Exact three-seat framed-carrier/reconnect verdict | Only after verifier PASS; original single final proof law. |
| Route release | Select production modify carrier and protect required amendment | Sol compares empirical MCP result with actual B1/C1/S0 gates; no implementation from assumptions. |
| B2 or justified MCP adaptation | One unarmed transport vertical into existing CeoIngress | Only the selected, explicitly released path; existing auth/schema/identity owners. |
| C2 or its accepted successor | One harmless real admission, duplicate and changed-payload conflict | Exact current production permission; one Job, QUEUED, dispatched=false, Attempt=0, Worker=none. |
| Journey proof | One real benign task through existing execution, return, review and continue/stop | Separate execution authority; reuse RuntimeBinding/Wake/dialogue/worker owners. Admission alone is insufficient. |
| Parity acceptance | Paired 20-row workflow and failure evidence | Actual Personal and Business sessions; source merge is not acceptance. |

Do not block R1 on BSC #469, Steward packaging or Auth0 merely because they block the separate Business browser program. Conversely do not resume Business carrier `C0BSBM78V1N/1788507245.001099`: it remains dependency-held under its current owner, with no live browser/OAuth canary result.

Every later implementation handoff must inherit the full Chairman outcome, exact owner/source precedence, current path/collision census, complete user journey, deterministic versus model responsibility, time/null/correction semantics, failure matrix, ordered sequence, real proof, stop condition and continuation path. Do not commission all parity in one PR.

### Commission actually delivered

- Operation: `personal-pro-executive-read-census-20260905-sol-001`.
- Carrier: `C0BSBM78V1N/1788605608.765019`.
- Addendum: `1788606075.371379`.
- Target: existing Secretary placement service; preferred bounded host-capable CTO Sol avenue after current capacity/native-delivery qualification.
- Scope: current read-only tunnel/root/permission census and permitted live reads; no host, account, source or production modification.
- Status at latest read: `DELIVERY_UNCONSUMED`. Only a Linear bot reply was present; no receiver ACK, START, RuntimeBinding, Job or live host result is claimed.
- This session has no qualified background-watch tool and makes no unattended-supervision claim. Keep the exact root for future reconciliation; do not blindly duplicate delivery.

No B2, C2, credential, tunnel-reconfiguration or production canary was commissioned.

## 7. Personal versus Business acceptance matrix

`BLOCKER` below denotes missing acceptance evidence, not a proved plan limitation. Positive comparison categories are limited to the ordinary shared SaaS workflow; a controlled paired account run remains owed. Current-session observations do not attest another account's configuration.

| # | CEO job | Personal evidence now | Business comparison | Classification |
|---|---|---|---|---|
| 1 | Fresh Sol boot | Current source recovery performed | Same protected bootstrap contract; paired boot not captured | FUNCTIONALLY_EQUIVALENT, procedure scope |
| 2 | Load current Skillpack | Exact protected SHA and same-SHA reads | Same GitHub procedure | FUNCTIONALLY_EQUIVALENT, source scope |
| 3 | Executive state | No direct tool / no live hot-state publication | Loopback/test-issuer only, not account proof | BLOCKER |
| 4 | Executive inbox | No real account invocation | No paired account receipt | BLOCKER |
| 5 | Inspect Job | No canonical live Job read | No paired account receipt | BLOCKER |
| 6 | Inspect intent status | No canonical live intent read | No paired account receipt | BLOCKER |
| 7 | Organizational context | Real Macro Agent OS reads | Same durable source path | FUNCTIONALLY_EQUIVALENT, GitHub read scope |
| 8 | GitHub search/read | Performed | Existing native source path; account comparison owed | FUNCTIONALLY_EQUIVALENT, SaaS scope |
| 9 | PR/CI evidence review | PR/source/review metadata inspected; not an independent approval | Same GitHub evidence owner | FUNCTIONALLY_EQUIVALENT, inspection scope |
| 10 | Slack search/read | Exact complete carriers read | Same transport source | FUNCTIONALLY_EQUIVALENT, SaaS scope |
| 11 | Authorized worker message | Handoff sent/read back; no receiver ACK | Same ordinary Slack path; not CEO admission | FUNCTIONALLY_EQUIVALENT, delivery only |
| 12 | Bounded CEO intent | Not attempted | No live baseline established | BLOCKER |
| 13 | Canonical admission/refusal | Not observed from production | No paired account receipt | BLOCKER |
| 14 | Duplicate reconciliation | Source/fixture semantics only | No real account proof | BLOCKER |
| 15 | Same key / changed payload | Source/fixture semantics only | No real account proof | BLOCKER |
| 16 | Relay restart | Not exercised | No paired proof | BLOCKER |
| 17 | Slack reconnect | Not exercised | No paired proof | BLOCKER |
| 18 | MCP/tunnel outage | Failure behavior specified, not account-tested | No paired proof | BLOCKER |
| 19 | QUEUED versus executing | Contract understood; no real admission canary | No paired proof | BLOCKER |
| 20 | Review / continue / stop | Existing owners identified; no new end-to-end cycle | No paired proof | BLOCKER |

No row is accepted as literal UI identity. No claim is made that Business currently has a fully working Executive baseline. Product parity means the complete job, not equal plugin labels.

## 8. Failure and correction contract

| Failure family | Required visible behavior |
|---|---|
| Mixed manifest rejected; write shown but platform-blocked | Record exact failing layer and tool/server generation. Do not infer C from an auth or network error; never relabel a modify tool as a read. |
| Wrong tunnel association; tunnel disconnected; stale/unavailable MCP | Show unavailable/stale and last verified source/runtime time. No old-green response with a new timestamp, no alternate write carrier. |
| Wrong Slack workspace/app/channel/sender; bot recursion; valid command in a thread reply | Refuse before CeoIngress. Eligibility comes from verified transport facts, never role text in the message. |
| Edit/delete after origin; attribution mutation; oversize; malformed JSON | Re-read source and apply the closed accepted grammar and byte bounds. No fuzzy suffix stripping, repair by inference or silent truncation. |
| Stale grounding; CeoIngress unavailable; canonical refusal | Distinguish diagnostic availability from admission readiness. Refusal/unavailability is not an empty successful queue. |
| Effect unknown; duplicate; changed-payload conflict | Same logical operation and original carrier; canonical exact identity/status reconciliation. No speculative rerun or new key. |
| Receipt-post failure after Executive commit | Preserve the canonical commit/result; repair receipt projection only after reconciliation, never resubmit the business effect. |
| Daemon restart; Socket Mode reconnect; incomplete history | Remain non-ready until accepted bounded reconciliation completes; do not add a local replay DB. |
| Two matching state messages; stale SOL_STATE | Fail closed. Preserve source evidence; do not choose newest or synthesize a fresh green wrapper. |
| Agent OS unavailable; Executive unavailable; source/projection disagreement | Isolate organizational versus runtime degradation. GitHub owns implementation, Executive lifecycle, Agent OS organization; Linear/Slack do not outvote them. |
| Personal works while Business differs, or reverse | Record account/plan/app/schema/generation and failure layer separately. No silent account switch, entitlement assumption or same-operation failover. |

Deterministic code owns identity normalization, parser/auth checks, freshness, canonical admission and idempotency. Sol/model reasoning selects and reviews a bounded mission; it cannot infer missing permissions or convert descriptive state into execution authority. Backend null/missing values remain explicit, not default-green values. Corrections are appended with evidence and supersession scope; historical receipts are not rewritten into current truth.

## 9. Measurements and remaining native actions

Production write latency samples: **n=0**. p50 and p95 are not measured. Do not substitute Slack handoff latency, fixture timings or API estimates.

After the harmless canary is explicitly released, record: tool-call start, Slack acceptance if applicable, event receipt, CeoIngress invocation, canonical receipt creation, visible receipt and Sol readback. Separate client/approval delay, transport delay and backend delay; preserve failures and ambiguity. Use synchronized clocks or same-process monotonic spans and disclose cross-host clock error. Predeclare representative cold/warm/reconnect scenarios and sample counts; report tail latency with the small-sample limitation. Few-seconds-class is a UX objective, not a current result. Existing instrumentation owners retain storage; no new metrics/control database.

Native actions that cannot be represented as completed by this chat:

1. Resolve the existing tunnel owner/generation and canonical read root, then qualify actual Personal account access. If a required organization/workspace association or Tunnels Read+Use permission is absent, its existing native administrator changes only that binding. No new tunnel by default.
2. In that qualified account, Plugins > + > New Plugin > Tunnel selects the existing owner-approved READONLY target; scan unchanged tools, then use a fresh chat for the four reads. Account consent/privacy/Project-clearance settings require their actual native owner. Never connect the historical fixture and call it production.
3. Recover original C1 RuntimeBinding/session evidence and settle Step D without replay. This is not a request to reinstall or use a substitute session.
4. Correct the same S0 fixture's installed grant and protected credential through its existing native secret owner, then stop for verifier qualification. No token enters chat, Slack, GitHub, logs or process arguments.

The BSC Auth0 native setup remains a separate existing owner path, not an automatic Personal-Pro prerequisite. Public plugin publication is a later product/distribution decision: current OpenAI guidance requires a public HTTPS endpoint for public distribution, unlike private tunnel testing. Do not expose the control plane merely to obtain a marketplace label. [O6]

## 10. Return and next action

This study establishes current-source reuse boundaries, real projection disagreements, a gated route decision, explicit falsifiers, a complete parity checklist and one actual read-only diagnostic delivery. It does not supply a live Executive receipt, exact C1 host reconciliation, Personal MCP account result, production latency or final parity.

The exact next action is to consume or canonically reconcile the existing read-census carrier, obtain its current host/root receipt, then run the no-code Personal-Pro MCP canary on the qualified existing read target. Do not start another Executive backend or B2 while those facts are missing.

Durable continuation belongs in Macro Agent OS, with this source record as the detailed artifact. Linear should receive a projection correction, not a new command queue or a false Done state. All later sessions must fresh-read the exact census root and C1/BSC owners before any new edge.

## Sources and reproducible reads

All internal paths below were read at the pins in section 1 unless a separate immutable PR head is named.

- Mastermind `docs/sol_skills/INDEX.md` and the required same-SHA procedures.
- Mastermind `docs/EXECUTIVE_MCP.md`, `scripts/executive_mcp.py`, `integrations/executive_mcp/server.py`, `adapter.py`; `control_plane/executive_runtime.py`.
- Mastermind `research/EXECUTIVE_OS_PERSONAL_PRO_RELAY_STATE_TRANSPORT_AMENDMENT_2026-08-20.md`.
- Mastermind `docs/runbooks/mastermind-executive-app.md`; PR #363; PR #469 head/tree and `list_pull_request_reviews`.
- Macro `agentos/README.md`, `agentos/workstreams/WS-AGENT-OS.md`, `WS-CHAIRMAN-CONTROL-ROOM.md`, `agentos/decisions/DEC-PERSONAL-PRO-NATIVE-WRITE-P0-REQUALIFICATION.md`, `agentos/discoveries/DSC-PERSONAL-PRO-INGRESS-PRINCIPAL-GAP.md`.
- Linear `get_issue`: MAS-48, MAS-75, MAS-101, MAS-102, MAS-105, MAS-108, MAS-109, MAS-112. These are projection reads, not runtime receipts.
- Complete Slack threads: C1 `1787889177.672699`; BSC host `1788507455.090679`; Business account `1788507245.001099`; #469 cleanup `1788583309.533059`; new census `1788605608.765019`, all channel `C0BSBM78V1N`.
- Slack `slack_read_channel(C0BSGABKBFY, limit=30)` returned complete history; `slack_list_channel_members(..., include_bots=true)` returned six members. These observations do not reveal Step-D host effects.

Official OpenAI sources, checked 2026-09-05; disagreement is retained rather than silently reconciled:

- [O1 Developer mode](https://developers.openai.com/api/docs/guides/developer-mode)
- [O2 Help Center MCP availability](https://help.openai.com/en/articles/12584461)
- [O3 Secure MCP Tunnel](https://developers.openai.com/api/docs/guides/secure-mcp-tunnels)
- [O4 App permissions](https://help.openai.com/en/articles/20001495)
- [O5 Personal versus Business training controls](https://help.openai.com/en/articles/8983130-how-does-chatgpt-use-my-data)
- [O6 Connect a plugin to ChatGPT](https://developers.openai.com/plugins/deploy/connect-chatgpt)
