# Rates-aware replay: observed integration boundary

This is implementation evidence for the existing Mastermind decision context and the approved rates-aware leader-pullback workflow. It is not a new program, replay engine, trading authority, or replacement for Macro Agent OS.

## Mission and ownership

Preserve the Chairman's whole journey: regime and policy expectations -> sector leadership -> stock leadership -> anticipated price/time setup -> causal confirmation -> outcomes and learning. The present child closes one information-loss boundary, not that parent mission. Rates & Inflation Command and Transmission retain rates ownership; Sector Intelligence/GMI, Prophet, Technical Opportunity Intelligence, Live Entry Radar, Options Intelligence, and Evaluation OS retain their existing responsibilities.

The Chairman reports that research briefs 05_POLICY_AND_RATES, 06_LEADERSHIP_AND_TIMING, and 07_OPTIONS_AND_INCREMENTAL_VALUE were delivered and the sessions are working. Those reports are not independent Executive START receipts. Results return through the Chairman to the integrating session; no duplicate researchers or replacement commissions were created.

## Exact implementation identity

Operation: `rates-evidence-context-20260917-sol-002`.

Mastermind source and procedure: `b731149296a9d837d426730813f68d5acc6133ac`; Skillpack schema mastermind.sol_skillpack.v1, version 1.0.1, bootstrap major 1.

Managed workspace: `/Volumes/Mastermind/agent-workspaces/web/rates-evidence-context-20260917-sol-002`; branch `sol/web-rates-evidence-context-20260917-sol-002`. The installed mmx-workspace owner returned APPLIED and this exact source identity. A current bounded GitHub collision check examined all 146 open Mastermind PRs and all their file pages; none touched the four initial implementation/test paths.

This child adds `brain/rates_evidence.py` and connects it to `brain/decision_context.py` assemble/build/prompt_summary. It reads the existing vendored `data/rates_command/latest.json`; no provider request, new data writer, portfolio rank, score, risk gate, order, or deployment is added. Five nominal maturities and three source momentum horizons are retained. Existing governor, signals, regime, Neural Web, and aggregate data-quality behavior remains unchanged under the tested fixtures.

## Actual positive evidence

The first test failed because decision_context had no rates_evidence field. After implementation, 51 focused and existing decision-context tests passed. Three further adversarial cases produced the current result: **52 passed, 2 failed**. The newer result supersedes the earlier all-green subset for release readiness.

A real source exercise used Macro `63fb8dd9fa7dbe43c02ca6eac84b22fbbc9706dd`:

- RIC source blob `2cc4c7e61c1125a22821e5bdf445bba7fcb62fb3`, SHA-256 `21fd520953a1a1af6a7858d990f49ec98f3afd8ec62853b1ee8825c422f78af9`.
- Regime source SHA-256 `85c127d5028dde89512936e76871b1a810de2e2a2ce78e85873f8a2a812c7893`.
- Source observation date: 2026-09-16; source producer build: 2026-09-17T09:41:43.670227+00:00.
- The real build(write=False) -> assemble -> prompt_summary path retained all five nominal maturity rows. Timed rows: **zero**.
- At the earlier explicit cutoff 2026-09-16T13:30:00-04:00, all five rows were withheld, with producer_build_after_cutoff recorded.
- The market_view and Neural Web inputs were intentionally absent in this bounded exercise; their existing degraded state was not hidden. This is actual source-input/consumer-code evidence, not a complete live production generation, deployment or profitability test.

Raw market snapshots are private in `/Volumes/Mastermind/research/rates-aware-opportunity-20260917/rates-evidence-context-sol-002`; they are not committed. That directory also contains consumer_proof.json. The projection explicitly says as_observed_replay_certified=false, and real-yield momentum, meeting-specific surprises and intraday rate timing remain unavailable.

## Material negative evidence and source freeze

Two adversarial failures remain in `tests/test_rates_evidence.py`:

1. A source can claim an observation date later than its own availability/build date, yet the current candidate may retain the number when it matches market_asof.
2. A UTC cutoff shortly after midnight is still the prior New York trading date; the current candidate does not enforce that market-date boundary.

The attempted repair request through Remote Desktop Commander was blocked by OpenAI with the exact message: `This tool call was blocked by OpenAI because we couldn't determine the safety status of the request.` No process receipt was issued. A subsequent read on the same Studio confirmed the proposed timezone change was not applied: the projector remained 186 lines without the proposed ZoneInfo import. The held repair was not retried, re-homed, delegated, or applied through another tool.

Freeze that exact modifying action until a genuine tool/permission-state change permits same-carrier reconciliation. Do not remove the failing tests, call the previous 51-test subset current-green, merge, deploy, or ask another worker to apply the blocked repair through another route. Documentation and evidence publication preserve the incomplete candidate; they do not release it.

## Joint replay admission: the actual consequence

Reuse Terminal #601 at `773b16f8e825ab4b39ce3c7a5fa5caf32868d55f`, especially `docs/research/TERMINAL_TACTICAL_D0_EVIDENCE_2026-09-17.md`. Do not repeat its archival census. That evidence records 22 readable non-INTC 5m/1h stores ending September 11, missing September 14-16, and zero legacy observations admitted by its as-observed view because per-bar availability is absent. The eleven 1m static 404s are not proof of absent vendor entitlement. INTC's held operation must remain held. Terminal #595 remains the existing refresh owner.

**No certified joint historical opportunity is established by these inputs.** A September 16 latest rates snapshot cannot be inserted into September 11 stock decisions, and a fresh download cannot become an old receipt. Current dated context, corrected-history exploration, and as-observed replay are different products and must remain separately labeled.

A certified replay request must bind these existing-owner inputs at decision time t:

| Input | Owner | Required join evidence | Present boundary |
|---|---|---|---|
| Nominal/real rate observations | RIC / existing rate collectors | Instrument and units, observation interval, first availability, version, compatible horizon | Current nominal context is inspectable; receipt/vintage history is not certified here |
| Policy expectations | Existing Fed-path/rate-futures owner | Dated individual contracts, reference periods, meeting calendar alignment, pre/post observation and knowledge times | Daily interpolated latest display is not a meeting-surprise service |
| Equity bars | Existing Terminal data owner / #595 / #601 | Symbol, corporate-action basis, grain, completed interval, session, first availability, correction version | D0 reports missing recent dates and missing historical receipts |
| Leadership cohort | Existing Sector/Prophet owners | Membership and selection issued before the opportunity, method/version and cutoff | Await the independently commissioned leadership research; no retrospective winner list is substituted |
| Options evidence | Existing flow/package/positioning owners | Package state, signed evidence quality, expiry/OI knowledge time and data rights | Await the options research and existing owner qualification; no synthetic dealer position |

The join uses existing cutoff and study owners; no new generic event store or evaluator is introduced. For a slower daily field, an explicitly approved age window can make it context for a faster decision, but cannot turn it into an intraday observation. Null is unknown, not neutral, and an absent optional source cannot silently become agreement. The decision timestamp, feature availability, producer build, and outcome measurement have different meanings.

## Scientific and release boundaries

The three research returns must keep selection skill, entry-timing skill and combined policy outcome separate. The comparison must use the same contemporaneously selected stock cohort. Waiting costs, missed uninterrupted leaders, turnover, liquidity, overlapping macro episodes and realistic execution remain part of the outcome. No probability, horizon, indicator weight, or options increment is promoted by this consumer patch.

Release is held for the exact temporal repair, full relevant tests, independent exact-head review, required concluded CI and any separately authorized deployment proof. Parent product status remains unfinished. The code is an unmerged candidate with known failures, not PROVEN_LIVE.

## Continuation without rediscovery

Continue on the existing operation/workspace after actual tool-state reconciliation. First resolve the two retained temporal tests on that carrier; then rerun the complete candidate suite and actual source proof. Independently consume the three research returns and reconcile their proposals against existing RIC/#7088, TOI/Radar, Options and Evaluation owners. Do not repeat the completed six contract probes, this source fingerprint, the D0 census, or the 146-PR collision check absent a material invalidator.

GitHub owns this implementation/evidence. Macro Agent OS continuity publication remains owed through its accepted source-custody route; no duplicate Agent OS store is created in Mastermind. Link this child back to existing Macro #7088 without claiming ownership transfer, architecture acceptance, or release of its held work.


## Continuation checkpoint: temporal repair and actual consumer reach

Fresh protected procedure was loaded from `7a191cc11039199843d4734c7df8d5523280e09c`; the required Skillpack files were fetched at that exact commit and their bytes matched the previously read compatible bodies. The operation, branch, and source-custody workspace remain unchanged.

The original Studio/Remote Desktop Commander path accepted the guarded temporal repair after same-workspace reconciliation proved the prior repair had not applied. The earlier source-repair freeze above is historical, not a permanent claim that source editing is unavailable. No alternate source carrier, replacement branch, or duplicate worker was used.

The two original failures are repaired. Added discriminators also exposed impossible date-only availability/build chronology; those are now withheld. US observation dates use America/New_York with daylight-saving handling, not UTC or the host-local timezone. Missing timezone data and out-of-range conversion withhold affected values, never substitute a guessed offset. US market/timezone metadata is explicit. No missing timestamp is manufactured.

The expanded rates, decision-context, Neural Web decision-policy, and portfolio-intelligence set completed successfully: 149 tests on the existing Python 3.14 interpreter. These are code-level results, not efficacy, live deployment, or a complete production generation.

The actual repository gate discovered 633 modules with zero exclusions, then stopped during collection with 37 errors. Named missing dependencies include claude_agent_sdk, PyJWT, MCP, reportlab, and the CI-pinned Macro engine/lib import surface. This is not a passing full gate. The attempt to provision an isolated Python 3.12 environment and materialize the CI dependency was safety-blocked; same-device readback confirmed both requested destinations remain absent. That exact provisioning action is not repeated or delegated. The existing hosted repository workflow remains the authoritative independent CI environment; its current-head result is still owed.

A source-level consumer trace found that get_regime intentionally returns a compact decision-context subset and does not include the new maturity evidence. PM prompt rendering also does not render the new field. Preservation by prompt_summary alone is therefore not proof that every actual reasoning seat receives it. The next bounded read-only consumer extension must preserve the existing tool server and transport boundary, not introduce a new scorer or data service.

The newly scoped tool-server path check examined all 164 open PRs and complete file pages. It found existing owners #495 (strict JSON response boundary) and #772 (unavailable overnight evidence). No shared tool-server edit has been made at this checkpoint. Their source/hold state must remain intact and compatibility must be established before adding a separate rates read.

Release remains held for complete relevant verification, independent exact-head review, concluded hosted CI, and separately authorized production proof. The broader leader-pullback suite, its research assignments, and the requirement for a genuine Prophet/Terminal consumer remain unfinished. Local test-environment provisioning failure is lane-local, not a mission-completion state.


## Actual SDK consumer proof and expanded verification

A pre-existing Python 3.12.13 test runtime was used read-only, with bytecode writes disabled; no package was installed or changed and no sibling environment receipt was rewritten. Its actual SDKs were claude-agent-sdk 0.2.152 and MCP 1.28.1. This was independent of the blocked new-environment provisioning action, which remains unapplied.

The new read was first tested absent: all eight integration tests failed on the missing registration/handler. After implementation, the actual decorated handler and SDK in-memory client/server transport passed file-backed tests for normal data, missing source, missing availability, isolated stale maturity, safe exception handling, payload bounds, and unchanged source/output state. No fake SDK or external provider/model call was used.

The expanded Python 3.12 rates, decision-context, Neural Web, portfolio, existing MCP and PM suites collected **192 tests: 191 passed, one pre-existing live-Macro-data skip, zero failures/errors**. The authoritative full repository gate in the same existing environment discovered **634 modules, zero exclusions**, but could not collect two Macro-dependent modules because the CI-pinned `vendor/macro_src/engine` and `lib` dependency is absent. Do not report the full repository gate as green, install a new exclusion, or route around the blocked dependency-materialization action. Hosted CI uses its normal canonical dependency checkout and remains a distinct proof gate.

The same captured real RIC source and immutable real regime source identified above were passed through `decision_context.build(write=False)` -> `get_rates_evidence` -> actual SDK `tools/list` + `tools/call`. The complete 3,223-byte UTF-8 response retained five dated maturity rows, zero timed-availability rows, explicit false replay certification and all-false trade authority, with no transport truncation. Wire SHA-256: `adbece143271611479af6fdfc9576e01fcb838026ce57bbb94d31aa9391bf68d`. The earlier explicit cutoff still admits zero rows. Market-view and Neural Web were intentionally absent and are not represented as complete production inputs.

Private detailed receipts: `sdk_real_input_proof_20260917.json`, `tool_sibling_compatibility_20260917.json`, `rates_related_py312.xml`, and `repository_gate_existing_py312.log` in the existing operation evidence directory. Raw licensed source data is not committed. Code-level SDK proof is not a deployment, a provider's actual use of the tool, browser proof, a stock-selection model, entry forecast or positive trading result.

Highest-leverage next integration: use the existing Prophet candidate-episode and Intelligence Vector owners, not a new opportunity/event store. Current `prophet.intelligence_vector/v1` is an explicitly closed earnings-family projection with all-false action authority and an episode decision cut. A rates family requires that owner's reviewed contract extension and current-data versus vintage qualification, not inserting rates into its earnings payload or claiming current readings existed when an old episode opened. The separate research returns will determine proposed signal methods; this child supplies inspectable inputs only.


## Final dated-context hardening

Matching source dates are alignment, not present-session freshness. A controlled case with both RIC and regime dated January 2, 2020 exposed that the previous `current_context` name could overstate what was actually checked. The normal mode is now `dated_context`; `current_session_freshness=not_certified` and `freshness_basis=aligned_to_supplied_market_asof_not_current_session` are explicit. This preserves useful historical/delayed context without claiming a live session, introducing a second calendar, guessing a freshness threshold, or manufacturing availability.

The new stale-whole-snapshot and cutoff discriminators failed before the amendment. Final related Python 3.12 verification is **194 collected / 193 passed / one existing absent-live-data skip / zero failures or errors**. A new actual SDK round trip on the same immutable real inputs yields **3,341 UTF-8 bytes**, no truncation, five dated rows, zero timed rows and zero rows at the earlier cutoff. Wire SHA-256: `b6c0cc012ef42ca4f9b0a2862fb500b964b1b233a0744e8f0bc555642dcad4f1`. The exact #495 proposed serializer preserves this amended payload as well. The earlier 191-pass and 3,223-byte receipts remain historical proof of their earlier source, not the final head's counts.

Latest same-revision procedure set was reloaded at `320f586126b7c82c843ef17612f12d40d20a42e0` after material source-law movement. Its new maintenance-release technical writer-gate requirements were inspected. This session does not transfer/release source custody, commission a maintenance release, or assert a technical writer gate. The current operation and original workspace remain bound. A fresh GitHub CLI read reports the branch MERGEABLE/BEHIND; current protected movement is disjoint from this candidate's code. A conflicting cached connector mergeability flag was not used to rebase or replace the branch. Latest-base integration and independent review remain separate release obligations.

The controlled current B1 fractional-second ordering finding is durable in existing Macro parent #6805 comment 5722218553. Its reproduction does not establish a production occurrence and is not permission to mutate B1 here. Preserve candidate/event serialization and content IDs; fix comparisons through that owner and its correction/replay contract. Parent #7088 comment 5722244567 and sibling #495/#772 compatibility notices carry cross-program pointers; they are not Agent OS source updates or worker execution claims.


## Stock-context continuation: existing one-call consumer

The current continuation extends the existing `get_ticker_package` read, not Prophet candidate identity, D5's earnings-family schema, B4 Entry Availability, or any scoring engine. It calls the already-added `get_rates_evidence` owner and keeps the original stock intelligence, lenses, intake and scores. Separate artifact dates, `US_market_context_only`, `ticker_specific_sensitivity=not_provided`, and `decision_time_join=not_established` prevent current market context from being presented as historical candidate evidence.

Regression-first verification covered the absent stock-context interface, the previously disconnected real function body, preservation of stock evidence under payload pressure, and a lossless compact rates representation. The final locally executed dependency-free set is **94 passed, zero failures/errors/skips** across rates, decision context and the new stock-context tests. The source-body harness removes decorators and isolates unrelated lens/intake reads: it is explicitly not an SDK transport test. Three additional actual-SDK tests are written for the existing repository CI, but were not executed locally in this continuation.

The existing generic JSON serializer is not rewritten. The addition first preserves full object-form rates. Where necessary, `decision_context.rates_evidence.columnar.v1` factors repeated per-maturity fields into `series.shared`, with variable fields in `series.columns` and `series.rows`. `source_schema` names the original projection, and `rates_context_encoding=columnar_shared_fields_v1` makes this presentation explicit. Every value, missingness field, clock, unit, caveat and authority field reconstructs exactly. If neither representation fits without losing previously deliverable stock information, the entire rates addition is explicitly omitted with the dedicated read tool named; impossible whole responses return bounded unavailable status.

A real, same-Macro-commit input exercise used `60db17a8e6e59381f0648f110035b1cc513da638`: the existing 3,401-name intelligence artifact (September 17) and RIC/regime artifacts (September 16). The intelligence bytes are 4,314,267, Git blob `06e04058503756b1e40273f9b53accfd91f3db55`, SHA-256 `0430f8f061d5f1a08e65ae61ccc305576a313ce1eb65b86443279d7aede2396a`. Existing RIC/regime hashes above are unchanged. No data provider was called, and the held INTC live/history operation was not touched.

The source-body differential compared old `c432bd62` behavior with the new consumer over all 3,401 names. Results: **3,300 complete object-form rates; 46 complete lossless columnar rates; 38 explicit rates omissions retaining stock evidence; 17 explicit whole-response transport-unavailable results**. Available outputs changed zero preexisting serialized stock fields, and reconstructed rates changed zero facts. Every response was at most 8,000 UTF-8 bytes. AMD retained the complete rates contract at 7,826 bytes and NVDA at 8,000 bytes; both kept September 17 stock and September 16 rates dates separate, with zero timestamped-availability rows. These are captured-input/source-body results with lens/intake owners deliberately absent in both comparisons, not full production, SDK, provider-consumption, freshness, historical replay or alpha proof.

Detailed private evidence remains in the original operation evidence directory: `stock_context_real_input_proof.json`, `stock_context_real_input_pre_columnar.json`, `verify_stock_context_snapshot.py`, input-capture receipts and source snapshots. The initial failed-fit result is preserved, not overwritten as if AMD/NVDA always worked. Raw licensed data is not committed.

The final proof binds source hashes: rates_evidence.py `4561c77b1aee1977c2929cef586012fcca4593a0b61b53bc293ee901f84a447f`; decision_context.py `7eae40ae0e0dbe70030c9fa97120dcce3790d7391411a08f9c23e1bc41d26032`; bot_mcp.py `5f3c4834a3b33baf2a8acdff41007c480e64ed244be40dcfaca24df07aa8712e`.

A local existing-SDK-runtime/access probe was refused by the platform, and was not retried through another environment. A later combined AST/compile/status verification request was also refused without a process receipt. Those exact operations remain held; the earlier successful 94-test and real-source-body receipts are not promoted to new SDK/full-gate proof. Existing normal hosted CI, independent review and release evidence remain separate requirements. No new dependency installation, provider call, generic serializer rewrite, merge, deployment or trade authority is claimed.

Current original operation/workspace/branch remain bound. The three Chairman-delivered research assignments are untouched. Next: publish this same-source candidate as Draft/HOLD, obtain exact-head actual-SDK/required CI and independent review through allowed paths, then integrate through existing Prophet/TOI/Radar owners only after their evidence/contract gates are met. Macro Agent OS synchronization is still owed via accepted Macro source custody; a GitHub comment is not that synchronization.
