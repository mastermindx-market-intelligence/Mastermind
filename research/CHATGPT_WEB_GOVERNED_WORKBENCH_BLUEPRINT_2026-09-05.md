# ChatGPT Web Governed Workbench — Integration Blueprint

## Binding Decision-First current-protected supersession — 2026-09-08

**Marker:** `PR490_DECISION_FIRST_CURRENT_PROTECTED_SUPERSESSION_20260908`
**Protected authority consumed:** Mastermind PR #521, protected in `185dc742dac94d39bcbca81d20d89963ed36f744`.
**Capability ceiling:** `SPEC_ONLY / RECORDS_ONLY / PRODUCTION_INERT`.

Decision-First **Today** is the sparse default Chairman experience: coverage-qualified act/no-act, admitted decisions and attention, Sol accountability, material exceptions, and evidence drill-down. Lifecycle, worker, provider, Workbench, evidence, permission, binding, source, and diagnostic machinery belongs under explicit **Advanced** progressive disclosure. **Linear** remains the human portfolio/project projection rather than being duplicated by this product. **Ask Sol** remains `HIDDEN_UNTIL_GROUNDED`; model explanation cannot manufacture current company truth or authority. Missing admission coverage is `NOT_CLEAR`, never an empty or healthy zero. Owner-specific Workbench canaries are `ENABLING_INFRASTRUCTURE_NOT_CHAIRMAN_PRODUCT_COMPLETION`; source, CI, merge, install, rendering, production proof, and Chairman acceptance remain distinct.

This clause changes no route, UI, reducer, source reader, cache, lifecycle, provider session, permission, action, Linear object, installation, deployment, or production state. It preserves the governed long-run-leader thesis while making the Chairman outcome—not machinery—the default product ruler.

## 2026-09-08 governed direct-access amendment

**Marker:** `PR490-R4-COHERENCE-REPAIR-20260908`
**State:** `SPEC_ONLY / SOURCE_REPAIR_CANDIDATE / NO_RUNTIME_OR_INSTALL_AUTHORITY`

### User journey and profile

```text
current authorized human intent
→ exact Web conversation/generation attestation from existing Web-Sol/RuntimeBinding authority
→ list owner-issued permitted target options
→ request one opaque option and bounded scope
→ owner revalidates subject/conversation/policy/generation/occupancy/source
→ issue one expiring workbench_context_ref
→ read or prepare one bounded action
→ commit at most once through the existing owner
→ reconcile the same original action after response loss
→ observe live bytes/process terminal truth with explicit freshness/coverage
→ durable source/evidence receipt
```

`ATTENDED_WEB_OPERATOR` is a technical grant, not an organizational role or lifecycle identity. It does not fabricate Job/Attempt/Worker identifiers and does not require a second provider reasoning turn merely to carry out each already-authorized direct step. Tool access is deny-by-default at the profile/tool pair: a surface name grants nothing, worker-validation launch tools remain `BOUNDED_WORKER`-only, and attended workspace calls require the current owner-issued `workbench_context_ref` or an action/process reference already bound to it. Its ceiling is `ATTENDED_ONLY`; it proves neither unattended execution nor autonomous fleet capability.

Authenticated Workbench caller identity is not exact ChatGPT conversation identity. HC0 and Web-Sol v1 locator/probe evidence cannot create the grant. Executive supervisor requires a real Job/Attempt and remains a `BOUNDED_WORKER` path. Protected PR #471 provides `mastermind.worker_execution_contract/v1` for that Executive path, with provider homes and credentials adapter-private; it does not implement attended execution or RuntimeBinding. Remote Desktop Commander issue #507 stays under its own transport owner and safe-boundary law. Source Continuity is read-only verification, not writer or process owner.

### Owner-issued target selection

`list_permitted_workbench_targets` projects the existing resource owner. It returns opaque target options, policy generation, coverage, and observation time. `prepare_attended_context` accepts only one option, bounded requested scope, and non-authoritative correlation. The owner binds exact conversation, subject, target, source, policy/capability generation, scope, and expiry into `workbench_context_ref`.

An option is not an access grant. There is no ambient current target, first-online-device rule, model-selected host/cwd/root/branch/worktree, or implicit worktree/process/browser creation. A foreign, stale, revoked, or overbroad option is `REFUSED` before backend effect and does not alter any caller's current context.

### Real attended command route

Restricted worker validation recipes remain intact. The separately authorized direct route is:

```text
prepare_attended_command
start_attended_command
reconcile_attended_command_start
```

The model may request `command_text`, a shorter timeout, and a smaller output ceiling within the bound context. The existing process owner derives cwd, executable/shell policy, environment, network, resources, principal, source, timeout ceiling, output ceiling, cleanup, and target identity. `start_attended_command` consumes one owner-issued launch reference at most once. Lost launch response is reconciled through that same reference; no blind retry, provider switch, backend switch, carrier switch, or process two is allowed while effect is unknown. This route may eventually prove real exit `0` and exit `7`, but source text alone proves neither.

### Effect and action history

The universal modifying truth is:

```text
NOT_APPLIED | APPLIED | EFFECT_UNKNOWN
```

`REFUSED` is response status, not effect truth. `PARTIAL` is owner-native/per-step outcome evidence, not a fourth universal state. Commit permission, receipt-read permission, and durable original-action correlation are separate. Revocation or expiry blocks new commit; it cannot erase a historical effect. A separately authorized successor may read the old result but may not execute it. Evidence-retention exhaustion yields `EFFECT_UNKNOWN` and blocks modifying continuation; it never yields a guessed `NOT_APPLIED`.

### Process observation

`read_process` uses `process_ref` plus observer-scoped `observer_cursor_ref`. Two observers never consume one another's output. Every result carries process generation, retained range, explicit gap ranges, next observer cursor, terminal state, exit code, truncation, and observation time. Terminal, cancel, effect, cleanup, process identity, and retained/gap facts stay outside truncatable display text. A visible head/tail cannot prove that omitted output contained no failure.

### Live working-tree and search freshness

A read after an allowed patch returns the new live working-tree bytes and separately reports the unchanged committed baseline, owner-issued working-tree observation, file identity, content hash, and time. Git HEAD is not the identity of uncommitted bytes. Search reports requested live observation, indexed observation, coverage, freshness, truncation, and continuation. **Stale or incomplete search never claims exhaustive live content.** Reuse existing CodeIntel invalidation/refresh; do not create a second index.

Preimage checks and atomic rename are useful conflict detectors but are not exclusive-writer locks. Exclusive-writer claims require enforcement at the real owner/OS boundary. Where unmanaged writers may race, preserve that risk and verify the postimage.

### No-rebuild boundary

No new lifecycle, session, permission, source-selection, process, evidence, retry, queue, workspace, transcript, identity, or control plane is approved. The facade consumes PR #519, PR #529 composition evidence, PR #511 profile-census research, and existing Web-Sol/RuntimeBinding, resource/auth, process/host, CodeIntel, GitHub, Dialogue/Wake, and Source Continuity owners at their actual proof ceilings.

**Date:** 2026-09-05  
**Research carrier:** Mastermind PR #490  
**Parent operation:** `chatgpt-pro-long-run-leaders-research-20260905-sol-001`  
**Status:** `RESEARCH_CANDIDATE / RECORDS_ONLY / NOT_AN_ARCHITECTURE_FREEZE / NO_RUNTIME_AUTHORIZATION`  
**Historical protected basis of the 2026-09-06 continuation:** Mastermind `8f3370e349ab8f1a54acac4c63697740f32715b1`; `mastermind.sol_skillpack.v1` 1.0.1; bootstrap major 1.

This blueprint turns the Chairman's desired end state into one coherent integration candidate: ChatGPT web sessions should lead difficult, sustained projects, while exact workers and local tools perform bounded execution under existing Mastermind authority. It does not create a new workstream, app, MCP server, browser extension, worker, Job, Attempt, session registry, permission service, queue, watcher, credential, installation, release, or production effect.

The source basis moved after the first PR #490 research publication. The inspected move from `0106aaa832a8146810e0905d52b7d5444f6827f0` to `8f3370e349ab8f1a54acac4c63697740f32715b1` changed Source Continuity implementation/tests, not the protected Skillpack or these research paths. That comparison is a research grounding receipt, not release compatibility for any current implementation carrier.

---

## 1. Executive ruling candidate

Do **not** install Chat On Steroids with its default broad permissions into the Chairman's primary authenticated environment and do **not** adopt its prime/worker/session/history/loop system as Mastermind infrastructure.

Use it in three narrower ways:

1. **Reference implementation and disposable canary:** build a pinned upstream source revision in an isolated, non-sensitive environment to observe actual host behavior.
2. **Selective pattern adoption:** reuse or independently implement only bounded interaction patterns that improve model efficiency and operator ergonomics—batched reads, structured patches, durable process handles, current-permission enforcement, observation-bound desktop actions, and explicit incomplete/effect-unknown states.
3. **Upstream contribution where generic:** fixes that are broadly useful and do not expose Mastermind architecture may be proposed upstream after separate review. Mastermind-specific authority, identity, evidence and continuity behavior remains first-party.

The desired operating system already exists conceptually: Executive OS owns lifecycle/admission; Agent OS owns durable organizational continuity; RuntimeBinding owns exact rotating surfaces; Capacity owns eligibility/placement; Dialogue/Relay/Wake own return and attention; GitHub owns source/evidence; Steward/Control Room compose reads; Code Intelligence owns governed code discovery; Web-Sol/Worker Browser own bounded browser actuation. The Workbench is a facade over one owner-bound context—Leader read, Executive worker, or attended Web—and never another owner.

---

## 2. Product outcome and completion ruler

### Chairman job

Open a Mastermind ChatGPT surface, state a large outcome once, and observe that the organization:

- recovers the correct product/system intent;
- decomposes the work without shrinking the ambition;
- selects bounded workers and tools without routine account allocation by the Chairman;
- lets execution continue after an individual reasoning turn ends;
- returns blockers, evidence and results to the correct leader;
- transfers the responsibility safely when a conversation must rotate;
- rejects stale, duplicate or unauthorized actions;
- produces visible product or machine proof; and
- persists the accepted ruling and next action outside chat.

### Sol job

Use frontier reasoning for architecture, hard diagnosis, synthesis, adversarial review and acceptance—rather than file shuffling, polling, ceremonial status turns or repetitive commands.

### Worker job

Receive an exact mission and exact execution context, inspect and modify only its authorized workspace, run bounded validation, produce attributable evidence, and return through one canonical child path.

### 10/10 end state

One real, consequential program slice completes through:

```text
Chairman outcome
→ current-source Sol recovery
→ bounded CEO/worker admission
→ authority-mode-bound workspace and tools
→ durable execution
→ material return to the exact leader
→ difficult evidence-based Sol review
→ real product or machine-consumer proof
→ explicit child STOP
→ durable Agent OS/GitHub/Linear reconciliation
```

No Chairman tab hunting, message shuttling, account choosing, duplicate branch cleanup, or reconstruction from a dead chat is required. Every modifying path included in the claim is technically enforced or explicitly attended; unexamined native-app paths are not counted as autonomous.

---

## 3. Three authority profiles, not one super-agent

### 3.1 Leader read mode

Leader read mode is the normal Sol experience. It needs protected procedure, current company context, source-attributed Steward/Control Room reads, GitHub/CodeIntel evidence, and bounded CEO-intent submission only where production-proven. It has no unrestricted filesystem, shell, desktop, credential, account, source-writer, or lifecycle authority.

**Leader read binding** is issued by the existing read/resource owner. It binds subject, permitted resource and scope, source/capability generation, expiry, freshness, and correction behavior. It does not synthesize a Job, Attempt, Worker, project, worktree, or process.

### 3.2 Executive worker mode

Executive worker mode is an exact admitted Job/Attempt/Worker. It may receive a local Workbench only when existing owners inject and enforce:

- exact `job_ref`, `attempt_ref`, `worker_ref`, responsibility and operation identity;
- exact repository/worktree, branch, source head/base, and allowed path set;
- execution profile, network, resource, environment and secret policy;
- provider-neutral worker contract values from protected PR #471;
- effect-reconciliation owner and current RuntimeBinding generation where relevant.

**Executive worker binding** is lifecycle-bound and short-lived. Concrete provider adapters exclusively own provider homes, credentials and launch mechanics. A Business subject, plugin connection, browser title, or arbitrary host path cannot manufacture this binding.

### 3.3 Attended operator mode

`ATTENDED_WEB_OPERATOR` is a separately granted task-scoped technical context for the exact current Web conversation and generation. It does not fabricate a Job, Attempt, Worker, source-writer assignment, organizational role, account election, or unattended autonomy claim.

**Attended Web binding** contains `workbench_context_ref`, exact conversation identity, conversation generation, one owner-issued target/source/scope, policy and capability generation, expiry, revocation behavior, and reconciliation ownership. It may request bounded file/process work only through the existing resource/auth/process owners. Worker validation remains unavailable unless a real Executive Job/Attempt is separately admitted.

A profile-scoped CENSUS from protected PR #511 is not this binding. Authenticated selected-project read composition from PR #529 proves one read path, not exact conversation attestation or general mutation/shell/process authority.

### 3.4 Why this split matters

Combining leader authority, broad host access, browser credentials, lifecycle execution, and attended direct tools in one long-lived principal would widen blast radius and blur stale-session fencing. Three explicit profiles preserve one premium cockpit while keeping every action attached to the owner and identity that can truthfully authorize, observe, reconcile, and correct it.

## 4. Account-specific product routes

### 4.1 ChatGPT Business / Premium

Proposed user journey:

```text
Business ChatGPT leader
→ Mastermind Sol skills
→ authenticated Steward read
→ exact evidence from native GitHub/Linear/Slack as appropriate
→ separately confirmed bounded Executive admission
→ Executive/Capacity selects and binds a worker
→ optional Mastermind Operator + Workbench apps on the exact worker surface
→ evidence/result returns to leader
```

Business custom MCP write support makes a direct workbench technically possible, but it must not be exposed as a general Chairman tool. Use separate independently versioned applications and OAuth resources:

- Steward: read-only company truth composition;
- Executive: bounded root intent admission;
- Dialogue: exact worker return path;
- Workbench: authority-mode-bound file/process tools, if approved;
- Desktop: optional exact worker-browser/computer tools, if approved;
- Admin: separate normally disabled installation/credential infrastructure surface.

The existing Business architecture's one-experience/federated-authority ruling remains controlling. The first useful production target is still the one-cockpit authenticated Steward read. Workbench does not enter that canary merely because it is desirable later.

### 4.2 Personal Pro

Personal Pro need not have direct custom-MCP writes to become a strong long-run leader.

Proposed route:

```text
Personal Pro Sol
→ protected Skillpack + approved read context
→ supported Slack/native app intent transport
→ existing dedicated Relay/CeoIngress
→ Executive OS / exact worker
→ GitHub + Steward + Dialogue evidence back to Sol
→ Web-Sol exact continuation only through its accepted closed protocols
```

The parity target is **project-leadership parity**, not identical local-tool entitlement. An authorized external worker or Codex Attempt performs the local actions. The Pro leader spends its turn on reasoning and acceptance. Do not disguise mutation as fetch/search or use the extension to bypass the platform's app permission model.

### 4.3 Codex and other providers

Codex remains the preferred coding execution surface when it already provides the required repository, terminal, process and thread behavior. The workbench should reuse the existing provider adapter and attempt binding rather than wrapping Codex in screenshot automation.

Claude, Cursor, Grok/OpenClaw or another provider can supply a bounded execution/computer capability when their exact current host and permission contract is demonstrably useful. None becomes a second scheduler, CEO, memory owner, retry engine or account elector.

---

## 5. Target topology

```text
                              Chairman
                                  │
                     ┌────────────┴────────────┐
                     │                         │
             Personal Pro Sol          Business Sol cockpit
                     │                         │
       Slack/approved native action      Sol plugin + apps
                     │                         │
                     └────────────┬────────────┘
                                  ▼
                         Executive / Steward
                                  │
              ┌───────────────────┼───────────────────┐
              ▼                   ▼                   ▼
        Executive OS          Agent OS          RuntimeBinding /
        Job/Attempt          durable state       Capacity / Wake
              │                                       │
              ▼                                       ▼
     exact worker/provider  ◄──── Dialogue / material return
              │
      ┌───────┼───────────┐
      ▼       ▼           ▼
  CodeIntel  Workbench   Worker Browser/Desktop
   reads     file/process  optional exact actuation
      │       │           │
      └───────┴─────┬─────┘
                    ▼
           GitHub/evidence/product proof
```

No arrow creates a new company truth store. Every surface is removable; deleting it removes access, not the underlying organizational fact.

---

## 6. Proposed Workbench surface boundary

This is a semantic design candidate, not a final MCP schema or authorization to add tools.

### 6.1 Discovery surface — read-only

| Candidate operation | Model-authored request | Owner-bound context | Minimum truthful output |
|---|---|---|---|
| `workspace_manifest` | none | one permitted target plus the applicable Leader read, Executive worker, or Attended Web binding | bounded roots, source identity, permissions, capability generation, coverage and degradation |
| `read_workspace_files` | relative paths and bounded ranges | exact owner-selected root/read policy and current mode binding | content/artifact, file identity, content hash, committed baseline, live observation, truncation, continuation and time |
| `search_workspace` | query, bounded glob/language/ref | exact owner-selected target plus existing CodeIntel generation | matches, requested live observation, indexed observation, coverage, freshness, truncation, continuation and time |
| `workspace_semantics` | symbol/reference query | exact owner-selected target plus accepted CodeIntel backend and current mode binding | definitions/references/diagnostics, committed baseline, live and indexed observation identities, coverage, freshness and time |
| `view_workspace_image` | relative image path | exact owner-selected read root/media policy and current mode binding | artifact, dimensions, file identity, content hash, committed baseline, live observation, coverage, freshness and time |

These operations compose the existing filesystem, Workbench-read, CodeIntel and GitHub owners. Protected PR #529 strengthens the authenticated selected-project read-composition proof but does not install a service or grant any broader tool.

### 6.2 Change surface — authority-mode-bound

| Candidate operation | Required semantics |
|---|---|
| `prepare_workspace_patch` | validate closed relative paths, current mode binding, expected live/committed preimages, permission, size and candidate diff; return one opaque short-lived preparation |
| `commit_workspace_patch` | consume only that owner-issued preparation, revalidate authority/source/binding, apply at most once and return per-path post-images plus common effect truth |
| `reconcile_workspace_patch` | read the original action through its owner after timeout, revocation, rotation or response loss; never create a second action identity |
| `workspace_diff` | report committed baseline and live working-tree observation separately, with path/binary coverage, freshness, truncation, continuation and observation time |
| `checkpoint_workspace` | project existing Source Continuity facts; no commit, push, transfer, retry, release or ownership authority |

The patch request may carry unified text or a structured edit representation, but never an absolute root, branch, worktree, credential, Git ref update, force operation or target account. Executive worker mutation requires its real lifecycle binding. Attended mutation requires the current Attended Web binding and an existing owner willing to issue the preparation. Leader read has no patch commit authority.

Cross-file crash atomicity is not inferred from preflight. A lost write response preserves `EFFECT_UNKNOWN` until the same owner-native action is reconciled; no provider, account, backend, carrier or receiver failover is allowed.

### 6.3 Process surface — mode-separated existing execution owner

The process owner remains canonical. Two entry paths are deliberately distinct:

1. **Executive worker validation.** A real Job/Attempt may use `list_validation_recipes`, `prepare_validation_start`, `start_validation`, and `reconcile_validation_start` under the provider-neutral worker contract and adapter-private launch mechanics.
2. **Attended command.** A current `ATTENDED_WEB_OPERATOR` context may use `prepare_attended_command`, `start_attended_command`, and `reconcile_attended_command_start` when the existing process/host owner has separately approved the safe boundary. This path does not invoke an Executive supervisor or create a shadow Job/Attempt.

Both paths may then use owner-bound `read_process`, `send_process_input`, `request_process_cancel`, `reconcile_process_input`, `reconcile_process_cancel`, and `reconcile_process` only where the original process reference and generation permit them. Observer cursors are independent; terminal, exit, cancellation, effect, cleanup, retained-range and gap facts remain outside truncatable output.

The caller may request command text, a shorter timeout and a smaller output ceiling. Cwd, executable/shell policy, environment, network, resource policy, principal, source, target, cleanup, timeout ceiling and output ceiling remain owner-derived. A shell string that merely starts in an approved folder is not a sandbox. Cancellation request receipt is not terminal proof, and elapsed time does not prove an underlying worker stopped.

### 6.4 Desktop surface — separate optional connector

Desktop/computer schemas should never inflate the normal coding/read surface or be automatically enabled.

Candidate operations:

- `observe_worker_desktop`: exact approved profile/window/app class, redacted screenshot and semantic controls; returns `observation_ref` and expiry.
- `act_on_worker_observation`: accepts the observation plus a closed control/action, revalidates current window/control/profile and executes a bounded sequence.
- `reconcile_desktop_action`: returns completed step count, exact uncertain boundary and observable postcondition.

V1 should avoid generic screen coordinates, arbitrary JavaScript, clipboard reads, credential surfaces, account switching and arbitrary native-app selection. Any coordinate fallback belongs only to disposable evaluation after explicit review. A stale observation refuses rather than clicking the current occupant of old coordinates.

### 6.5 Dialogue surface

Workbench actions do not replace Company Dialogue. The exact worker still uses the existing semantic return channel for:

- pickup and start;
- progress when materially useful;
- blocker or decision request;
- result and evidence references;
- explicit Sol continuation or terminal stop.

The provider harness may mechanically produce these frames where accepted. The model should not need to remember etiquette to make completion observable.

---

## 7. Host-injected authority envelope

Every Workbench call is evaluated against common owner-bound context plus exactly one mode-specific binding.

Common owner-bound context contains:

```text
schema / capability generation
principal / resource / granted scope
responsibility_ref and operation_key
owner-selected target and source identity
allowed read/write/process/desktop capability set
network, environment, resource and secret policy
observed_at / expires_at / revocation generation
owner-specific reconciliation reference
```

**Leader read binding** adds the permitted read target, source/freshness ceiling and no-mutation rule.

**Executive worker binding** adds exact `job_ref`, `attempt_ref`, `worker_ref`, repository/worktree/branch, source head/base, expected path/blob state, execution profile and provider-neutral worker values.

**Attended Web binding** adds `workbench_context_ref`, exact current conversation identity and generation, one owner-issued target/source/scope, policy/capability generation, expiry and revocation behavior. It never synthesizes Job, Attempt or Worker identifiers.

The model sees only opaque references and useful public policy descriptions. It never supplies a UID, credential, host, account, browser profile, absolute path, branch, worktree, lease, fence, capability generation, privileged role or effect state. Caller content/path/hash/source assertions are mismatch-only; matching text grants nothing.

The relevant owner revalidates the envelope at invocation time and, after awaited I/O, revalidates any binding that could have changed. A cached schema or earlier successful read cannot keep a revoked capability alive. One action remains authority-mode-bound for its lifetime and cannot be remapped between profiles after timeout, rotation or succession.

## 8. Effect and correction contract

### 8.1 Common effect vocabulary

```text
NOT_APPLIED | APPLIED | EFFECT_UNKNOWN
```

`REFUSED` is response status. `PARTIAL` is owner-native/per-step outcome evidence, not common effect truth.

An individual existing owner may use its frozen vocabulary; adapters map only when the mapping is lossless and reviewed.

### 8.2 Required response fields

Every nontrivial call should expose, directly or by immutable evidence reference:

```text
operation / call identity
status and effect state
observed_at and source/capability generation
exact target owner
input fingerprint where safe
precondition and postcondition evidence
completed step count for batch actions
truncated / continuation / omitted coverage
error code from a closed vocabulary
reconciliation owner/reference
```

### 8.3 Retry law

- Refused before effect: bounded same-operation retry may be eligible only after the stated predicate changes.
- Applied with receipt: do not repeat.
- Lost response after possible effect: `EFFECT_UNKNOWN`; reconcile the same target/operation.
- Partial desktop or patch sequence: preserve completed steps; never replay the whole sequence blindly.
- Cancellation requested: reconcile terminal state and already-applied effects.
- Tool unavailable or schema mismatch: do not silently substitute another provider, account or app.

---

## 9. Session continuity and extension behavior

### 9.1 Extension role

The existing Mastermind extension/native host should remain a narrow exact-session actuator and observer. It may later supply:

- exact approved profile/Project/conversation inspection;
- foregrounding;
- closed successor creation/bootstrap/verification under current CR law;
- bounded turn-health observations;
- exact observation references for approved worker-browser cases.

It should not become the organizational transcript database, task registry, source of worker identity, newest-tab elector, account router, arbitrary prompt sender or generic desktop automation bus.

### 9.2 Turn signals

DOM/Fiber/visible-message signals are useful for local health and recorder reconciliation. They are not lifecycle, semantic ACK, completion, provider effect or authority. `completed`, `failed`, `stopped`, `interrupted`, `stalled` and `unknown` should remain distinct. Visible prose is not sufficient completion evidence.

### 9.3 Long-running execution

A leader turn may end while an exact worker/process continues. Deterministic services hold that state. A material return wakes or reaches the exact current leader through the existing Wake/Dialogue path—or truthfully reports that exact wake is unavailable. No reasoning model should stay alive solely to poll.

### 9.4 Rotation

When rotation is necessary, use the existing succession sequence: reconcile effects; ensure durable continuation; resolve exact target; create one successor; deliver one bootstrap; verify; obtain accepted semantic readiness; advance RuntimeBinding; fence predecessor; update navigation; perform the next governed action.

A process handle, open SSE stream, local session recording or title-matched new tab does not satisfy succession.

---

## 10. All-reachable-mutator coverage

For every autonomous modifying claim, construct a current configuration-specific inventory:

```text
R = every modifying route reachable from the exact chat/operator configuration
E = routes whose actual actuator enforces current identity, scope, binding, source and effect predicates
D = routes technically denied or unavailable
A = routes intentionally retained as attended human-confirmed actions
U = unknown/unexamined routes
```

The autonomous claim is bounded to `E ∪ D`. `A` is supported attended capability, not autonomous stale-session fencing. Any nonempty `U` blocks the stronger claim and must be reported, not averaged away.

Candidate route families:

- Mastermind Executive MCP;
- Company Dialogue MCP;
- native GitHub actions;
- native Slack/Linear actions;
- local Workbench file/process actions;
- browser/desktop actions;
- provider-native coding actions;
- administrator/installation actions;
- any connected third-party app with write semantics.

The inventory is a proof artifact from existing owners—not a new mutable permissions registry. Never test it by attempting an unauthorized production action.

---

## 11. Security architecture

### 11.1 Host isolation

An unattended Workbench needs an enforceable boundary such as a dedicated worker principal/container/VM with:

- exact worktree mounted at the reviewed access mode;
- no Chairman home directory, browser cookie store, Keychain or unrelated project mounts;
- scrubbed environment and no ambient cloud/provider credentials;
- process group ownership and deterministic cleanup;
- bounded CPU, memory, disk, process count and elapsed time;
- default-denied network or exact reviewed egress;
- controlled executable/runtime provenance;
- secret-safe output and logs;
- immutable build/server/capability identity.

A GUI permission dialog, folder picker or starting working directory does not establish those properties.

### 11.2 App separation

Keep read, intent, dialogue, workbench, desktop and admin resources independently authenticated, versioned, disabled and rolled back. A token for Steward must not invoke Workbench; a Workbench token must not submit CEO intent; desktop permission must not imply filesystem or clipboard access.

### 11.3 Untrusted content

Repository files, tool results, screenshots, Slack messages, issues and model output are data. They may contain instructions but cannot add tools, widen path/network scope, select credentials, change actor, authorize a retry, or override the outer current Chairman/source-law gates.

### 11.4 Recording and privacy

Do not record entire ChatGPT transcripts or desktop streams by default. Preserve the smallest required evidence: exact user-visible operation intent, tool call/receipt, source references, result and durable handoff. Any local recorder needs encryption, retention, access and deletion policy and still remains advisory evidence—not Agent OS.

### 11.5 Supply chain

Any upstream component evaluation must pin source and dependency locks, produce an SBOM/license inventory, build internally, scan the artifact, sign/notarize according to the owning deployment policy, and prove rollback. Preserve the MIT notice for copied substantial portions. A public release binary's existence is not sufficient supply-chain acceptance.

---

## 12. Closed degradation vocabulary candidate

```text
CAPABILITY_UNAVAILABLE
CAPABILITY_GENERATION_MISMATCH
PRINCIPAL_OR_SCOPE_REFUSED
WORKBENCH_CONTEXT_MISSING
WORKBENCH_CONTEXT_EXPIRED_OR_REVOKED
EXACT_CONVERSATION_IDENTITY_MISMATCH
TARGET_OPTION_INVALID_OR_STALE
ATTEMPT_BINDING_MISSING
ATTEMPT_BINDING_STALE
WORKSPACE_IDENTITY_MISMATCH
SOURCE_PRECONDITION_FAILED
PATH_OUT_OF_SCOPE
CODEINTEL_COVERAGE_INCOMPLETE
OUTPUT_BOUNDED
OUTPUT_UNREPRESENTABLE
PROCESS_START_UNKNOWN
PROCESS_STILL_RUNNING
CANCEL_REQUESTED_NOT_TERMINAL
PROCESS_EFFECT_UNKNOWN
DESKTOP_OBSERVATION_STALE
DESKTOP_TARGET_CHANGED
DESKTOP_ACTION_PARTIAL
PROFILE_OR_PROJECT_MISMATCH
MODEL_MODE_UNVERIFIED
TARGET_DELIVERED_UNACKNOWLEDGED
SEMANTIC_READINESS_PENDING
RUNTIME_BINDING_STALE
WAKE_UNAVAILABLE
CONTEXT_ROTATION_REQUIRED
NATIVE_MUTATOR_COVERAGE_INCOMPLETE
EFFECT_UNKNOWN
```

Do not turn these into one generic `ERROR` or `FAILED`. Several are truthful held/degraded states with different safe actions.

---

## 13. Proposed proof sequence mapped to existing owners

These are candidate proof slices, not official waves or assignments.

### Slice 1 — one useful authenticated read

Existing BSC/Steward owner proves one selected Business cockpit can recover useful nonempty company context with exact app/schema/source identity, explicit degraded cases, post-token-expiry refresh and rollback. Two Personal-Pro controls remain unchanged.

### Slice 2 — read-only repository investigation

Existing CodeIntel/GitHub and worker-sandbox owners let one exact synthetic Attempt inspect a repository, search symbols, read files and an image, while refusing forbidden paths and incomplete-coverage absence claims. No source mutation.

### Slice 3 — bounded patch plus validation

One exact synthetic Attempt prepares and applies a small patch, runs a reviewed validation recipe and returns exact diff/test evidence. Lost patch/process receipts and stale source are injected. No commit, push, PR, provider or production effect.

### Slice 4 — durable process and material return

A validation process outlives its initiating ChatGPT turn. The existing execution owner preserves it; after completion, the current leader consumes a material result through the accepted Dialogue/Wake path. Duplicate start and cancellation uncertainty do not create a second process.

### Slice 5 — desktop observe and bounded actuation

A disposable profile/app proves redacted observation, semantic-control targeting, stale-observation refusal, one bounded action and partial-effect reconciliation. No credential/settings/account surface.

### Slice 6 — exact responsibility succession

Use the existing CR ladder to prove a dead predecessor is not required for recovery; one verified successor obtains semantic readiness and binding authority, and the predecessor is refused on every included autonomous route. Ambient native mutators are either denied, attended or explicitly outside the claim.

### Slice 7 — one real program vertical

A real product capability travels from Chairman outcome through leader, worker, evidence, acceptance and production consumer, with no Chairman shuttling and durable closeout. This—not the presence of tools—is the long-run leadership acceptance test.

---

## 14. Evaluation and operating metrics

Use the existing proposed 24-task matched evaluation and current derived machine-readable research corpus. Add Workbench-specific measurements only through an accepted evaluation owner:

- correct useful result rate;
- accepted capability per scarce reasoning turn;
- time to first useful decision;
- tool discovery and total call count;
- bytes/context consumed by repeated source;
- protective refusal and expected RED rate;
- genuine command/input error rate;
- duplicate/unknown effects;
- human interventions;
- recovery from dead predecessor;
- stale/incorrect ownership conclusions;
- product/machine consumer completion.

Do not optimize raw duration, raw call count or zero-error rate in isolation. A lower error count achieved by weaker tests or silent omission is regression. Elapsed wall time is not GPU-hours.

---

## 15. Build-versus-adopt matrix

| Component/pattern | Disposition | Reason |
|---|---|---|
| Batched file reads and bounded continuations | Adapt/reuse concept | High model-efficiency value; fit existing exact workspace owner |
| Structured patch operation | Adapt concept | Useful if expected-source and partial-effect behavior is stronger than broad write access |
| Long-running process handle | Reuse existing provider/Attempt capability; adapt UX | Required for turn-independent execution; must not create another process registry |
| Core/Desktop discovery split | Adopt design pattern | Reduces schema/permission/context blast radius |
| Observation-bound desktop action | Adapt through Worker Browser owner | Correct stale-target semantics and clear optional permission boundary |
| Invocation-time permission enforcement | Required | Cached schemas must not preserve revoked capability |
| Local session recorder | Do not adopt as organizational memory | Duplicates Agent OS/Dialogue and raises privacy/retention risk |
| Prime/worker registry and slot model | Do not adopt | Duplicates Executive/Capacity/RuntimeBinding |
| Automatic compaction as authority transfer | Do not adopt | Does not satisfy semantic readiness or fenced succession |
| OpenRouter goal/loop controller | Do not adopt as company controller | Partial evidence, extra data/cost path, model judgment without canonical receipts |
| Broad logged-in-user shell | Reject for unattended production | Approved folders do not confine execution |
| Clipboard and arbitrary desktop control by default | Reject | Unnecessary blast radius and secret exposure |
| Upstream release binary on primary host | Reject pending source-build canary | Unsigned/unnotarized and not reviewed under Mastermind host law |
| Small generic upstream fixes | Consider contribution | Reduces fork burden when no Mastermind-specific authority is exposed |

---

## 16. Implementation handoff requirements

Any later owner-approved implementation slice must include:

- one observable user/machine capability;
- current protected source and exact owning records;
- exact source paths and existing active carriers;
- explicit non-goals and no-rebuild boundaries;
- complete leader/worker/tool/consumer journey;
- host-injected identity and permission fields;
- source/time/null/coverage/correction behavior;
- deterministic versus model-generated method;
- effect, partial action, retry and cancellation rules;
- hostile content, path, process, desktop and native-mutator cases;
- ordered RED/GREEN and real consumer proof;
- immutable source/proof receipts;
- exact stop condition and continuation handoff.

No worker should receive “build the workbench.” It receives one of the proof-complete slices above after the responsible existing owner freezes the relevant contract.

---

## 17. Dated research capability snapshot

> This is a dated/historical interpretation. Current PR #490 continuation authority lives only in `research/CHATGPT_LONG_RUN_LEADER_CURRENT_STATE_SUPERSESSION_2026-09-06.md`.


| Capability | State for this research |
|---|---|
| Chat On Steroids source/reference assessment | `PARTIAL` — static pinned review, not executed |
| Mastermind Sol/Operator skills-only packages | `BUILT_NOT_PROVEN` as packages; current web/app canary separately gated |
| Authenticated Steward S1 source | current successor candidate exists, `BUILT_NOT_PROVEN / DRAFT_HOLD` at inspected evidence |
| One-cockpit authenticated Steward read | `NOT_PROVEN` |
| Executive MCP production write | inspected source exposes no production-write mode; live production capability not established here |
| Company Dialogue source | source exists; exact production/live state belongs to its owner |
| CodeIntel production read capability | `PARTIAL / active prerequisite work` in its existing carriers |
| Exact Workbench local-file/process app for ChatGPT web | `PARTIAL` — PR #519 read source and PR #529 hermetic composition proof exist, but installation, exact-conversation binding, mutation/process capability and production proof do not |
| Native profile census | `SPEC_ONLY / RESEARCH_EXECUTED / PRODUCTION_INERT` via protected PR #511; no installed CENSUS or Control Room fleet proof |
| Personal Pro project-leadership parity | `PARTIAL` architecture/transport pieces; end-to-end proof missing |
| Exact ChatGPT wake/resume and rotation | `PARTIAL / proof ladder incomplete` |
| All-reachable-mutator stale-session fencing | `NOT_PROVEN` |
| Real long-run program vertical | `NOT_PROVEN` |

This table is a research synthesis and must not overwrite a fresher canonical owner projection.

---

## 18. Historical review and owner actions

> This is a dated/historical interpretation. Current PR #490 continuation authority lives only in `research/CHATGPT_LONG_RUN_LEADER_CURRENT_STATE_SUPERSESSION_2026-09-06.md`.


### Primary

Freeze one immutable V8 semantic head on this same PR #490 carrier, publish it once, establish exact remote identity, regenerate the separate current-protected merge-tree/check receipt, and obtain one fresh full non-author exact-head architecture review. Keep Draft/Hold and all implementation verticals unadmitted until that review returns.

### Review adjudication

Sol consumes the exact-head review and issues one explicit ruling: bounded same-carrier repair, continued HOLD, or acceptance of the records-only architecture candidate. A passing review still does not install, execute, merge or prove production capability.

### Records-only protection

Only after explicit Sol acceptance may an authorized release owner perform current-base maintenance and expected-head records-only protection. The release must preserve the nineteen semantic paths, settle the exact effect, and stop on movement or uncertainty without retry or failover.

### Post-protection product continuation

After records are protected, select one independently useful owner-specific vertical—such as installed authenticated read, attended command, live worktree freshness, durable process return, or native census-to-Control-Room display—and issue a new complete operator handoff through its existing owner. Do not launch an umbrella implementation program or duplicate lifecycle, identity, permission, process, index, retry, transcript, queue, or state planes.

### Held

No primary-host Chat On Steroids install; no Workbench app publication; no arbitrary shell; no account/permission changes; no native CENSUS activation; no new Web-Sol composer authority; no exact-chat autonomy claim; no fleet rollout; no parallel task/session registry.

## 19. Sources

### Mastermind

- Action-time protected source and Skillpack basis for V8: `mastermindx-market-intelligence/Mastermind@185dc742dac94d39bcbca81d20d89963ed36f744`; PR #511 and PR #529 are consumed only at the proof ceilings stated above.
- Historical protected source/Skillpack basis for the 2026-09-06 continuation: `mastermindx-market-intelligence/Mastermind@8f3370e349ab8f1a54acac4c63697740f32715b1`.
- Original and integration research in this PR.
- `docs/CEO_BOOT_PACKET.md`.
- `docs/superpowers/specs/2026-08-29-business-sol-surface-convergence-design.md`.
- `docs/superpowers/plans/2026-08-29-business-sol-plugin-packages.md`.
- `docs/superpowers/plans/2026-08-30-sol-capability-fabric-tool-catalog.md`.
- `docs/EXECUTIVE_CHAT_NATIVE_SOL_HIERARCHY_LAW.md`.
- `docs/EXECUTIVE_WEB_SOL_CONTEXT_ROTATION_LAW.md`.
- `docs/AGENT_DIALOGUE_SESSION_CLOSE_LAW.md`.
- `integrations/mastermind_secretary_mcp/**`, `integrations/mastermind_company_mcp/**`, `integrations/executive_mcp/**`.
- Current Steward S1 successor evidence: PR #463 at the immutable head recorded in the integration annex.
- Code Intelligence organizational evidence: `macro@03a971458f4d2977a29e12fdae16a2a35553d2bc:agentos/workstreams/WS-CODE-INTELLIGENCE-FABRIC.md`.

### Chat On Steroids

Pinned review source: `totec448-spec/chat-on-steroids@dbff15b7296358102ee3e141f183e89a4a8d1e0e`, including README, SECURITY, LICENSE, tool-surface, MCP surfaces, platform support, extension manifest, turn-signal analysis and historical tool-error report. Newer upstream main was observed separately and is not silently mixed into the pinned conclusions.

### Current platform assumptions

Official OpenAI custom MCP, app permissions, plugin marketplace/skills, Secure MCP Tunnel, Codex App Server and terms documentation; official MCP Tasks specifications. These are current external dependencies, not permanent Mastermind law, and require revalidation at implementation/install gates.
