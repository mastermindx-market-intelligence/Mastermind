"""Tunnel-facing, owner-bound Workbench MCP adapters.

The package is an adapter over existing Workbench read primitives. It is not a
project registry, permission service, remote shell, retry plane, or lifecycle
owner.
"""
