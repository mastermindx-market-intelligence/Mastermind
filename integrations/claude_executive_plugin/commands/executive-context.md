---
name: executive-context
description: Recover the current Mastermind Executive OS mission frontier for COO judgment without creating a new effect.
---

Recover the smallest sufficient current Executive frontier for the active Mastermind mission.

Use only the existing authenticated `mastermind-executive` connector and read-only Executive/Workspace/Fabric operations. Do not invoke any CEO-specific modifying operation or invent a COO mutation path.

Return a compact working frame containing:

- selected workstream and root Job, if current;
- Executive readiness and relevant attention;
- current children and their lifecycle states;
- returned results that need principal consumption;
- accountable seat and owed turn when projected;
- effect/reconciliation/source-custody blockers;
- the highest-leverage next COO action that is already inside the accepted mission;
- any exact higher-seat or human gate that actually prevents that action.

Preserve these distinctions: admission is not START; delivery is not ACK; ACK is not execution; CI is not acceptance; a partial/historical projection is not current authority.

If the connected Executive surface is missing or unauthenticated, report that exact transport capability as unavailable. Do not ask for credentials, create a replacement server, or expose a private Executive endpoint.
